# TODO

Centralized backlog. Source-of-truth pointers under each section so the detail is one click away — keep this file an index, not a repository of context.

> **Conventions.** `[ ]` open, `[x]` done (rare here — done items move out, not get checked). Each item has a 1-line "what" + a parenthesized source pointer. Items rot — sweep this file when you touch a related area, prune what's no longer real, add what is.

---

## v0.6.0 build-attempt incidents — investigate + resolve properly (2026-07-16)

Three bugs surfaced during the aborted v0.6.0 release build (release deferred ≥2 days for comprehensive testing). Each got a fix **today**, but the user flagged all three for proper detective work: verify the fixes LIVE **and** close the process gap that let each get ship-adjacent. Do not re-attempt release until these are resolved.

- [ ] **Re-entry silent-advance ate a live departure PA** (auto-driver). Start-from-a-middle-station: on departure, a moving badge (PASSING) while the app was still parked at 1C woke re-entry, which silent-advanced AND set `departure_observed=True` — suppressing the real `FIRE_DEPARTURE`, so the departure announcement never played. Fixed today: deleted `or badge == "PASSING"` in `_resolve_reentry_target` (PASSING ≡ MOVING, the 900m arrival is the only difference) — **verified by TRACE ONLY**. Tomorrow: (a) confirm LIVE on a real drive (start-from-middle + a pass-through next station → the departure PA must fire); (b) audit the whole re-entry gate for other synced-departure / cold-boot / click-jump edge cases — it mis-fired on a *correctly-synced* departure, so the gate's "parked + game-moving ⇒ desync" assumption deserves a proper review. (`auto_input/driver.py` `_resolve_reentry_target`; `auto_input/README.md § Re-entry`.) **Update 2026-07-16 (2nd block):** root reframed = *incomplete spec* (the README re-entry table itself said `PASSING → 1A`, drifted from the truth table — spec+code born wrong together, so a coherence test wouldn't catch it). 3 T1 unit tests now lock it at the root — `_tests/t1_unit/test_reentry_target.py` (incl. the `PASSING@<30,parked → None` incident row) + `test_inferred_state.py` + `test_detector_update_seq.py`. The eaten-PA *symptom* proof (departure plays on a real drive) = the blocked **T3 replay harness** — see `_tests/README.md § Build plan` (4a harness buildable now, 4b real corpus blocked on a recording). (a) live-verify + (b) gate audit still open. A single-attribute re-entry hardening was considered + **ditched**.
- [ ] **Clean-state first-run invisible in dev** — v0.6.0 first-run showed the stale pre-TIMS grey `LanguagePicker`; never caught because dev/test `settings.json` is always populated, so that branch never runs locally. Fixed today: picker removed, first-run → OS-locale default + TIMS home language knobs persist; codified as `critical_lessons §6`. Tomorrow: act on §6 — re-walk EVERY clean-state / first-run path from a *deleted-settings* baseline for other stale screens, and consider a first-run smoke checklist so this class can't recur. **Update 2026-07-16 (3rd block):** detective root = *staleness-at-integration* (born-right-went-stale — opposite of re-entry's born-wrong); mechanism = **axis-blindness** (the flip-to-default commit re-walked the OOBE gate on the flag axis it was flipping but missed the picker on the orthogonal persisted-state axis — same redundancy logic, adjacent screens). Landed: `i18n.resolve_language` extraction (pure/screen-free = the regression guard) + **T1** `test_resolve_language.py` + **T4** `test_clean_frame_startup.py` (fixture-parametrized absent/valid/corrupt + `language_picker`-not-importable guard). Review mechanism built (per "next flow-switch needs a stale-code review"): integration-residue smell (`review-dirty` Lens 2 #13 + `vibe-check` #13), **Lens 4 test-integrity**, **INTEGRATION scope** (auto-escalate on a flow-routing change), §6 axis-blindness fold, `principles § Engineering rigor` testability bullet. **Still open:** (a) the delete-state re-walk of the OTHER first-run screens (OOBE / OCR-consent) for staleness; (b) the **T3 normal-launch full-wiring** test (screens-skipped, headless) — needs `main()` made display-free (logged as the T3 gap in `_tests/README.md`).

## Auto-input / OCR

The OCR auto-PA feature shipped in `feat(auto-input): OCR-driven auto-PA — in-process driver, PASSING badge, setup-screen toggle` (commit `65d0cd3`). Open work that didn't make that scope:

- [ ] **Multi-PA queue auto-advance.** `_next_pa()` plays one PA per call; multi-PA stops (transfer hubs with 3+ PAs) currently auto-fire only the first arrival, user manually fires the rest. Either fire multiple `pending_next_pa` flags spaced by audio duration, or have the simulator auto-chain. (`auto_input/README.md` § "Future enhancements".)
- [ ] **Resolution selector in main program.** `driver.py` auto-detects from bootstrap grab (no user input needed today). If multi-monitor or non-native-fullscreen edge cases surface, expose override in setup screen. (Deferred — auto-detect sufficient for current use.)
- [ ] **Separate-window debug panel.** Today the panel shares the LCD pygame window via sub-surfaces (no overlap, but same window). Could decouple via `pygame._sdl2.video.Window` for a fully separate OS window. Not blocking; deferred. (`auto_input/README.md` § "Future enhancements".)
- [ ] **STA auto-fire.** Not modeled — STA is IRL-manual (station master, not driver). Plumbing exists in 1b if needed later. (`auto_input/README.md` § "Future enhancements".)
- [ ] **Cross-attribute hardening, further cases.** Cross-attribute corroboration is primary; per-attribute confidence gates are secondary and fine where the element has a naturally wide real-vs-garbage gap. The two layers collaborate. 2026-05-08 black-screen-at-platform incident shipped both: (a) confidence gate in `classify_badge_state` — badge_diff > 50 returns `(None, diff)`, badge has a clean separator (real <15, garbage 60-110) so the gate is cheap and correct ([auto_input/README.md § Badge classification](auto_input/README.md)); (b) structural cross-attribute rule in `_Detector.update` — prev_badge=STOPPED + speed=0|None forces badge to None, exploits "black-screens only happen at platforms, real departures monotonically increase speed" ([auto_input/README.md § Cross-attribute reject](auto_input/README.md)). Further cases as misfires surface. (Project memory: `~/.claude/projects/D--pids-jre-simulator/memory/project_hardening_philosophy.md`.)
- [ ] **Luminance-aware adapter for pale-brand line colours.** Sobu yellow `#FFD400`, Yamanote yellow-green — currently render at full saturation on white card top accents and the eyebrow text would be hard to read. Need W3C relative-luminance darkening for the eyebrow/accent uses while the wide accent strip stays vivid. Drafted in earlier session and reverted; re-add when a Sobu/Yamanote drive recording surfaces a real readability problem.
- [ ] **OCR — distinguish legitimate `0cm` from reset-after-overrun `0cm`.** Both render as identical green `0cm` text — same OCR read, different game-state context. A perfect stop by the player and a game-forced reset after the player overran the platform mark currently look the same in the data; the plot's "stopping accuracy" metric would be misleading if reset stops are counted as perfect ones. Possible disambiguating signals (per-arrival, evaluated within a context window): (a) was there a black-screen / OCR-FAIL frame near the `MOVING→STOPPED` transition (reset uses a black-screen fast-forward); (b) speed trace just before stop — legitimate stop decelerates smoothly to 0, reset jumps from non-zero to 0 abruptly; (c) distance trace — overrun has distance crossing through 0/negative before the reset frame. Out of scope for the OCR layer; lives in `plot_drive.py`'s arrival-event analysis. Tabled 2026-05-09; revisit when the plot tool actually renders stopping-accuracy data and the false-perfect-stops bias becomes visible.

## Display / LCD fidelity

- [ ] **New train models — re-skin work, not re-architecture.** Priority order: E235-0 Yamanote (in progress — see below), then E231-500, then E233 series. Each model lives under `displays/train_models/{model}/`. (CLAUDE.md § "Mental Model — Direction of travel".)
  - **E235-0 + calibration-editor work now happens on `master`** (the `feat/calibration-editor` branch was FF-merged ~2026-06-20/21 and is dead). Editor *standard* graduation (Phase 2) is still pending — see `WIP_calibration_editor.md § Merge plan`.
  - [x] E235-0 lower LCD — **5-station zoomed view** (replaces inherited 8-station). Shipped 2026-06-15: stopping pentagon + APPROACHING marker (dark-yellow circle, cumulative digit, ring-stamp-halo sweeping chevron) + green band fill-on-switch; owns the EIGHT slot in all modes. Minor visual finetune still pending (below).
  - [x] E235-0 lower LCD — ENGLISH renderer. Fixed 2026-06-15: FULL → Yamanote-circular and EIGHT → 5-station in ENGLISH mode (same-instance override; was leaking e235_1000 linear full-route + 8-station). Station names stay kanji (IRL-correct for the Yamanote route map).
  - [x] E235-0 lower LCD — transfer-info slot integration. Verified + fixed 2026-06-15: `upper_height` parametrized so the upper-LCD boundary matches the other slots (was 13px short); visually verified at Tokyo + Shinjuku.
  - [ ] E235-0 5-station — remaining visual finetune (marker sizes / positions / sweep timing) + re-probe Yamanote `contrast_color` (currently `[101,20,5]` from maxresdefault, but JPEG clips saturated red — want a cleaner reference shot).
    - **Chevron sweep easing curve** — switched to ease-out (fast→slow) with decoupled fade-in 2026-06-15; `ease_out_power` / `fade_in_full_at` / fade-out tuneables in both `_compute_chevron_animation_state` (full-route circular) + `_compute_five_station_arrow_animation` (5-station), kept matched. Cherry-on-top; nudge the powers when there's time to compare side-by-side.
- [ ] **Calibration editor — route bar wiring + lower-view dispatch (completes P5).** The lower full-route track (`CircularFullRouteDisplay`'s `self.track_*` geometry, shared with `OpenRouteFullRouteDisplay`) is the last `e235_0` element not editor-wired. Blocker: the editor has no lower-LCD **view dispatch** — `five_station` (`ARC_RECT`) + `transfer_panel` assume the 5-station view, but the route bar lives in the *full-route* view over the same lower-screen region, so a static hit-test rect collides. Fix: add a `view` field (`"full"`/`"eight"`/`"transfer"`) to lower `_REGISTRY` entries + filter hit-test/handles by the active `--lower-view`; then extract `_TUNEABLES_FULL_ROUTE` (track_top_y/bottom_y/left_pad/right_pad/stroke_w/vert_seg_h_outer/inner/circle_outer_radius) with a signature-check resync (re-apply + `_build_positions()` only when the dict changes, so production stays free), declare a `FULL_ROUTE_RECT`, register. Last item gating `WIP_calibration_editor.md` dissolution (P5). (2026-06-23, Phase 2 graduation.)
- [ ] **DISPLAY_E235.md — caveman-full voice + Voice EDIT-CONTRACT line.** Deferred from 2026-05-11 caveman adoption push (DATA_FORMAT / DISPLAY / auto_input/README.md rewritten that session). Skipped due to active E235-0 churn — mass rewrite mid-flight risks conflicts with in-flight content. Apply when E235-0 stabilizes (5-station zoom + ENGLISH renderer + transfer-info verification above all shipped).
- [ ] **Through-service continuity arrow — screen-edge / row-end case.** Verify the full-route chevrons + 8-station triangle render correctly when a frame boundary lands at a row end that runs off the screen edge, against IRL reference. ([DISPLAY.md § Through-Service Display Frames](DISPLAY.md) — "Pending IRL verification".)
- [ ] **Transfer-info — stations data population.** Extend beyond current ~48 (code_3 catalog + JO Sōbu Rapid done). Curation priority: stations served by ≥1 LCD-equipped line (E233+/E235). Skip 戸塚, 高輪ゲートウェイ, JY-only, JO-only east. `transfers_by_view` populated as raw observations, not derived. Note: 赤羽 JK/JA inner spacing observation pending verification. **Deferred until E235-0 + E233 models land** — no point adding e.g. Chūō transfers when there aren't enough in-spec train models to calibrate against. ([DISPLAY_E235.md § Transfer Info](DISPLAY_E235.md))
- [ ] **Transfer-info — per-train-model badge rendering policy (E233 sub-series).** E235-1000's `_universal + color → color square` rule landed. Different E233 sub-series render certain transfer entries as color-squares instead of icons (JK E233: Kawagoe; JA E233: all JR except Shinkansen). When E233 lands, declare its policy in its own `transfer_info.py` (no parameterized DSL). Color data entry is opportunistic — fill when an IRL ref is on hand. ([DISPLAY_E235.md § Color-square policy](DISPLAY_E235.md))
- [ ] **Transfer-info — recalibrate Step-1 N=5 tier.** Currently 1.8× extrapolated from N=7/N=9 anchors; 千葉 JO_east row 0 cramped is the known case. Need more IRL refs at N=5. ([DISPLAY_E235.md § Pipeline](DISPLAY_E235.md))
- [ ] **Transfer-info — EN size constraint trade-off (deferred).** Shinkansen row currently parked at EN 12 pt. Fixes parked.
- [ ] **Transfer-info — data-driven pattern analysis (future).** Once `transfers_by_view` populates ~10+ views × ~3+ drops, pivot the `(station, view, dropped_slug)` tuples to look for clusters that suggest IRL rules (upstream-coverage suppression, parallel-corridor redundancy, direction-aware suppression). If a pattern emerges, codify as a derivation rule with per-station data preserved as fallback for exceptions.
- [ ] **Transfer-info — `render_transfer` monolith refactor (future, when 2nd train-model lands).** ~770 lines with 6 nested closures sharing implicit outer scope. Real fix is a `LayoutContext` dataclass holding per-render state. Premature without a 2nd consumer. Trigger to revisit: when E233 sub-series adds a sibling `transfer_info.py` and starts to share/diverge code with E235.

## Distribution

Speculative side-quest from the future-direction discussion. Competing for budget against further auto-input polish. (CLAUDE.md § "Direction of travel".)

- [ ] **Signed Windows installer.** Replace the zip-extract distribution with a proper signed installer for the lightly-public audience.
- [ ] **First-run smoothness.** Whatever needs polishing for the ~1800 video-play / occasional-railfan audience.

## Chrome / i18n / OOBE

The chrome i18n foundation shipped 2026-04-29 evening: `i18n.py`, language picker, setup-screen translation, EN-mode font swap to HelveticaNeue-Bold, line-marker badges, lifted theme. Open follow-ups:

- [ ] **OOBE — game-pairing screenshots.** Placeholders implied in step 3 (~25s departure cue) + step 5 (~1200-800m approach distance) + step 8 recap. User provides screenshots later.
- [ ] **Re-enable home cards 設定 (settings) + 行車記錄 (driving record).** Out of scope this release → currently silver/disabled + inert (`home.INACTIVE_ACTIONS`). Wire them (settings page + drive-record viewer) and drop from `INACTIVE_ACTIONS` when built. (2026-07-15.)
- [ ] **`setup_tims` `--tims` launch — follow-ups (TIMS is now the DEFAULT flow 2026-07-11; `--classic` opts into legacy).** Feature parity with `setup.py` reached (see `APP.md § Setup flow`). DONE: OCR-launch (the crash was the RETIRED `draw_debug_panel`; live drive renders the graduated `status_band.render`), model on C07AA (列車型號 row), tutorial screen, band Home → straight home + beat every page, consent read-only entry from the 教學 tutorial, the **`--tims`→default flip** (`main.py --classic` for legacy), OOBE 教學-flash now carried onto the 2nd-page 基本操作 button (flip on basic-tutorial entry), the version update-hint made REAL (dropped the `PREVIEW_FORCE_HINT` stub — now driven by `update_check.get_update()`). The full final-check pass (real manual + OCR launch into a live drive; live language-switch) is DONE — user-verified end-to-end 2026-07-15 — and that session also landed the whole-flow **button-interaction coherence pass** (silver/yellow/beat/flash model + `chrome.DISABLED` centralization; see `conventions.md § UI code style`). Remaining is just the deferred items below:
  - **Per-module `ACTIVE_LANG` globals → single `i18n._current_lang`** (also § "Deferred review findings" 2026-06-27) — each screen hand-syncs its own `ACTIVE_LANG`. **Initial-load desync FIXED 2026-07-15** — `home.run()` now syncs `ACTIVE_LANG = i18n.current_lang()` at entry (was stuck at module-default `zh_HK` → zh_CN Simplified-in-TC-face tofu on the packaged flow; the actual shipped blocker). The **full 114-site unify** to a single `i18n._current_lang` stays **DEFERRED post-ship** (info-level for the rest, masked; net-negative risk at ship-wrap), ideally with `/third-man` on approach.
  - **`chrome.FONT_PX` adoption (trivial, when-touched).** Adopt the role font sizes where a screen's px already equals a `FONT_PX` role. NOT a hoist of `_LANG_TUNEABLES`/`_BAND_BTN_TUNEABLES`/`_NO_T`/`_BOX_T_BASE` — `conventions.md § UI code style` settled those stay LOCAL (one-off, single-screen). Earlier "hoist" phrasing was stale.
- [ ] **TIMS low-res chrome + button — standing visual finetune only (graduated + broadcast, blockers cleared).** DONE + shipped: two primitives (`draw_lowres_text`/`lowres_text_size` + `draw_tims_button`/`tims_button_size`) in `widgets.py`; style vocabulary (palette / role presets / `FONT_PX` / `DISABLED`) in `tims_chrome.py`; broadcast to all 8 `setup_tims` screens + the live `status_band`. Font LOCKED — Noto Sans per-locale, AA-off native, no upscale (`conventions.md § "TIMS chrome text"`). The 2026-06-24 review blockers are all resolved by the graduation (verified 2026-07-15): no `sys.path.insert`; no sandbox `_FACE` default (`font=None` valid only with `label=""`); `t=` defaults to widgets' own canonical `_TUNEABLES_TIMS_BUTTON`; `_bezel_ring`/`_vgradient_rounded` are `@lru_cache(maxsize=64)`d = the per-`(size,state)` pre-render the blocker asked for. Remaining is open-ended visual finetune, done lazily when a screen is touched.
- [ ] **Add EN data translations for routes not covered by `translations.json` / `train_types.json`.** Today's EN mode falls back to kanji + CJK font for line 2 when a dest/type entry is missing. **Joban data pending** (e.g. `土浦`) — same logic as transfer-info: no point without a Jōban-line train model to calibrate against. Mock-route fake dests (e.g. `品川・高輪ゲートウェイ`) don't count — no point translating unreal data. Long-term: add `english` field to route.json for route names + line-name itself, expand `translations.json` for missing dests. User: "in the future all of those will be in English."
- [ ] **Through-service / direct-connect (直通) route mapping.** Route names were simplified to **basic line form** for the TIMS route-selection screen (上野東京ライン・常磐線直通→常磐線, 総武快速線・成田線直通→総武快速線, 京浜東北・根岸線→京浜東北線, 埼京線・川越線→埼京線, 中央線快速電車→中央線快速) — this also changes the LCD route name (`app.py` reads the same `route` field). The combined names encode real through-running; proper direct-connect modeling is **deferred pending IRL PIDS behaviour sampling** (user to capture). See `APP.md § Route selection`. (2026-06-27.)

## Housekeeping (small, do-when-you-touch-the-area)


- [ ] **Re-grab `passing_en.png` at native 2560×1440.** Current capture is 2559×1439 — pygame blit handles it, classifier diff=0.00, but a clean re-capture is tidier.
- [ ] **Full-route numbered-circle digit centering.** The 5-station view's `_draw_numbered_circle` now centers digits on ink bounds (baseline + glyph metrics); `CircularFullRouteDisplay`'s copy still box-centers (digits park ~1.5px low). Apply the same fix if it reads off there too. (2026-06-12.)
- [ ] **Migrate live commit gate to the hardened `commit_gate.py`.** `.claude/settings.json` still uses the old spoofable inline-bash gate (`CLAUDE_COMMIT_VIA_SKILL=1` substring match); the `init-memory-loop` package moved past this to a preflight-file marker via `.claude/hooks/commit_gate.py` back at the 2026-05-15 revision — the live repo never adopted its own fix. Port the package's `commit_gate.py` + preflight-marker flow back into this project. (Surfaced 2026-06-09 during the harness-package port.)
- [ ] **Add `validate_data.py` to `/build` Step 2a pre-flight** (alongside `check_deps.py`). Now unblocked — the validator's full-scan skips `_*/` (shipped-data gate), so it won't false-block on WIP/mock data. Stops incomplete / i18n-tofu data shipping silently. (2026-07-15.)
- [ ] **Version-drift sensor.** No layer catches a version constant going stale: the scanners target specific primitives (SysFont / `Path(__file__)` / `.ttf` / deps), and the review lenses only see the dirty diff — so `home.py`'s hand-maintained `VERSION = "0.5.4"` displayed while the build stamped `0.6.0` into PE metadata. Fixed 2026-07-16 by reading `app_paths.display_version()` (frozen→PE metadata, dev→pyproject) so the tag holds NO constant. Sensor: **(a) DONE 2026-07-16** — the `ban-derivable-literals` pre-commit hook (`lint_primitives.py --derivable`, AST) flags any hardcoded semver-looking literal (+ bare bundled-asset path) outside the canonical homes, scope = production + `*_draft.py`. **(b) OPEN** — `/build` pre-flight asserts `pyproject.toml` `version` == the version stamped into `version_info.txt`, warns if the git tag will differ — so pyproject / PE metadata / tag can't diverge silently (a build-time consistency gate; could seed `_tests/` T3 or live in the build skill). Root cause is `critical_lessons`-class (build-time truth vs hand-maintained constant, sibling of §6). (Surfaced 2026-07-16, v0.6.0 build.)
---

## Deferred review findings

Open items surfaced by `/review+fix` that were deferred (not blocking, scope-creep, or dead-in-practice). Dedup by `<file>:<line>` + summary; bump recurrence counts when re-flagged. Mark `[x]` and strikethrough when fixed; remove entirely when user says "not real".

### From 2026-05-02 (dual-stream audio)

- [ ] **PA paused-state asymmetry vs STA's explicit `_sta_paused` flag** — `app.py:115` (lens 1, info; first flagged 2026-05-02) — `is_pa_playing()` returns True even while PA is paused (pygame `mixer.music.get_busy()` quirk). Pause is idempotent so no live-incorrect behavior, but mental model is asymmetric. Revisit when an unpause UI key is added.

### From 2026-05-02 (transfer-info schema)

- [ ] **Variant-only base slugs raise KeyError downstream rather than at resolver** — `DATA_FORMAT.md:254` (lens 1, info; first flagged 2026-05-02; recurred 2026-06-16) — `yokosuka_sobu` has no `name_ja`/`name_en` at base; if referenced plain (no `.variant` suffix), the entry resolves without name fields → KeyError downstream. Today's data always uses variants for these slugs. 2026-06-16 review surfaced the same gap in the new e235_0 inline panel (`displays/train_models/e235_0/lower_lcd.py` `_entry_w`/`_draw_entry`/`_entry_lines` direct `e["name_ja"]` subscript) — in-spec Yamanote never triggers it. Revisit when validator pass surfaces, or add explicit resolver guard (covers both call sites).
- [ ] **`ueno_tokyo.tohoku` variant inherits non-IRL `name_ja`/`name_en`** — `data/lines.json:28` (lens 1, info; first flagged 2026-05-02) — IRL on JU-zone (Takasaki/Utsunomiya) the LCD typically labels by 宇都宮線/高崎線, not "Ueno-Tōkyō Line". Out of scope until first JU-zone station with `transfers` is added.

### From 2026-05-03 (transfer-info LowerDisplay hookup)

- [ ] **`_resolve_transfers` called twice per frame at LowerDisplay layer** — `displays/train_models/e235_1000/lower_lcd.py:_station_has_transfers` (lens 1, info; first flagged 2026-05-03) — Once for slot-membership decision in `_available_slots`, again on render in `TransferInfoDisplay.show_stops`. Reaches past underscore-prefix private boundary. Perf is fine (dict lookup + small list comps); cache once per frame at LowerDisplay level if needed.

### From 2026-05-04 (transfer-info per-N scaling)

- [ ] **`_N >= 10` clause in dense-tier scaling is anticipatory** — `displays/train_models/e235_1000/transfer_info.py:168` (lens 1, info; principles.md § Implementation-completion-as-spec; first flagged 2026-05-04) — Max station N in current dataset is ~9; the `N >= 10` branch is uncalibrated. Defer until either (a) an N=10+ station enters the dataset, or (b) algorithm-redesign Step 1 formally calibrates the full ladder.

### From 2026-05-07 (E235-0 circular full-route + clock fine-tune)

- [ ] **Cycler-lock lambda fragility in preview** — `preview_display.py:200` (lens 1, info; first flagged 2026-05-07) — `sim.lower._tick_cycle = lambda current_time: None` and `sim.lower._handle_at_station_edge = lambda state, current_time: None` duplicate the bound-method signatures and will silently rot if those methods gain a parameter. Deferred because: preview-only; the clean fix would add a `_cycler_locked` boolean to the production `LowerDisplay` class for a preview-only feature.
- [ ] **Wrap-around minute undercount at loop terminal** — `displays/train_models/e235_0/lower_lcd.py:747` (lens 1, info; first flagged 2026-05-07) — `_compute_minutes_for_ahead` dedups by `sta_code`; when the duplicate JY24 marker at the loop terminal (Yamanote's `stops[0] = stops[-1]`) is skipped, its `time` value isn't credited to the next surviving entry's first-cumulative. Deferred because: dead in practice — IRL the wrap-back marker is `at_station=True` which uses the static branch.
- [ ] **CLOCK_RECT magic-number duplication** — `displays/train_models/e235_1000/upper_lcd.py:217` + `e235_0/upper_lcd.py:195` (lens 2, info; smell #5 within-file constant duplication; first flagged 2026-05-07) — `S_WIDTH - 170` and `80` repeat as both `CLOCK_RECT` const + inline `clock_x` / `clock_w` magic numbers in both `draw_clock` methods of each model. Deferred because: pre-existing pattern in e235_1000, not introduced by this session's diff. Worth a `/vibe-check` sweep to refactor inline references to `CLOCK_RECT.x` / `CLOCK_RECT.width`.

### From 2026-05-08 (E235-0 polish + font-path migration)

- [ ] **EnglishDisplay single-line dest sibling-divergence (e235_1000)** — `displays/train_models/e235_1000/upper_lcd.py` EnglishDisplay.draw_destination single-line branch (lens 3, warning; sibling-drift; first flagged 2026-05-08) — User asked for single-line English dest in **e235_0** to be LEFT-aligned at x=5 mirroring the 2-line branch (was previously centered via `draw_text_given_width(..., collapse=True)`). Same pattern exists in e235_1000 but was not touched. Deferred because: user-stated scope was "in E235-0" specifically (per `principles.md` § Scope fidelity for codified feedback); whether to propagate to e235_1000 is a separate question. Decide whether IRL e235_1000 PIDS centers or left-aligns its single-line English dest, then either propagate or codify the divergence.

### From 2026-05-09 (OCR speed-limit + stopping-offset feature shipping)

- [ ] **`MISREAD_DUMP_DIR` placement under `_ocr_calibration/`** — `auto_input/driver.py:120` (lens 3, info; sibling of `critical_lessons.md` 2026-04-27 § "naming carries semantics"; first flagged 2026-05-09) — Misread dumps land at `_ocr_calibration/_misread_dumps/`, but `_ocr_calibration/` is documented elsewhere as "OCR calibration sources" (input screenshots consumed by `extract_ocr_assets.py`). Mixing runtime debug-output into a sources-named folder conflates two dev-material categories; sibling precedent is `_recordings/` for runtime blackbox. Deferred because: thematic locality wins (both are OCR-related) + 31 existing dumps already in the path. Revisit if the mixed-purpose folder causes navigation friction.

### From 2026-06-10 (through-service display frames)

- [x] **Frame-swap fire is boundary-aligned only when the junction has transfers** — RESOLVED 2026-07-18. The swap hold was `sum(available slot durations)` (coupled to the view-cycle), which both ran too long (~18–30s, user flagged after arriving 千葉) and carried this mid-page-cut caveat. Replaced with a fixed `_SWAP_HOLD_DURATION` decoupled from the cycle — no rotation phase to land mid-page, caveat moot.

### From 2026-06-11 (Fable review of the through-service commit)

- [ ] **`holding_ahead` misreads a backward jump to exactly the junction** — `displays/train_models/e235_1000/lower_lcd.py` `LowerDisplay._update_active_frame` (lens 1, warning; rule: DISPLAY.md § Through-Service "Jump / backward / fast-page → resync to the natural frame"; first flagged 2026-06-11 by Fable review) — The post-fire hold is inferred via `a == natural + 1 and gi == _frames[natural]["to_idx"]`. A backward jump (click / arrow) to EXACTLY the junction (千葉) from the next frame satisfies the same predicate, so the display stays on frame N+1 instead of resyncing to the earlier frame per the contract. Visually coherent (junction is frame N+1's cell 0), preview-only, exact-junction-only. Deferred because: the explicit-`_holding_ahead`-boolean fix adds more state than this edge earns (Simplicity First). Fix path: track the hold with a boolean set at fire, cleared on departure/resync, instead of inferring it.

---

### From 2026-06-16 (editor save/edit refactor review)

- [ ] **Dangling cross-ref: `critical_lessons.md § "AST source-edit must iterate values in reverse"` doesn't exist** — `WIP_calibration_editor.md:137,187` + inline comments in `_dev_scripts/calibration_editor.py:_swap_dict_literal` cite a numbered critical_lessons section that was never written; the lesson lives only in the WIP doc + inline comment (lens 3, info; rule: principles.md § "Single source of truth"; first flagged 2026-06-16) — Deferred because: doc-consistency only, code complies (reverse iteration intact). Fix path: either add the AST-reverse lesson as a numbered `critical_lessons.md` entry (it's deployment-class source-corruption) or correct the citations to point at the inline comment as canonical.

### From 2026-06-20 (E235-0 open horseshoe full-route)

- [ ] **`_draw_passed_band` re-derives track geometry inline** — `displays/train_models/e235_0/lower_lcd.py` `OpenRouteFullRouteDisplay._draw_passed_band` (lens 1, info; rule: conventions.md § UI code style "All downstream coordinates derive from those"; first flagged 2026-06-20; recurred 2026-06-23 vibe-check) — Recomputes `v_outer` / `border_outer` / `straight_right` / `cy`, already derived in `_build_positions` + `_draw_track`. Values are consistent (all from the same `self.*` tuneables) so not a bug, but the gray-band clip rects could desync if a track-geometry formula is later edited in one site only — and this is the riskiest math in the class. Deferred because: not required for correctness + Surgical-Changes at wrap-up. Fix path: factor a shared `_band_geometry()` helper reused by `_build_positions`, `_draw_track`, and `_draw_passed_band`.

### From 2026-06-27 (E235-0 5-station band fill — curve-following reveal review)

- [ ] **`_TUNEABLES_ARC` split-personality: live caps (p0/p6) coupled into a "retired" dict + dead remnants** — `displays/train_models/e235_0/lower_lcd.py` `_build_fill_centerline` (lens 2, info; rule: vibe-check #2 dead helpers / #5 duplicated constants; first flagged 2026-06-27; recurred 2026-07-16 release-prep scan) — The band-fill centerline now reads `arc_p0`/`arc_p6` from `_TUNEABLES_ARC`, a dict the module header + DISPLAY_E235.md document as "retired." So p0/p6 are live while p1–p5 + their strokes + `arc_color` + `arc_smoothing` + `_ARC_STATION_SLOTS` + `_smooth_centerline` are dead remnants of the removed `draw_arc` path (zero callers). Deferred because: cleanup (graduate p0/p6 to a live home + drop the dead arc constants/helper) is scope-expansion beyond the animation task (Surgical Changes), and the dict is a transitional state per `WIP_calibration_editor.md § "Two-tier tuning model"`. Fix path: fold into the calibration-editor route-bar/lower-view-dispatch item (P5) — graduate the band-fill caps + delete the retired arc machinery in one pass.

### From 2026-06-27 (setup_tims package promotion review)

- [ ] **Per-module `ACTIVE_LANG` globals = second source of truth vs `i18n._current_lang`** — `setup_tims/pa_setting.py:~223` + band/home/route_select (lens 1, info; first flagged 2026-06-27) — Each screen keeps its own `ACTIVE_LANG`, hand-synced to `band.ACTIVE_LANG` at draw; the dev L-key cycles a child picker's locale but the parent keeps its stale copy, so after backing out the band's font-locale (`pixel_font_for_lang(band.ACTIVE_LANG)`) can diverge from `i18n._current_lang`. Currently masked (band's `i18n.t` paths only fire with a live status dict, always None today). Deferred because: belongs to the `run()→config` launch-bridge phase — fold locale into a single source (`i18n._current_lang`) the band reads, dropping the per-module globals.

### From 2026-07-16 (canonical-source-duplication harness sweep)

Held from the 4-agent complete-codebase scan (the version-hardcode detective pass). The deterministic facets (version, asset-path) were fixed inline (`app.py:893`) + are now `ban-derivable-literals`-caught; these are the SEMANTIC color/i18n facets held for per-item review.

- [x] ~~**`LINE_INK_GRAY` duplicates `chrome.DISABLED["text_color"]`** — `setup_tims/model_select.py:51`~~ **FIXED 2026-07-16** (release-prep review+fix) — now `LINE_INK_GRAY = chrome.DISABLED["text_color"]`.
- [x] ~~**`TITLE_COLOR` / `CODE_COLOR` duplicate `CYAN` / `CODE_INK` intra-file** — `tims_chrome.py:82,85`~~ **FIXED 2026-07-16** (release-prep review+fix) — now `TITLE_COLOR = CYAN`, `CODE_COLOR = CODE_INK`.
- [ ] **`LIMIT_LABEL = "制限"` fixed-JP without the "don't localize" annotation** — `status_band.py:87` (lens 3, info; i18n-bypass judgment; first flagged 2026-07-16) — a live user-facing band label hardcoded JP; its sibling `NOTIF_TEXT` carries an explicit "INTENDED fixed-JP, don't localize" note, this one doesn't. Deferred because: likely deliberate (TIMS system label, like the km/h unit) — needs a human confirm + the matching annotation, NOT necessarily an `i18n.t` change.

(Also reviewed, NOT tracked — structurally not-a-drift: `widgets.py:384` and `setup.py:82`, both `(62,68,80)`==`BG`. `widgets` sits BELOW `tims_chrome` in the import graph so it *can't* reference `chrome.BG`; `setup.py` is the classic screen's own independent palette. Both are own-source, not duplication.)

### From 2026-07-16 (release-prep whole-code scan — setup_tims focus)

Held from the 6-agent release-prep scan of new features since `v0.5.4`. **Obvious-safe fixes in the TIMS-setup / chrome / entry focus were applied inline this pass** (palette-drift derivations, dead-code, stale docstrings, consent `_load_state`, `NEXT_FLASH_MS` extraction). These are the **needs-context** (fix risks a bug without deep module context) and **out-of-focus** (E235-0 + auto_input — per user steer "well-tested"; logged so real ones aren't lost) items. Integration/first-run re-walk came back CLEAN (picker-class defect verified fixed + T1/T4-locked).

**TIMS setup / chrome (needs-context):**
- [ ] **`_wrap_mixed` re-implements the inherited `Tutorial._wrap_lines`** — `setup_tims/tutorial_basic.py:116` (lens 2 #1 forked-helper, warning; first flagged 2026-07-16) — the vertical tutorial re-wraps mixed text with different space-boundary handling than the parent it subclasses, so the two wrap rules drift independently. Deferred: swapping to `self._wrap_lines` shifts break points at spaces → visual check needed, not a blind swap.
- [ ] **`DOT_DONE` progress-cyan ≠ `chrome.CYAN`, comment says "TIMS cyan"** — `setup_tims/tutorial_basic.py:63` (lens 3, info; first flagged 2026-07-16) — `(54,214,226)` vs `CYAN (84,214,226)`; either snap to CYAN (then R=54 is a drift bug) or it's a deliberately dimmer progress-strip cyan and the comment misleads. Visual-design call.
- [ ] **Bottom-bar 返回/設定 sizing recipe duplicated `_render_grid` ↔ `_render_diagram`** — `setup_tims/route_select.py:684` (lens 2 #1, warning; first flagged 2026-07-16) — same `tims_button_size`/pad/`BACK_EXTRA_W` block in two places → the "grouped = same size" invariant drifts if one is tweaked. Deferred: `_render_grid` interleaves the ▲/▼ arrow cluster between the two buttons, so a shared helper must return rects for the caller to place arrows — needs the layout intent.
- [ ] **Dead `"start"` field in `run_on`'s return dict** — `setup_tims/route_select.py:826` (lens 2 #3, info; first flagged 2026-07-16) — `stop_idxs[sel_start]`; the inline comment reserves it for a launch bridge, but the bridge (`pa_setting._build_config`) re-resolves by NAME and nothing reads `start`. Deferred: dropping a deliberately-reserved field is a call on the variant/start-resolution model.
- [ ] **Dead pre-loop `btn_font`/`home_font` assigns** — `setup_tims/route_select.py:384` (lens 2 #2, info; first flagged 2026-07-16) — reassigned at loop top before the late-binding `frame()` closure first fires. Harmless; deferred (info + fiddly comment relocation).
- [ ] **`stops.index(start_name)` resolves to the FIRST name occurrence** — `setup_tims/pa_setting.py:157` (lens 1, info; first flagged 2026-07-16) — on a loop route with a repeated station name (Yamanote, in-spec for e235_0) picking the 2nd occurrence jumps to the 1st. Deferred: correct fix needs the picked index, but `run_on` dropped it as untrustworthy cross-variant — reconciling needs the variant/stop-list model.
- [ ] **Consent view rebuilds a 730×2600 surface every frame** — `setup_tims/ocr_setting.py:128` (lens 1 perf, info; first flagged 2026-07-16) — `_build_content` re-wraps all body text + smoothscales the screenshot at 30fps; measured content is only 728px (2600 is an unexplained magic). Deferred: animated flow/journey regions need per-frame redraw, so caching the static body needs the scroll/animation coupling understood.
- [ ] **`LIMIT_LABEL_COLOR` straggler literal vs the shared slate palette** — `status_band.py:91` (lens 3 canonical-source-dup, warning; first flagged 2026-07-16) — `(150,164,178)` sits 2 units off `chrome.GRID (150,162,176)`, an un-consolidated survivor of the 2026-07-06 palette centralization. Deferred: GRID vs DIM is a color-judgment call. (Distinct from the `LIMIT_LABEL="制限"` label entry at :87 above.)
- [ ] **Retired-supersample dead knobs + stale comment on `_draw_number_ss`** — `status_band.py:70` (lens 2 #8/#3, warning; first flagged 2026-07-16) — `READOUT_NUM_H`/`READOUT_DIGIT_XSCALE` are passed as `target_h`/`xscale` the function's own docstring calls "unused (kept for caller compat)" though it has one caller; `READOUT_NUM_H`'s comment still describes the removed AA-on supersample path. Deferred: dropping the params touches the `_draw_number_ss` signature — confirm `xscale` isn't wanted as a future knob.

**Entry / integration (needs-context):**
- [ ] **Route picker derives the line badge from `sta_code[:2]`, not canonical `line_code`** — `setup.py:176` (lens 3 canonical-source-dup, warning; first flagged 2026-07-16) — a route.json authoring `line_code` (validated) but whose first stop's `sta_code` prefix differs (through-service / pre_stop) shows the wrong line icon. Deferred: many shipped routes likely omit `line_code`, so a blind switch would drop their badges — confirm coverage first (prefer `line_code` when present, fall back to the derivation).
- [ ] **`if start_idx:` treats a start-station index of 0 as "no jump"** — `main.py:83` (lens 1 / §6 state-gated, info; first flagged 2026-07-16) — falsy-zero vs `is not None`; harmless only because the sim boots at stop 0. Latent if the boot default changes or the grid re-indexes. Fix: `config.get("start_idx") is not None`. Deferred: depends on whether the TIMS start grid can yield idx 0.
- [ ] **`scan_routes()` runs twice on classic launch** — `main.py:59` + `setup.py:1591` (lens 2 #3, warning; first flagged 2026-07-16) — the line-59 scan's `self.routes` is discarded and re-scanned inside `run()`; a pure redundant disk walk (classic flow only). Deferred: a CLEAN removal cascades 5 sites (`base_dir` param → `BASE_DIR` → `project_root` import all exist only for it) in the entry point — not worth blind entry-point surgery this pass.

**E235-0 (out of focus this pass — user: "well-tested"; logged so real ones aren't lost):**
- [ ] **Universal 5-station view doesn't skip passing (empty-`pa`) stations** — `displays/train_models/e235_0/lower_lcd.py:1932` (lens 1 out-of-spec, warning; first flagged 2026-07-16) — `_visible_stop_indices` linear branch has no `not stop.get("pa")` filter (unlike sibling `OpenRouteFullRouteDisplay._ahead_indices`), so a passing station in the next-4 window renders as a numbered countdown circle AND its `time` joins the cumulative chain; a future `time: null` passing station → `None+int` TypeError. No shipped route hits it today (needs an out-of-spec skip route into e235_0). Deferred per steer + needs render/state context.
- [ ] **Stale e235_0 docstrings (fallback renderer / "interim" slot claims / UPPER_HEIGHT)** — `displays/train_models/e235_0/__init__.py:5`, `lower_lcd.py:3`, `upper_lcd.py:26` (lens 2 #8, info/warning; first flagged 2026-07-16) — claim out-of-spec routes fall back to e235_1000's LINEAR renderer (actually the e235_0 horseshoe), that the 5-station view hasn't landed (it has + owns the EIGHT slot), and "UPPER_HEIGHT=117" (it's 130). Deferred per steer (E235-0 out of focus — these ARE obvious-safe, just not this pass's scope).
- [ ] **Inherited `_should_lock_to_eight` linear heuristic leaks onto circular Yamanote** — `displays/train_models/e235_1000/lower_lcd.py:1849` (lens 1, info; first flagged 2026-07-16) — e235_0 doesn't override it, so `len(stops)-curr_stop <= 7` drops the FULL (circular) slot for the last ~7 loop stops though a loop has no end. Behavioral (slot rotation), not a crash. Deferred: confirm intended vs a leaked linear end-of-route lock.

**auto_input (out of focus this pass — user: "well-tested"; logged so real ones aren't lost):**
- [ ] **Bg OCR thread mutates `state.cnt_pa_at_station` (single-writer break)** — `auto_input/driver.py:941` (lens 1 thread-safety, warning; first flagged 2026-07-16) — `_fire_departure`'s silent drain does a read-modify-write of `cnt_pa_at_station` off the background thread; a concurrent main-thread at-station PageDown (its own RMW) or the render read can interleave → lost update / stale queue index. It's the lone `self.sim.state.X =` write in driver.py; README says AppState is main-thread-only. Deferred per steer + needs state-machine context.
- [ ] **`_try_read_speed_limit` uses unscaled `DIGIT_MIN_W` at 1080p** — `auto_input/ocr.py:789` (lens 3 resolution-scaling, warning; first flagged 2026-07-16) — the glued-digit boundary guard uses the 1440p module constant (7px) instead of the seg-scaled `_seg.digit_min_w` (5px at 0.75) that every other digit-width test uses; tightly-kerned 100/110-class limits fail to split → `read_speed_limit` returns None → the band's cyan speed-limit cue silently misses frames at 1080p (Layer 3 fire triggers unaffected). Deferred per steer (mechanical fix, but auto_input out of focus).
- [ ] **`_dump_misread_speed_limit_cell` writes debug PNGs in production drives** — `auto_input/driver.py:118` (lens vibe-check debug-in-prod, info; first flagged 2026-07-16; cf. the `MISREAD_DUMP_DIR` naming entry from 2026-05-09) — on any suspicious speed-limit read with auto-PA on, the shipped exe mkdir's `_ocr_calibration/_misread_dumps/` next to itself and writes `sl_*.png`. Fail-soft (no-ops on read-only install dirs). Deferred: gate on a debug flag rather than always-on in packaged builds.

## Closed-off paths (don't re-propose)

Recording the ground we've explicitly decided NOT to walk, so future sessions don't re-litigate:

- **Memory hooking the game's `*saf.dll` modules.** Tried, dead end.
- **Decrypting SimDATA assets.** Encrypted; not pursuing.
- **Audio fingerprinting** for stop detection. Replaced by HUD OCR which works.
- **Full-desktop OCR** instead of window-bound capture. Privacy + perf concerns.
- **Tesseract-based OCR.** Too heavy; pixel-perfect template match works.
- **Mac build.** The companion game (JR EAST Train Sim Real) is Windows-only — no Mac audience exists for this app. Not worth the porting cost.
- **Scaling to lines the game already covers** (Sobu local, Yokosuka, etc. — newer game routes ship with PA, don't duplicate).
- **OCR-as-display-layer fidelity-purity argument.** OCR is an *input layer* (replaces PageDown press), not display. Don't recycle.
