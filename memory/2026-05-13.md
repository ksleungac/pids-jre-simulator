# 2026-05-13

## Session: Capture-region cut — production OCR path narrowed to top-right quadrant

90-minute window, user-budgeted. Goal frame: push auto-input/OCR toward production-ready, fastest concrete shipment. Picked the capture-region cut (TODO line 22) out of the ranked list because it was mechanical, scope-sealed, verifiable offline, and visibly perf-positive.

Refactor shape:
- `hud_layout.py` gains `CAPTURE_REGION_2560_1440 = (1280, 0, 2560, 720)` (dxcam region= signature) + derived `HUD_BBOX_IN_CAPTURE = (920, 20, 350, 480)`. Canonical `HUD_BBOX` unchanged — kept as desktop-relative reference because the `*_from_surface` helpers in `ocr.py` (consumed by 1b dev tool + calibration extractor) still need the full-desktop semantics.
- `auto_input.py` switches `camera.grab()` → `camera.grab(region=CAPTURE_REGION_2560_1440)` and crops via `HUD_BBOX_IN_CAPTURE`.
- Bootstrap full-frame grab at AutoDriver start asserts desktop shape `(1440, 2560)`; fail-loud + disable on mismatch. Same fail-loud shape as the three sibling lessons in `critical_lessons.md` (2026-04-27 runtime materials; 2026-04-30 dep classification; 2026-05-05 PyInstaller deployment frame).
- Smoke: parity script ran 6 reference screenshots through both old (full grab + HUD_BBOX) and new (region slice + HUD_BBOX_IN_CAPTURE) paths. 6/6 bit-identical OCR outputs across MOVING/PASSING/STOPPED badges, speed reads, distance reads, stopping-offset, speed-limit reads.

Pyright gotcha surfaced mid-flight: first naming was `CAPTURE_REGION_2560x1440`. Python accepted it (verified via standalone `python -c` import), but Pyright reported the import as `unknown import symbol` AND all downstream consumers as undefined. Renamed to `CAPTURE_REGION_2560_1440` and the cascade cleared. Worth codifying as a convention because this pattern (digit-letter-digit identifier run, e.g. encoding resolution in a name) is something we'll hit again when 1080p support lands or anywhere coordinate constants get named by dimension.

User asked mid-session about root-level sprawl. 38 loose files at root. Most are production runtime that should stay; 8 are dev tools that don't belong (5 `preview_*.py`, 2 `compare_*.py`, `_enum_3line.py`); OCR cluster (`auto_input.py`, `hud_layout.py`, `ocr.py`, `plot_drive.py`) would make a natural `auto_input/` package given `auto_input.py` is ~1000 lines mixing 5 concerns. User wants the OCR-cluster move next — gated on `/third-man` design input first, per the structural-refactor trigger codified this session.

User instruction worth pinning: *"on the structure, ask the /third-man for ideas, be mindful of vibe coding"* — different trigger shape from the existing impasse-trigger of `/third-man`. Codified by extending the existing principle to cover structural refactors as a second trigger.

## Codifications this session

- `AUTO_INPUT.md § Architecture / HUD layout / Capture` — region-cut path + resolution gate, region-relative HUD_BBOX_IN_CAPTURE
- `conventions.md § Tooling` — Pyright identifier-naming gotcha (`FOO_<digits>x<digits>` style breaks; use underscore separator)
- `principles.md § Self-propose /third-man at impasse OR before structural refactors` — extended the existing impasse-trigger entry with a second trigger for multi-file structural refactors (≥3 files / package restructure / module boundary changes)
- `TODO.md` — closed capture-region item; added OCR-cluster `auto_input/` package refactor as a new item gated on /third-man
