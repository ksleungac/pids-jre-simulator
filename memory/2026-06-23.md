# 2026-06-23

## Session: git sync + reconcile stashed E235-0 fix onto master; merge-plan docs de-staled

Pure git-ops + reconcile session — no new feature code authored. The substantive fix (Osaki circular wrap) was written in the 2026-06-16 session, left uncommitted, and recovered here.

**State found.** Local was on dead `feat/calibration-editor` with an uncommitted tree (4 tracked files). origin/master had advanced 4 commits past the feat tip — the branch had **already been FF-merged** to master (~06-20/21, Phase 1 of the graduation plan), then open-horseshoe (`22b3745`) + dropdown (`0482085`) landed directly on master. The WIP doc + `TODO.md:24` still claimed "not merged / work happens on the branch" — stale the moment `b6aedb8` recorded the plan (the merge had already happened). User confirmed the new policy: **E235-0 fixes go on master, branch is dead.**

**Reconcile.** Stashed the tracked work, FF'd local master to origin, popped the stash onto master. Popped **clean** — the 406-line open-horseshoe change lives in a *separate class* (`OpenRouteFullRouteDisplay`), zero overlap with the `JapaneseFiveStationDisplay` edits (Osaki wrap + arrow staggered-fade + `a0_*` ease tuneables). 

**Verify.** Confirmed the circular wrap by calling `_visible_stop_indices` directly against the real Yamanote route (text ground-truth per the "don't read structural facts off a PNG" convention) — every position returns 5 wrapped indices with name-dedup of the doubled terminal; old code dead-ended at len=1. review+fix (1 cycle) + a fresh-context opus vibe-check on `22b3745`+`0482085` both came back clean. Vibe-check confirmed `OpenRouteFullRouteDisplay` *subclasses* `CircularFullRouteDisplay` (inherits primitives, doesn't fork) and the train-model registry is live-used (legit graduation), and that the `_draw_passed_band` geometry-re-derivation is the **same** already-deferred `TODO.md:112` item, not new.

Deleted `feat/calibration-editor` (local + remote). The reconciled A/B/C work is the commit target for this session.

## Codifications this session

- `WIP_calibration_editor.md` § Merge plan — Status updated to "Phase 1 DONE, Phase 2 not started"; line-25 + line-217 "not merging today" claims corrected to past-tense executed.
- `TODO.md:24` — branch policy reversed ("work now happens on master").
- `TODO.md:112` — recurrence note (re-flagged 2026-06-23 vibe-check, same instance).
- No `principles.md` / `conventions.md` changes — branch-lifecycle facts are transient project state, not durable rules.

## Session 2: calibration-editor graduation — Phase 2 (P1/P3/P5-upper/P2)

Executed the recorded graduation Phase 2 from `WIP_calibration_editor.md`. Order P1 → P5 → P3 → P2.

**P1** — added the module-level `_TUNEABLES_*` editor standard to `conventions.md § UI code style` (the method-local `# fmt: off` block named as predecessor; convert-lazily, no backfill).

**P5 (upper only)** — wired badge + PA-hint into `_TUNEABLES_BADGE_RECT` / `_TUNEABLES_PA_HINT_RECT` (region-rect pattern) + registered. Badge subtlety: its clip rect spans the framed 68px square PLUS a 12px code_3 band, so rect height (80) ≠ framed square; `badge_h` derives `rect.height − code_3_band_h`, `badge_x/_w` read from the rect, vertical anchor kept bottom-pinned to preserve the exact render. Verified byte-identical (SGW/JY25 screenshot read-back) + registry resolves.

**Route bar DEFERRED (user: "defer as own task").** The lower full-route track is the last unwired `e235_0` element, but it surfaced a real editor gap: no lower-LCD **view dispatch**. `five_station`/`transfer_panel` (5-station view) and the route bar (full-route view) share the same lower-screen region, so a static hit-test rect collides — the editor must disambiguate by active `--lower-view` first. Filed in TODO with the full recipe (`view` field on lower registry entries + `_TUNEABLES_FULL_ROUTE` signature-check resync). This is now the sole item gating WIP-doc dissolution.

**P3** — verified (not changed): `preview_display --edit` imports `calibration_editor` lazily inside `_run_edit_loop` via a `project_root()` path-hack; no static `_*/` import, release-safe.

**P2** — authored `.claude/skills/calibration-editor/SKILL.md` (canonical editor mechanics: launch, suffix table, add-an-element runbook w/ the badge worked example, new-model fork runbook, two-tier model, AST-reverse caveat). The WIP doc's reference sections are now superseded by it but kept until the route bar lands and the doc dissolves.

Group C (editor preview-layout refactor from the 06-16 stash) got reviewed for the first time here — clean, dead-code fully removed. review+fix: 1 cycle, only a cosmetic stale "drives sidebar layout" comment (Group C made the panel position fixed) fixed in two spots.

## Codifications this session (Session 2)

- `conventions.md § UI code style` — new "Editor-compatible tuneables: module-level `_TUNEABLES_*` dict" entry (P1).
- New skill `.claude/skills/calibration-editor/SKILL.md` (P2) — canonical editor-mechanics home.
- `WIP_calibration_editor.md` — Phase 2 progress + route-bar deferral + dissolution now gated on the route bar.
- `TODO.md` — route bar + lower-view dispatch item (completes P5).
