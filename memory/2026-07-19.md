# 2026-07-19

## Session: Yamanote train-type data fix (内回り → 各駅停車)

One-line data fix. Yamanote `route.json` `type` field held `内回り` (Inner Loop) — a direction, not a train type. Changed to `各駅停車` (Local); the translation entry already existed in `train_types.json`. Left `remarks.direction` = `内回り` untouched (genuine direction, separate field). Full `validate_data.py` scan clean.

## Codifications this session

- no codifications this session

---

## Session (block 2): OCR test migration → brainstorming skill → auto-driver logic hardening

Continuation session (post-compaction). Three arcs:

**Phase 1 — validate_ocr → T3 test.** Migrated the dev `validate_ocr.py` harness into the real `_tests/` framework as `t3_invariant/test_ocr_reads.py` (committed cropped-cell fixtures for full read-coverage + capture-region quadrant frames for crop-geometry; `manifest.json` = single ground-truth source). Fixture generation folded into `extract_ocr_assets.py` (net −1 dev script). Motivated by v0.6.0 going public + the "born-wrong-together spec/code" re-entry incident.

**Brainstorming skill** installed from superpowers (only that skill) + customized with a project-overrides block: ask only real-world/mental-model Qs (decide practice yourself), spec-location follows project doc conventions not `docs/superpowers/specs/`, never auto-commit, terminal step = direct implementation. HARD-GATE (no code before design approval) kept.

**Auto-driver OCR "logic hardening"** (the user's named prerequisite before resuming Phase-2 multi-res). Two rules shipped, both T1-tested:
- **Speed value-domain rectify** (`_rectify_speed`, `ocr.py`): the decimal-slip misread (72.7→"727", when the decimal-point bbox isn't detected) is recovered by dropping one trailing digit above the 140 km/h ceiling and re-checking — recovers the sample instead of dropping it. Chosen over fixing the fragile decimal segmentation (user's steer). Drivable max 135.
- **Stopping-offset gate** — evolved live through the user's own convergence: speed==0 → speed+badge → **badge-only** (`_accept_stopping_offset(offset, badge)`, accept iff `badge=="STOPPED"`). The green ±cm read is phantom-prone (scenery-green bleed), so it needs a stopped-signal gate; the **badge is the groundtruth** because *badge read > (distance, speed)* in reliability. Speed dropped out entirely.
- Plus: reader-layer `|offset| ≤ 500cm` domain clamp; `offset_reject` drive-log event (mirrors `cross_reject`).

**The design principle the user crystallized** (`"no point of adding uncertain signal to a very stable logic, this is the mental model"`): corroboration earns its keep guarding a **phantom-prone READ**, not bolted onto an already-**stable TRIGGER**. So `FIRE_AT_STATION` stays badge-ONLY (adding the noisier speed read would only dilute it) — a comment now locks that at the site. Same reason the offset gate is badge-only. This is why I had to redo the gate from speed+badge to badge-only mid-session: I started editing before the model settled (`§ Converge on the model` — should have locked it first).

**Temporal stabilization** (hold-last-good on OCR dropout) raised as the RIGHT alternative to a score-floor ban (which the user rejected — no score distribution to set an honest threshold, and a real 0.6 read shouldn't be discarded). Deferred to a separate task → TODO with its three design questions.

review+fix (1 cycle, zero blocking): one stale `speed != 0` comment at the offset_reject site (leftover from the speed→badge evolution) + a README rationale trim.

## Codifications this session (block 2)

- `principles.md` § Collaboration ("Commit to a recommendation") — decide-practice / ask-only-real-world bullet.
- `auto_input/README.md` — badge-gate acceptance (badge > speed/distance) + ±500 clamp + decimal-stop rectify note (reference-shaped, written during the work).
- `driver.py` at-station comment — locks the deliberate badge-only choice.
- `TODO.md` § Auto-input/OCR — temporal-stabilization item.
