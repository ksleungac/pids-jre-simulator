# 2026-04-29

## Session: through-service `pre_stops` feature, per_line refactor, doc-tighten convention, omiya `pa_at_station` cut

Four discrete pieces of work, only loosely related — handled in this order.

---

## 1. Keihin 1275A: split `1.mp3` at 13s for 大宮's `pa_at_station`

Small data fix using the `pa_at_station` schema introduced in the 2026-04-28 PM session. The original `1.mp3` (ref'd by さいたま新都心 as `pa[0]`) had a 13s welcome announcement for 大宮 stuck on the front before the dep-from-omiya / arr-saitama-shintoshin PA. Same shape as Tokaido 1865E 東京.

- Snapshot: `audio_src/keihin/1275A/1_original.mp3` (gitignored backup per "Backup before in-place destructive modification" principle).
- ffmpeg stream-copy at exactly 13.0s — front → `audio/keihin/1275A/pa/omiya_stopping.mp3` (new), latter → overwrites `audio/keihin/1275A/pa/1.mp3`.
- route.json: 大宮 gains `"pa_at_station": ["omiya_stopping"]`.
- Filename `omiya_stopping` is descriptive; route uses numeric naming throughout, but user opted for a one-off descriptive name on this single migrated file ("just for this one mp3, better be like omiya_stopping"). Documented as a deliberate convention exception.
- Validator brackets: both files flagged `MISSING LEAD/TRAIL` at the cut boundary because 13.0s landed exactly on the voice onset — silence got distributed to neither side. Acceptable for now; if dissonant by ear, re-cut at ~12.85s for a 150ms lead buffer.

---

## 2. Lower LCD `_calculate_layout` — uniform 14-per-line rule

Removed the `>17 or even / else 17` branch that special-cased 17-stop odd routes (concretely: keiyo/780Y_1510Y) to render single-row of 17. The IRL E235-1000 PIDS is 14-per-line; the 17-fallback was a one-time concession to keep keiyo single-row even though that's not how E235-1000 actually displays.

With the per-train-model architecture in place + best-effort policy for out-of-spec routes (keiyo isn't an E235-1000 line IRL), the cap can now be applied uniformly. New rule:

```python
if num_stops <= STOPS_PER_LINE:        # ≤14 → 1 row
    self.per_line = num_stops
else:                                   # >14 → 2 rows, capped at 14
    self.per_line = min(STOPS_PER_LINE, math.ceil(num_stops / 2))
```

Visible regression: keiyo 17 → 9+8 wrap (was 1×17). Accepted as the single-route cost of consolidating on the IRL rule. All other shipped routes pixel-identical because they were already at the 14-cap or matched the new ceil(n/2) rule by coincidence (sobu 19→10+9, tokaido 21→11+10, saikyo 24→12+12, nambu 26→13+13, chuo 32→14+14 with flip, keihin 46→14+14 with flip).

DISPLAY.md table updated to the cleaner three-row form (≤14 / 15–28 / >28).

---

## 3. `pre_stops` — through-service prefix on the lower LCD

The headline feature. JR East trains regularly through-run between lines; the canonical example is Sōbu/1217F, which physically runs Yokosuka Line 久里浜→東京 then continues as Sōbu Rapid 東京→成田空港. IRL the LCD displays the combined journey with the Yokosuka portion dim/passed.

### Schema

New optional `pre_stops` field at the route level:

```json
{
  "pre_stops": [
    {"name": "久里浜", "sta_code": "JO01"},
    {"name": "衣笠",   "sta_code": "JO02"},
    ...
    {"name": "新橋",   "sta_code": "JO18"}
  ],
  "stops": [
    {"name": "東京", "pa": [], "time": 0, "sta_code": "JO19", ...},
    ...
  ]
}
```

Each pre-stop has only `name` + `sta_code`. No `pa` / `pa_at_station` / `sta` / `sta_cut` / `time` — the simulator never advances into pre_stops, so those fields would be ignored anyway. Documented as "forbidden" to make intent explicit.

Sobu/1217F populated with all 18 Yokosuka Line stations (JO01..JO18).

### Implementation

Sim code (`app.py`, `audio.py`, `upper_lcd.py`) **untouched**. `state.curr_stop` still indexes `stops[]` (sim truth). All shifting happens in the lower-LCD renderers:

- `JapaneseDisplay` and `JapaneseEightStationDisplay` build `self.display_stops = self.pre_stops + self.stops` and `self.display_offset = len(self.pre_stops)` at init.
- `_calculate_layout` sizes against `len(self.display_stops)`. Circular detection stays on `self.stops` only (pre_stops can't make a route circular).
- `show_stops` in both renderers computes `curr_stop_disp = state.curr_stop + self.display_offset` (and same for cursor_pos) at the top, then passes display-shifted indices to all helpers (`draw_marks` / `draw_ptr` / `draw_times`). The helpers operate purely in display-index space.
- Pentagon boot fallback `curr_stop == 0` updated to `curr_stop == self.display_offset` — fires at the active route's first cell. For routes without pre_stops (display_offset=0) it's identical to the original.
- `text_color` boot exception `gi == 0` likewise updated to `gi == self.display_offset`.
- Pre-route cells naturally render in `INACTIVE_COLOR` because `cursor_pos_disp ≥ display_offset` always, so any pre cell has `cursor_pos > gi` — falls outside the active range.

### Window-flip — the design wrinkle

Native long routes (chuo, keihin) use **early-flip** in `_get_stops_list_disp`: when `remaining < STOPS_QUANTITY`, window flips from `[0:28]` to `[-28:]`. This shows "end approaching" once the train is in the latter half.

Pre_stops routes break this. For Sobu/1217F at boot: 18 pre + 19 active = 37 cells, train at display idx 18 (Tokyo), `remaining = 19 < 28` → standard logic flips immediately, hiding Kurihama..Tokyo from view. User observed this and corrected: "i want the fullroute display to show from kurihama".

Fix: dual flip-rule, branched on whether `pre_stops` is non-empty:

```python
if self.pre_stops:
    # late-flip: keep window pinned at start until train scrolls off the right edge
    should_flip = curr_stop > self.STOPS_QUANTITY - 1
else:
    # early-flip: standard "approaching end" trigger (preserved for chuo/keihin)
    remaining = len(self.display_stops) - curr_stop
    should_flip = 0 < remaining < self.STOPS_QUANTITY
```

Same final shape (one-shot flip from `[0:28]` to `[-28:]`), different trigger. Late-flip keeps the through-service prefix visible at boot; the flip fires later when the train would actually scroll off the right edge of the first window.

Verified: keihin/chuo unchanged, sobu/1217F now shows 久里浜..千葉 at boot with row 1 = 久里浜..新川崎 (14 dim) and row 2 = 武蔵小杉..新橋 (4 dim) + 東京 pentagon + 新日本橋..千葉 (10 active).

### IRL nuance — what was clarified during the discussion

- The combined frame is **one frame**, not a frame swap at the junction (Tokyo). User: "is actually a combined frame".
- IRL frames swap at Chiba (the Sōbu Rapid yields to Sōtobō / Narita Line views). User: "IRL the frame swaps at chiba, but that's too complicated, we will discuss that in another feature, it's similar to this problem (different line, through service)". Deferred.
- The 14-per-line layout does NOT honor a row-break at the through-service junction (Tokyo). Earlier in the discussion I hypothesized `per_line = len(pre_stops) + 1` to put the row-break at Tokyo; user corrected: "no, although it is through service, it doesn't honor this break for some reasons". The standard 14 rule applies uniformly.
- Pre-route includes Kurihama and Kinugasa. Earlier user said "from yokosuka to chiba" but later corrected: "i made a mistake earlier saying that it's from yokosuka, but i just meant yokosuka line". Pre_stops = JO01..JO18 (full Yokosuka Line up to but not including the join station).

### Cycle-1 review fixes

`/review-plus-fix-relentlessly` flagged two info-level items:
- `_draw_continuation_marker` (8-station scaffolding helper, deliberately uncalled) has a `len(self.stops) - 1` guard that would silently no-op on pre_stops routes. Added a `PRE_STOPS NOTE` block to the dormant-scaffolding comment naming `len(self.display_stops) - 1` as the correct fix at wire-up time.
- DATA_FORMAT.md said the flip is "the same" as native long routes — functionally yes, mechanically no. Replaced with explicit early-flip vs late-flip distinction + pointer to `_get_stops_list_disp`.

Cycle 2 verified clean.

---

## 4. "Tighten before appending" — doc-editing convention

User noticed domain docs (DISPLAY.md, DATA_FORMAT.md, AUTO_INPUT.md) had been growing additively across recap cycles, accumulating stale duplicates. They debated whether to fix in `/session-recap` or as doc headers; we landed on **doc headers + principles.md complement + minor session-recap nudge**.

Logic for placement: same as the project's existing `# CONTRACT:` pattern for code (per `conventions.md` § "Contract Pointers in Code"). The agent always reads the doc before editing it, so a header lands in context at the decision moment. A skill-level rule alone misses ad-hoc mid-session edits.

Concrete additions:
- Each domain doc top: `> **Editing this doc.** Before adding new content, scan for redundant or stale claims that the addition would duplicate or contradict — merge / remove first, then add. Domain docs are tightly maintained, not additive. See [principles.md § "Tighten before appending"](.claude/rules/principles.md).`
- `principles.md` § "Documentation strategy": new "Tighten before appending" subsection with rule + Why + How to apply. Frames bloat as "the doc-version of the kitchen-sink rules-file failure mode".
- `/session-recap` SKILL.md Rule 2 extended: "Before writing, check if the info already exists or has gone stale. Update / remove in place — don't add a second copy, don't leave outdated content standing."

The doc callouts and the principle entry stay in sync via cross-link — single edit point if the rule changes.

### Evolution later in the day — EDIT-CONTRACT upgrade + `/distill-docs` skill

User came back asking *why* the bloat happens despite the new convention and whether the existing gates are enough to keep docs clean (the way `.claude/rules` stays clean). Diagnosis: write-time gate alone is structurally insufficient because (a) cross-doc drift can only be caught reading multiple docs, (b) cumulative staleness (a feature commit makes half a section obsolete but the editor focuses on new content), (c) self-blindness — the writer rarely sees their own bloat. Same reason `.claude/rules` cleanliness depends on hard structural caps **plus** `/distill-memory` periodic sweep, not the cap alone.

Two interventions landed:

1. **Upgraded "Editing this doc." callout into a structured `EDIT-CONTRACT` block** — modeled on the inline `# CONTRACT:` pattern. Concrete refuse list (history notes, code-snippet illustrations, speculative future sections, design-rationale prose, cross-doc duplication), "name what you merge into OR replace" requirement, ~10-line size gate that pauses for diff-review. Replaces the earlier scan-for-redundancy callout. `principles.md` § "Tighten before appending" how-to-apply refreshed accordingly. `/session-recap` Step 3 + Rule 2 extended to re-read the EDIT-CONTRACT before writing to a domain doc.

2. **`/distill-docs` skill created** — periodic sweep that pairs with the EDIT-CONTRACT, mirroring `/distill-memory`'s discussion-first item-by-item shape. Scope locked to `DISPLAY.md` / `DATA_FORMAT.md` / `AUTO_INPUT.md`; removal-only (no restructuring); seven named bloat shapes with verification recipes (cross-doc grep, git log, code read). Wired into `CLAUDE.md` "When Working On..." for discoverability.

Dry-run on `DISPLAY.md` + `DATA_FORMAT.md` (post-earlier-audit state) surfaced 11 high-confidence findings beyond what the manual audit caught. Applied:

- DISPLAY.md 786 → 683 lines (−103). Removed Changes Log section (28 lines, git log territory), three stale code-snippet illustrations (UpperDisplay / LowerDisplay / PASimulator constructors — all confirmed stale vs current `app.py` / `upper_lcd.py` / `lower_lcd.py`), four duplicate Design Decisions entries (3-6 were restatements of content earlier in the doc), one intra-doc duplicate line ("Always displays as kanji" stated in two places).
- DATA_FORMAT.md 492 → 479 lines (−13). Removed "Implementation behavior" bullets under Stop-Level Destination Override (display semantics, not schema — belong in DISPLAY.md) and 5 countdown display bullets under `time` field (display semantics, already covered by inline `# CONTRACT:` on `lower_lcd.py:draw_times`). Both replaced with one-line cross-references.

Calibration notes captured during the dry-run:
- The Changes Log catch is load-bearing — if `/distill-docs` had missed it, the bloat-shape vocabulary would be undercalibrated. Caught.
- Tension between the skill's "let recently-added content marinate ~2 weeks" rule and the EDIT-CONTRACT's shape-based refusal: history-note shape is bloat regardless of recency. Surfaced for user judgment rather than silently applying one rule.
- Cross-doc duplication is exactly the failure mode write-time can't catch — confirms the skill earns its keep.

Items deferred (not applied this pass): "Adding New Train Model" + "Usage" sections in DISPLAY.md (~30 lines speculative-future how-to; high-confidence on action, med on full-vs-partial removal — separate decision); past-tense bug-fix narrations (low-confidence flags, ambiguous).

---

## Documentation updates this session

- `audio/keihin/1275A/route.json` — 大宮 gains `pa_at_station: ["omiya_stopping"]`.
- `audio/sobu/1217F/route.json` — `pre_stops` array (18 Yokosuka Line stations, JO01..JO18).
- `audio/keihin/1275A/pa/omiya_stopping.mp3` (new) + `1.mp3` (re-cut, latter half).
- `audio_src/keihin/1275A/1_original.mp3` — gitignored snapshot (rollback safety net).
- `displays/train_models/e235_1000/lower_lcd.py` — per_line refactor, pre_stops support across both renderers, dual flip-rule, boot-pentagon shifts. PRE_STOPS NOTE added to `_draw_continuation_marker` scaffolding comment.
- `DISPLAY.md` — per_line table updated, "Editing this doc" callout added.
- `DATA_FORMAT.md` — `pre_stops` subsection added under Route-Level Fields with deferred display_end note. "Editing this doc" callout added. Cycle-1 review fix: flip-trigger description sharpened (early vs late distinction).
- `AUTO_INPUT.md` — "Editing this doc" callout added.
- `CLAUDE.md` § IRL display conventions — through-service combined-frame one-liner added.
- `.claude/rules/principles.md` § Documentation strategy — new "Tighten before appending" subsection.
- `.claude/skills/session-recap/SKILL.md` Rule 2 — extended for stale-removal.
- `TODO.md` — two new items (display_end, frame-swap-at-junction); `_draw_continuation_marker` rephrase to mention pre_stops compatibility.

## Preferences (jot)

*(empty — no ambiguous-middle preferences this session; all rule-shaped/fact-shaped findings went directly to their canonical homes during the session itself)*

---

## Evening session — App state-machine docs + click-jump MVP + autodriver Layer 3 refactor

Continuation after the morning's `pre_stops` + EDIT-CONTRACT work. Picked up the click-to-jump MVP that had been paused mid-design on 2026-04-28 PM. By session end: state-machine docs placed across the doc tree, click-jump MVP shipped end-to-end on the lower LCD, autodriver Layer 3 refactor done (`*_fired` → `*_observed`, `inferred_state()` accessor extracted), doc-tree housekeeping completed, `/review+fix` passed in 2 cycles.

### State-machine doc placement (CLAUDE.md / DISPLAY.md / AppState contract)

The first hour was a doc-placement discussion. User asked where the App state-machine knowledge should live. Three options surfaced:

- (A) Expand `DISPLAY.md § Unified State Machine` (existing home).
- (B) Promote to a new `APP_STATE.md`, cross-reference from DISPLAY.md.
- (C) Split — mental model in `CLAUDE.md`, edge cases in DISPLAY.md, field semantics inline as `# CONTRACT:` on `AppState`.

User leaned (C). Final landing:

- **CLAUDE.md § Mental Model** gained one paragraph "App state machine" — the 3 sub-states (`APPROACHING_EARLY` → `APPROACHING_FINAL` → `STOPPING`) + cycle, pointers to DISPLAY.md and the inline contract. Honors "preloaded mental model": the cycle shape is what you keep in head when working on this project.
- **DISPLAY.md § Unified State Machine** gained "Edge cases & guards" subsection: audio-playing guard at `_next_pa`, `cnt_pa` dead during STOPPING (naturally-entered vs `jump_to_stop`-entered both work because `at_station` overrides prefix mapping), terminus silent no-op, `pa=[]` with non-empty `pa_at_station` edge case.
- **`app.py` `AppState`** gained an inline `# CONTRACT:` block citing DISPLAY.md § "Unified State Machine" + § "Edge cases & guards", listing non-obvious field semantics: `cnt_pa` DEAD during STOPPING, `cnt_pa_at_station=-1` pre-first sentinel, `frame_mode` dormant scaffolding (read by lower_lcd as early-return gate but never mutated).

The split honors "preloaded mental model vs progressive implementation detail": cycle shape is mental-model (preloaded); edge cases + field semantics are implementation details (read on demand at the relevant edit site).

DISPLAY.md also got a Pass 1/2/3 cleanup applying the same EDIT-CONTRACT discipline the morning's session codified: deleted "Key Changes from Legacy `display.py`" migration table (display.py was deleted in the 2026-04-25 refactor), pre-2026-04-25 history notes, "Future: Element Clear-Background Convention" placeholder, entire "Distribution" section (CLAUDE.md already points to `/build`), 2026-03-11 + 2026-03-14 changelog entries; merged the "Skip animation runs through the renderer in pure-read mode" sub-subsection into its parent (kept the call-order code block, dropped the wrapper); trimmed the Mode Renderer Design 17-line code example to a one-line pointer at upper_lcd.py; trimmed Design Decisions list 11 → 6.

### Click-jump MVP — App-state side shipped on the lower LCD

#### Contract

Trigger: user clicks a station cell on the lower LCD.
Action: `app.jump_to_stop(target)` + `audio.pause()` (lifted into `jump_to_stop` itself).
Result: `STOPPING@target` — `at_station=True`, `cnt_pa=0`, `cnt_pa_at_station=-1`, all animation rig zeroed, `cnt_sta=0`, `departure_time=0.0`.

#### Implementation

- `JapaneseDisplay.hit_test(state, mx, my)` — full-route view. Multi-row band split: row 1 covers `[self.y, mid_split]`, row 2 covers `[mid_split, row2_bar_bottom + 30]` where `mid_split` sits between row 1's bar bottom and row 2's bar top. Single-row layouts use the full band. Returns sim_idx (`gi - display_offset`) for clickable cells, `None` for pre_stops (sim_idx < 0) or out-of-cell clicks.
- `JapaneseEightStationDisplay.hit_test(state, mx, my)` — single-row band from `label_top_y` to `badge_y + badge_h + 5`. Same sim_idx semantics.
- `LowerDisplay.hit_test(mx, my)` — dispatches to the active renderer (mirrors `draw()`'s mode dispatch). ENGLISH mode falls back to `japanese_display` so clicks DO work in ENGLISH (visually mirrors Japanese full-route).

#### Hit-test caching pattern

Both renderers stash their last-rendered window as `self._last_window` in `show_stops`. `hit_test` reads from there, ensuring (a) click geometry matches what was actually drawn, and (b) avoiding re-running `_get_stops_list_disp` (which has the `continuity[2]` mutation side-effect — it would re-mutate on every hover frame otherwise).

#### Filter placement

- **Pre_stops** (display_index < display_offset) → renderer's `hit_test` returns `None`. Lives in renderer because pre_stops is a property of the displayed content, not the click handler's concern.
- **Past-dest** (sim_idx > `dest_stop_idx` on non-circular routes — Keihin 727B's 磯子→大船 reference tail) → click handler's `_click_target`. Lives there because `dest_stop_idx` is on `PASimulator`, not the renderer.

#### `audio.pause()` lifted into `jump_to_stop`

The 2026-04-28 WIP previously said "callers should `audio.pause()` before invoking" but the cleaner contract is "`jump_to_stop` self-pauses, callers don't have to." Single ownership point covers click-jump, preview ←/→, and any future caller. `_SilentAudio` no-op makes preview-mode pausing harmless.

#### Hover cursor

`_update_hover_cursor` ticks every frame in `run()`. Same predicate as click filter (via shared `_click_target` helper) so hover-feedback exactly matches click behavior. `pygame.mouse.set_cursor` is cheap when state unchanged. Off-LCD / past-dest / pre_stops cells stay `SYSTEM_CURSOR_ARROW`; clickable cells switch to `SYSTEM_CURSOR_HAND`.

#### MOUSEBUTTONDOWN wiring

- Real-app `run()`'s pygame event loop: new `elif` branch after the existing debug-panel click branch — handles clicks below the panel via `_handle_lcd_click` (which subtracts the `DEBUG_PANEL_HEIGHT` offset when `auto_input=True`).
- Preview `_handle_input_preview`: new `elif` next to KEYDOWN — preview drains all events itself, so MOUSEBUTTONDOWN must be handled here too.

### Autodriver Layer 3 refactor — bringing code in line with the doc

Mid-evening user noticed the AUTO_INPUT.md Layer 3 section described the canonical names (`STOPPING_FRESH` etc.) and the truth table as if they were exposed in code, but the code was still using raw badge + flag combos in event checks. The doc had been leading the code by ~24h since yesterday's design session settled the vocabulary.

Refactor closed the gap:

- **Module-level `Layer3State` namespace class** with the 6 canonical strings (bare strings, no enum — they round-trip cleanly through status dict + JSONL).
- **`_Detector.inferred_state()` accessor** — pure function of `prev_badge` + Layer 2 cache. Truth-table verified manually against AUTO_INPUT.md spec (8 cases pass: UNKNOWN, STOPPING_FRESH, STOPPING_AFTER_ARR, APPROACHING_BEFORE_DEP, APPROACHING_AFTER_DEP, MOVING_AFTER_ARR + PASSING variants).
- **Renamed `_Detector.*_fired` → `*_observed`** (3 attributes). Captures actual semantics: flags record "we observed the trigger condition this segment," not "we dispatched a fire." The dispatcher's mismatch-skip is a separate concern from observation.
- **Surfaced `inferred_state` in three places**: `sim.auto_input_status` dict (read by debug panel), JSONL drive log per-sample line (read by `data_tools/plot_drive.py`), debug panel line 3 — which now uses Layer 1 / Layer 3 vocabulary explicitly: `"app: stopped at X · game: STOPPING_FRESH"` instead of the old `"state: stopped at X"` hybrid.
- **Comments updated** to use Layer 3 vocabulary (no bare "badge=STOPPED" reasoning).
- **Backward-compat in `data_tools/plot_drive.py`**: `s.get("departure_observed", s.get("departure_fired"))` — reads the new key, falls back to legacy. Old JSONL logs from before the rename still render.

Out-of-scope (deliberately not touched): `data_tools/capture_game.py`. It's a separate diagnostic process with its own `_Detector` clone — the rename was scoped to in-process `auto_input.py`, not cross-process.

### WIP doc shuffle + AUTO_INPUT.md tightening

`WIP_lower_lcd_click_to_jump.md` was the working doc for the click-jump design. With the MVP shipped, content split:

- App-state behavior decisions → all shipped in code, no migration needed.
- OCR state machine vocabulary → already canonical in AUTO_INPUT.md § Layer 3.
- Entry-point flow design (still pending) → migrated to new `WIP_autodriver.md`.
- Q1-Q4 resolutions → migrated.

`WIP_autodriver.md` additionally absorbed AUTO_INPUT.md's Status / Calibration insights / Validation history / Future enhancements / For-next-session-Claude sections — everything that's not "fact or current item" per the user's directive ("put everything that's not a fact or current item back to WIP documentation"). This is what the EDIT-CONTRACT prescribes: domain doc holds facts; WIP holds pending design + history.

AUTO_INPUT.md tightening:

- Mental Model gained "Two integrations" paragraph (in-process via `auto_input.py` + separate-process via `data_tools/capture_game.py`).
- Usage section expanded to cover both 1a (`uv run main.py` toggle) and 1b (`data_tools/capture_game.py`) — was 1b-only.
- Layer 1 description simplified to a 6-line pointer at DISPLAY.md (the press-flow table was duplicating DISPLAY.md content; per "Tighten before appending" + single-source-of-truth, it became a cross-reference).
- Naming-collision note updated to use underscore style (`STOPPING` / `APPROACHING_EARLY` / `APPROACHING_FINAL`) consistent with CLAUDE.md mental-model + Layer 3 suffixed names.

`WIP_lower_lcd_click_to_jump.md` deleted. `TODO.md` click-jump entry checked off with a note that autodriver re-anchor on click-jump is still pending and lives in `WIP_autodriver.md`.

### `jump_to_stop` is single-purpose — confirmed

User pushed back hard during the design discussion against extending `jump_to_stop` with `APPROACHING_EARLY` / `APPROACHING_FINAL` sub-state variants for the autodriver toggle-ON alignment case. *"jump_to_stop only does 1 thing that's to jump to stop with STOPPING at the station, does not care about if it's approach early, approaching late."* The silent-advance mechanism for the alignment rule's "advance App by one stop" rows is **TBD**, to be designed alongside Layer 1 work. NOT a `jump_to_stop` variant. Captured in `WIP_autodriver.md` § "Pending — Entry-point flow → `jump_to_stop` is single-purpose".

### `/review+fix` — 2 cycles to clean

**Cycle 1** caught:

- **AUTO_INPUT.md `*_fired` references throughout the body** — the rename shipped in code but the doc still named flags as `*_fired` and explicitly flagged the rename as "pending." Critical doc-vs-code drift. Fixed via `replace_all` on `departure_fired`, `arrival_fired`, `at_station_fired`, `fired-flags`, `fired flags` + manual deletion of the "(Planned rename...)" parenthetical + addition of `inferred_state: str` to the status-dict schema.
- **DISPLAY.md "Audio-playing guard" wording** — told callers to pre-pause when `jump_to_stop` now self-pauses. Reworded.
- **AUTO_INPUT.md `STOPPING_FRESH` description** — undersold the trigger by listing only boot/post-click. Extended with "or arrival trigger missed by OCR."
- **`LowerDisplay.hit_test` docstring** — listed "English placeholder" as non-clickable, but ENGLISH falls back to `japanese_display.hit_test` (clicks DO work). Dropped the misleading parenthetical.
- **`panel_h` duplication in app.py** (info-level, deferred) — same formula in 3 places; optional cleanup.

**Cycle 2** verified clean across cross-doc consistency (Layer 1 vocabulary uniform across CLAUDE.md / AUTO_INPUT.md / DISPLAY.md / WIP_autodriver.md), Layer 3 truth table matches code line-for-line, AppState contract block accurate, no stray cross-references to deleted WIP doc (memory/2026-04-28.md still mentions it but that's immutable history — left as-is per memory-log convention).

### EDIT-CONTRACT pattern earned its keep

When migrating WIP→AUTO_INPUT and AUTO_INPUT→WIP_autodriver, the morning-codified EDIT-CONTRACT block (refuse list: history notes, code-snippet illustrations, speculative future, design-rationale prose, cross-doc duplication) drove the placement decisions automatically. AUTO_INPUT.md's deleted sections (Status / Calibration insights / Validation history / Future enhancements / For-next-session-Claude) all matched a refused shape. WIP_autodriver.md was the right destination for the same reason — it explicitly holds non-fact / non-current content. The contract turned a judgment call into a mechanical check.

## Documentation updates (evening)

- `app.py` — `jump_to_stop` calls `audio.pause()`; `_handle_lcd_click` / `_click_target` / `_update_hover_cursor` helpers; MOUSEBUTTONDOWN wiring in `run()` and `_handle_input_preview`; `AppState` `# CONTRACT:` block.
- `displays/train_models/e235_1000/lower_lcd.py` — `JapaneseDisplay.hit_test`, `JapaneseEightStationDisplay.hit_test`, `LowerDisplay.hit_test`, `_last_window` stashing in both `show_stops`, `Optional` import.
- `auto_input.py` — `Layer3State` namespace class, `_Detector` rename + `inferred_state()` accessor, status dict + JSONL log key changes, debug panel line 3 rewrite to Layer 1 / Layer 3 vocabulary.
- `data_tools/plot_drive.py` — backward-compat fallback for old logs.
- `CLAUDE.md` § Mental Model — new "App state machine" subsection.
- `DISPLAY.md` § Unified State Machine — "Edge cases & guards" subsection; Pass 1/2/3 cleanup deletions; cycle-1 audio-guard wording fix.
- `AUTO_INPUT.md` — Mental Model "Two integrations" paragraph; Usage section update; Layer 1 simplified to pointer; naming-collision underscore fix; cycle-1 `*_fired → *_observed` sweep + `inferred_state` schema entry + STOPPING_FRESH wording fix; deleted Status / Calibration insights / Validation history / Future enhancements / For-next-session-Claude sections.
- `WIP_autodriver.md` (new) — pending entry-point flow design + alignment rule + resolved Qs + validation history + calibration insights + future enhancements + for-next-session-Claude handoff.
- `WIP_lower_lcd_click_to_jump.md` (deleted).
- `TODO.md` — click-jump entry checked off; pointer to `WIP_autodriver.md` for autodriver re-anchor follow-up.

## Preferences (jot) — evening

- `[log-only] topic:doc-strategy — "put everything that's not a fact or current item back to WIP documentation" — context: directive when migrating AUTO_INPUT.md sections; consistent with the morning's just-promoted "Tighten before appending" principle, no separate rule needed`
- `[log-only] topic:autonomy — "after that have /review+fix on your changes. then /session-recap, then commit. You can do those yourself, I trust you." — context: explicit autonomy waiver for tonight's batch (refactor + review + recap + commit); per skip-confirmation rule the waiver applies to the current batch only`
- `[log-only] topic:scope-discipline — "You don't have to afraid those case becuase they don't even exist" — context: user pushing back on speculative jump_to_stop sub_state variants during click-jump design; aligns with existing Pragmatic-over-perfect`
