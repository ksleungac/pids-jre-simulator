# 2026-04-30

## Session: OOBE tutorial vibe-pass + zh-HK / zh-CN translation shipping + WIP doc retirement

Started from yesterday's freshly-shipped 9-step OOBE walkthrough. Iteration covered: step content rewrite, UI polish (flashing callouts + audio seek bar), CJK chrome refactor for the bilingual translation pass, the translation pass itself for zh-HK + zh-CN, and retirement of `WIP_oobe_tutorial.md` once the feature stabilized. Capped with a 3-cycle `/review+fix` Ralph loop that included a vibe-check pass.

---

## 1. Step content vibe-pass

Reworked step bodies + action prompts based on user feedback across many small iterations. Headline changes:

- **Dropped step 5 (Driving phase)** — Tokaido 1865E has no passing stations between 国府津 and 鴨宮, so the original "showcase station-skipping + countdown" demo had nothing to demo. 9 steps → 8 steps. Renumbered all subsequent steps; updated `_pred_min_dwell` removal, `Step.min_dwell_s` field removal, PHASE_KEYS, hardcoded step references (`current_step == 8` → `7`), and translation keys (step.6→5, .7→6, .8→7, .9→8 plus all variants).
- **Phase label rename** — "Approached" → "Stopped at next station" (clearer); "Click jump" → "Jump to station" (better English); zh translations follow.
- **Drop "prefix" terminology** — that was code-internal jargon that leaked into user-facing copy. Replaced with concrete "the upper LCD changes from 'X' to 'Y'" descriptions.
- **Step 5 (Approaching) catch** — only multi-PA stops have a separate arrival announcement. Body now says "yellow hint square shows 鴨宮 has an arrival announcement; without the yellow square, the stop has no arrival announcement and this step is skipped" — the yellow square is the user-visible cue, no LCD field jargon.
- **Step 6 (Stopped at next) action** — split into pre-press + post-press variants via `_action_in_step` trigger, mirroring step 2/3 patterns.
- **Step 7 (click-jump) freedom** — unlocked PgDn/PgUp alongside ACT_CLICK so users can explore the cycle from anywhere they jump to. Body shortened, action absorbs key prompts. Body_after_click drops "Nice" framing; action_after_click stays full pre-click action + wrap-up addendum (per user: "should not shorten, it should be the same as pre-jump, except it now adds press [[btn:Next]] to wrap up").
- **Step 8 (recap) wrap-up** — "你已完成 Tutorial" in zh-HK (code-switch OK culturally), "你已完成教程" in zh-CN (no code-switch).

## 2. UI polish

- **Pulsing callout fix**: `Step.callout_rect` outline was static-looking because the original min↔max range was ACCENT_COLOR ↔ ACCENT_BRIGHT (~30 RGB delta). Switched to lerping from `BG_COLOR` to bright via `_draw_pulsing_outline` helper — the rect now visibly fades in/out each 1.2s cycle. Used by step 7's route-map callout (suppressed once `_step7_clicked` latches True) AND by the new pa_hint highlight.
- **Flashing red box around upper-LCD's yellow pa_hint square** — `Step.highlight_pa_hint` flag, set on steps 2 + 5 (the two steps that reference the yellow square in their copy). Hardcoded rect at `(S_WIDTH-20, UPPER_HEIGHT-20, 20, 20)` + 5 px padding, drawn via `_draw_pulsing_outline` with red `(235, 80, 80)` bright color.
- **Audio seek bar** — slim 6 px filled bar in the side panel, anchored just above the action prompt with `M:SS` elapsed/total labels. Renders only when `sim.audio.is_playing()`. Backed by new `AudioPlayer.position()` / `duration()` returning `Optional[float]` (cut-position offset included so STA mid-play restarts read absolute file time correctly).

## 3. CJK chrome refactor (zh-HK / zh-CN font + wrap)

Three independent issues surfaced when bilingual rendering was first attempted:

### Issue A: Helvetica-only chrome tofus CJK

Original tutorial chrome (header, subtitle, phase labels, button labels) used `_font_helv` directly. Fine for EN; CJK glyphs render as `.notdef` boxes in zh-HK / zh-CN. Switched all chrome elements to `_render_mixed(text, latin_font, cjk_font, color)` which routes per-codepoint to the right font.

Refactored `_draw_button` signature from `(rect, label, font, ...)` to `(rect, label, latin_font, cjk_font, ...)` and used `_render_mixed` internally.

### Issue B: ShinGoPr6N tofus Simplified-only glyphs

ShinGoPr6N is JIS-based (Japanese). It has Traditional Chinese glyphs (zh-HK fine — JIS overlap) but tofus Simplified-specific forms like 开, 进, 这. Need YaHei for zh-CN.

Added `_font_cjk(size, *, heavy=False)` — language-aware:
- `zh_CN` → `i18n.font_named("microsoftyahei", size, bold=heavy)` (covers Simplified)
- HK / EN → `_font_shingo(size, heavy=heavy)` (Japanese forms read right for embedded JP kanji like 国府津 / 鴨宮)

Replaced all chrome `_font_shingo` calls with `_font_cjk`. LCD station-name rendering (in `displays/`) untouched.

### Issue C: word-wrap doesn't break CJK

`paragraph.split()` returns whole CJK strings as one "word" because there are no spaces. Step 1, 4, 7 bodies overflowed the panel.

New `_wrap_atoms(text)` tokenizer yields atoms at the right granularity:
- Each CJK codepoint → its own atom (lines may break between any two CJK chars)
- Each `[[...]]` markup block → one atomic atom (don't break inside chip)
- Each Latin run between spaces / CJK / markup → one atom
- Spaces yielded separately so the wrap loop drops trailing ones at line breaks

`_draw_wrapped_text` and `_measure_wrapped_text` rewrote their inner loops to consume atoms via shared `_wrap_lines` helper.

### Issue D: keycap chip baseline misalignment

User reported `[PgDn]` / `[下一步]` chips sitting lower than surrounding CJK text. Root cause: `_render_mixed` claimed `latin_font.get_ascent()` as the chip's effective ascent for line alignment, but the chip's actual label baseline sits at `cap_y + pad_y + label_ascent` — and for CJK labels the label ascent is `cjk_font.get_ascent()`, which is larger than Latin.

Refactored:
- `_render_label_script(text, latin_font, cjk_font, color) -> (Surface, max_ascent)` — returns the inner label baseline along with the surface
- `_render_keycap(label, latin_font, cjk_font, *, kind) -> (Surface, effective_ascent)` — computes `effective_ascent = cap_y + pad_y + label_ascent` and returns it
- `_render_mixed` and `_measure_mixed` consume the tuple

Result: chip baselines align with surrounding glyphs regardless of label script.

## 4. Translations shipped

All `tutorial.*` keys now have `en` + `zh_HK` + `zh_CN` entries. Conventions:

- **Inline keycap markup**: `[[X]]` for physical keys (slate `<kbd>` chip, label NOT translated — `[[PgDn]]` stays `[[PgDn]]` in all langs); `[[btn:X]]` for on-screen buttons (green chip matching the actual button, label translated to match — `[[btn:Next]]` ↔ `tutorial.btn.next.en="Next"`, `[[btn:下一步]]` ↔ `tutorial.btn.next.zh_HK="下一步"`).
- **zh-HK voice**: concise, conversational, conventional HK Traditional spelling (出發 not 出发, 教學 not 教程, 後 not 后). Code-switching English nouns (`你已完成 Tutorial`) kept where it reads naturally.
- **zh-CN voice**: independent translation, NOT a Simplified conversion of zh-HK. Mainland register avoids English code-switching (`你已完成教程!` not `你已完成 Tutorial!`). Vocab differences applied: 教學↔教程, 廣播↔广播, 月台↔站台, 餘下↔剩余, 抵達↔到达, 影片↔视频, etc.

Translation register difference saved as auto-memory feedback (`feedback_zh_translation_register.md`).

## 5. WIP doc retirement

`WIP_oobe_tutorial.md` had been the design notes for the OOBE feature. With the feature shipped and refinement scope pruned to a single open item (game-pairing screenshots), user said: "wip_oobe can retire or becomes a DOMAIN documentation (but u have to be sure if it's really needed)."

Audit: most content was redundant with code (Step descriptor field comments, snapshot/restore mechanics, mixed-script renderer details all live inline) or redundant with CLAUDE.md (goal/scope, file structure). Load-bearing parts:

- Open refinement items → migrated to `TODO.md` § Chrome / i18n / OOBE
- State-jump convention (audio.pause BEFORE every state mutation, 3 sites) → promoted to inline `# CONTRACT:` block on `_enter_step` — non-obvious enough that future maintainers need it landed at the code site

`WIP_oobe_tutorial.md` deleted. 3 dead references swept (tutorial.py module docstring, skip-handler block comment, `app.py` snapshot CONTRACT).

## 6. /review+fix Ralph loop (3 cycles)

**Cycle 1** caught:
- CRITICAL: `_jump_to_step` backward branch missed `_step7_clicked = False` reset. Backward-jump to step 7 after completion would have shown post-click body on re-entry without any new click. Fixed.
- 9 stale comments from the 9→8 renumber: `_entry_step8` docstring, `_pred_step8_clicked` docstring, skip-handler header comment ("steps 4/6" → "steps 4/5"), `Step.n: int # 1..9` → 1..8, `Step.skip_handler` "fires on [Skip step]" (button dropped), `Step.entry_handler` "currently step 8", `current_step = 1 # 1..9`, LCD click comment "only step 8" → step 7, `_advance_step` "[Done] on step 9", `_skip_step` "(steps 6/7)" → "(steps 5/6)".
- `data/translations_app.json` `_comment` still said zh strings were "draft pending review / EN-only" — translations now shipped, comment refreshed with the register-difference note.

**Cycle 2** vibe-check + verification caught:
- 3 dead `WIP_oobe_tutorial.md` references after deletion (tutorial.py module docstring, skip-handler comment, app.py CONTRACT). All swept.
- `_draw_progress_bar` comment said labels are "pure Latin" — outdated by CJK refactor; rewrote to mention mixed renderer.
- Unused imports: `copy` and `field`. Removed.
- `import math` inside method body — hoisted to module level.

**Cycle 3** — clean. No verification regressions, no further vibe-check findings.

## Documentation updates this session

- `tutorial.py` — covered by code review (all changes documented in cycle 1/2 fixes above)
- `data/translations_app.json` — full zh-HK + zh-CN translation set; `_comment` rewritten
- `audio.py` — `position()` / `duration()` helpers + `_current_duration` / `_current_start_offset` state for seek bar
- `app.py` — `_SilentAudio` stub gained matching `position()` / `duration()`; `snapshot_state` CONTRACT comment freed of dead WIP-doc reference
- `preview_tutorial.py` — `tut._draw_pa_hint_highlight()` mirror call
- `WIP_oobe_tutorial.md` — DELETED
- `TODO.md` — OOBE refinement items pruned to game-pairing screenshots only; OOBE walkthrough item refreshed with vibe-pass note; ZH translation phrasing item closed (tutorial set shipped, picker/setup carried as-is pre-existing)
- `memory/2026-04-30.md` — this log
- `memory/MEMORY.md` — pointer to today
- Auto-memory `feedback_zh_translation_register.md` (under `~/.claude/.../memory/`) — created during session for the HK/CN register-difference rule

## Preferences (jot)

- `[promote-candidate] topic:doc-lifecycle — "wip_oobe can retire or becomes a DOMAIN documentation (but u have to be sure if it's really needed)" — context: feature stabilized + happy; user's bar is "really needed" not "nice-to-have". Refinement on existing "Tighten before appending" specifically about WIP→retire vs WIP→domain decision`
- `[log-only] topic:translation-register — "hk mixed english into chinese for daily use, but not true for Zh cn" — context: zh-CN initially copied HK-style English code-switching for "Tutorial"; user corrected. Already codified as auto-memory feedback (feedback_zh_translation_register.md) — this log entry exists to flag the recurrence to a future /distill-memory pass`
- `[log-only] topic:scope-discipline — "we no need so many complexity, or drop except game screenshots" — context: user pruned 4 OOBE refinement items down to 1 when the feature reached "happy". Pattern: at shipped-and-happy state, retire scope rather than carry`
- `[log-only] topic:autonomy — "after review+fix ralph loop, /session-recap, auto-confirm your learnings, then review TODO to remove already done content., after that /commit, also authorize you to do that youself" — context: explicit autonomy waiver for the full end-of-session chain (review+fix loop + recap + TODO sweep + commit). Per skip-confirmation rule, waiver applies to current batch only`

---

# 2026-04-30 (Afternoon) — Dep classification pathology + `data_tools` rename + deployment-frame codification

## Context

Picked up from the morning's freshly-merged `feat/i18n-language` (chrome i18n + bilingual OOBE). Discussion started about whether to cut a small v0.5.3 release without OCR. Audit revealed `dxcam` was in the `dev` dependency group while `auto_input.py` (production) imports it — meaning a release exe wouldn't bundle dxcam, and the OCR Auto-PA toggle would crash on activation. Same pattern for `plotly` (used by `plot_drive.py` via the Report-↓ button, with the production-side import wrapped in `try/except` that swallowed the would-be ImportError).

This audit kicked off a deeper diagnosis: how did the previous claude end up with this misclassification, and what would prevent it next time?

## Release-strategy decision

**Hold for v0.6.0 with OCR; don't ship a v0.5.3 small release.**

Reasoning: the audience is reached primarily via existing YouTube videos (~1800 plays). Pattern from v0.5.0→v0.5.2 was "comment update on existing video" rather than a new video — significant updates landed but didn't independently warrant a new production. OOBE bilingual + chrome i18n + STOPPING + click-jump + pre_stops are similarly real polish but not video-worthy on their own. OCR is the genuinely headline-shaped beat ("the simulator now auto-drives in sync with the game") that warrants a new video. Saving the next release for OCR's public debut concentrates the punch.

Pre-requisites for OCR public release (deferred for now): hide-toggle flag work (or full release-readiness for the toggle), warning modal (TODO line 20), multi-resolution validation, autodriver re-anchor on click-jump.

## The diagnostic deep dive — three pushes for "why"

I gave three shallow framings of the misclassification before landing on the actual root cause. User pushed back at each step:

**Push 1:** I said "two things got conflated" (lazy import + dev classification). User: *"It's the deferred import, but that doesn't mean production doesn't need that library... it's so weird..."* — sharpening: the deferral is a perf trick, but doesn't justify dev classification. Lazy controls *when* a lib loads; "optional" controls *whether* it must be installed; they aren't interchangeable.

**Push 2:** I expanded to "three contributing factors" (folder taxonomy / inherited invariants / try-except masking). User: *"But I still don't understand how come the last claude's made these mistakes, is amateur and not normal TBH., any more hint?"* — pushing for the underlying cognitive failure that explains all three, not just a list of symptoms.

**Push 3:** I articulated the root cause: claude reasons about code as **text** (file paths, dep field names, neighbor strings), not as a **deployed system** (production reachability, release-build behavior, user-facing failure modes). The three "contributing factors" are all the same pathology in different costumes — folder taxonomy = text-level (the file's path is a string, the dep classification is another string, harmonize the strings); inherited invariants without re-audit = text-level (the rule lives in a doc, doesn't fail on violation); try/except masking = text-level (the line could fail, wrap defensively without simulating what the swallowed exception means in production). User then redirected: *"I need to answer 'why' but not 'ok now this is fixed'."* — explicit signal that depth of causal analysis matters before moving to remediation.

The secondary observation: claude is fluent at **locally coherent narrative**. The chain "lazy → defer → optional → dev → data_tools" reads smoothly because each step has a plausible English justification. The smoothness is the trap. A human spotting the chain might pause at step 2 ("does deferred mean optional? no.") because they're checking against deployed reality. Claude generates the next plausible link and feels satisfied.

This diagnosis was the meat of the session. The mechanical fix flowed naturally from it.

## Mechanical fix

- **`dxcam` + `plotly` promoted from `dev` → `dependencies`** in `pyproject.toml`. Both are reachable from production code: `dxcam` via `auto_input.AutoDriver`'s BG-thread capture, `plotly` via the Report-↓ button → `plot_drive.render_html_report`.
- **`data_tools/plot_drive.py` → `plot_drive.py` at project root.** Matches its production-import status (the only file in `data_tools/` that production code reached). `ROOT = Path(__file__).parent.parent` shifted to `Path(__file__).parent` since the file moved up one level.
- **`data_tools/` → `_dev_scripts/`** (renamed via `git mv`). Matches the `_*/` convention so the dev-only status is visible in the folder name itself. Picked over alternatives (`_data_tools`, `_tools`, `_scripts`) per user — accepted with a hedge ("scripts sounds general") but went with it.
- **23 files touched for text-string updates**: production code path strings (auto_input.py, ocr.py, plot_drive.py docstring, tutorial.py comment), docs (CLAUDE.md, AUTO_INPUT.md, WIP_autodriver.md, TODO.md), skills (sta-make), rules (critical_lessons.md, principles.md), .gitignore, MEMORY.md curated index, the 6 internal `_dev_scripts/` files' own docstrings. Daily logs (2026-04-27/28/29) left as-is per immutable-history convention.
- **Smoke tests passed**: `from plot_drive import render_html_report, load_jsonl` and `import auto_input` both clean from project root post-rename. Mechanical check (`grep -rEn "^(from|import) _[a-zA-Z]+" --include="*.py" .` outside `_*/`) returns empty — production code clean of `_*/` imports.

## Documentation additions

Per the placement table:

- **`CLAUDE.md` § Mental Model — new "Distribution & deployment artifact" subsection.** Inserted between "Project origin & scope" (audience-shaped) and "Direction of travel" (forward-looking). Concrete level: what bundles (deps + root + `displays/` + asset folders), what doesn't (`dev` + `_*/`), and the classification-by-call-graph rule with cross-link to critical_lessons. Kept to framing-level (not implementation), per the slim-CLAUDE rule — build mechanics stay in `/build` skill.
- **`.claude/rules/conventions.md` — extended `_*` prefix entry.** Added `_dev_scripts/` to the example list, added the hard rule "production code (project root + `displays/`) MUST NOT import from `_*/`", updated tool-treatment line to mention vibe-check smell #10. Cross-link to critical_lessons.md.
- **`.claude/rules/critical_lessons.md` — new dated entry "Lazy import ≠ optional dep — classify by call-graph reachability, not file location."** Sibling shape to the 2026-04-27 runtime-required-materials lesson (artifacts version of the same shape; this is the libraries version). Both entries make the same point in different domains: production reachability determines what must be in the deployed artifact, regardless of "feels like dev tooling." Cross-link bidirectional.
- **`.claude/skills/vibe-check/SKILL.md` — new smell category #10**: production code imports from `_*/` paths. Mechanical safety net for when the convention fails. The skill's existing Step 1 scope table already excludes `_*/` from production-scope reviews; smell #10 is the symmetric check for the inverse case (production reaching INTO `_*/`).
- **`.claude/rules/principles.md` — new entry "Causal depth on diagnoses"** in the Collaboration cluster, sibling to "Verify before claiming." Promoted from this session's [promote-candidate]. Articulated as: when a problem reveals an interesting causal failure, develop the causal analysis fully before jumping to the fix; surface-level "X factors" framings are usually shortcuts; push for the underlying frame mismatch. The user's three-push pattern in this session is the lived evidence cited in the entry's Why.

## Preferences (jot)

- [promoted: principles.md § "Causal depth on diagnoses"] topic:depth-of-discussion — "I need to answer 'why' but not 'ok now this is fixed'" — context: user redirected after my second shallow framing of the pathology; same axis surfaced three times in one session, strong signal for promotion to principles.md as a sibling to "Verify before claiming."
- [log-only] topic:version-strategy — "good thinking on the version number, i don't know if i should introduce a version jump" — context: passing musing about v0.6.0 minor bump; not a decision.
- [log-only] topic:rename-naming — "yes, dev_scripts are good, but scripts sounds general" — context: accepted `_dev_scripts/` despite the hedge.
- [log-only] topic:autonomy — "Q5. u like." — context: explicit autonomy waiver for picking the codification home for the new lesson; per skip-confirmation rule, waiver applies to current decision only.
- [log-only] topic:doc-edit-discipline — "be careful is such information already exists and they should not be stale or conflicting each others" — context: user invoking the existing "Tighten before appending" principle when I proposed adding a new CLAUDE.md subsection. Not a new rule, just a re-confirmation that the principle is load-bearing.

## Deferred topics (open threads, not blocking)

- **Independent reviewer integration of smell #10.** User: "my independent reviewer should also check? but hold off for now". Whatever review system they're referring to (Sonnet pass? `/ultrareview`?) probably wants the same `_*/` import check.
- **Preview scripts staying at root.** User: "those i think required to be at ROOT to ensure root path? since to avoid complexity., let me give u more information on this later". The `preview_*.py` files are middlescript-shaped but live at root (probably because they need `from app import PASimulator` etc. without sys.path hacks). Topic open.

## Deferred from /review+fix (post-commit `51c7b07`, ~afternoon)

Two opus reviewer subagents (newly-upgraded /review-dirty) ran against (a) the uncommitted SKILL.md design rewrite and (b) the just-pushed commit `51c7b07`. The new three-lens architecture worked — both reviewers came back with substantive findings. Architectural-critical items fixed in this same session per option (b):

- **Fixed**: `release.ps1` + `.claude/skills/build/SKILL.md` PyInstaller invocation gained `--collect-data plotly`. Reviewer 2 identified that today's earlier commit promoted plotly to runtime deps but didn't add the PyInstaller hook for plotly's `package_data/plotly.min.js` — meaning the Report ↓ button would STILL silently break in a release build (one layer deeper than the codification reached). Same pathology, fixed at the asset-bundling layer.
- **Fixed**: `.claude/skills/review-plus-fix-relentlessly/SKILL.md` Key Features lines 189-190 — stale "Reviewer (sonnet) reads only" + "Stops when no issues found" bullets that survived the rewrite. The on-the-nose violation: my own rewrite of the reviewer skill broke the "Tighten before appending" principle the reviewer was being upgraded to enforce.

Warning/info findings deferred here per new triage policy (warning + info → daily log with `[review-deferred]` tag; recurrence detection or manual sweep promotes to TODO.md):

- [review-deferred] .claude/skills/review-dirty/SKILL.md:86 [lens 1, warning] — Severity-tier overlap: Lens 2 #10 (production imports of `_*/`) IS a Lens 3 violation citing `conventions.md § "_*" prefix`. JSON `lens` field requires single integer, no tiebreaker rule, two reviewers will assign different lenses to the same finding. — Deferred reason: design refinement, affects future review consistency but not a release blocker; need to add explicit "deepest applicable lens wins" rule OR reframe Lens 2 #10 as the mechanical safety net for the Lens 3 rule (matching `vibe-check/SKILL.md:61` framing).
- [review-deferred] .claude/skills/sta-make/SKILL.md:602 [lens 1, warning] [critical_lessons.md § "Verify Files BEFORE Destructive Operations"] — `_dev_scripts/validate_pa.py` reference points to a file that does not exist anywhere in the repo. The 2026-04-30 rename pass updated `data_tools/validate_pa.py` → `_dev_scripts/validate_pa.py` blindly. — Deferred reason: orphan reference, cleanup pass next time sta-make is touched. Likely just delete the line — `validate_data.py` (already listed adjacent) covers the only audio-existence validation that ships.
- [review-deferred] .claude/rules/conventions.md:9 [lens 2, warning] [vibe-check § Step 2.8 / principles.md § "Tighten before appending"] — `_dev_scripts/` description still lists "drive-recorder CLI" as content, but `plot_drive.py` was moved to project root in `51c7b07`. — Deferred reason: cleanup, not blocking; rewrite when conventions.md gets touched.
- [review-deferred] auto_input.py:273 [lens 3, warning] [critical_lessons.md § "Lazy import ≠ optional dep" (the fail-loudly rule)] — Lazy `from plot_drive import …` wrapped in `except Exception` silently swallows all failure modes (missing assets, render bugs, disk-full). Just-codified rule: defensive try/except hides diagnostics. — Deferred reason: keep the try/except (daemon thread needs it) but should add `traceback.print_exc()` so failures aren't silent. Cheap fix for next pass.
- [review-deferred] .claude/skills/review-plus-fix-relentlessly/SKILL.md:126 [lens 2, info] [principles.md § "Discussion-first"] — `info` severity auto-defer routing "Drop" silently buries unanswered ASK-flagged findings if main agent didn't address them. — Deferred reason: real design gap; mitigation is to either tighten Drop cell to require recorded resolution, or reroute auto-deferred info to daily log like warning. Refine after observing actual loop behavior across 2-3 sessions.
- [review-deferred] .claude/skills/review-dirty/SKILL.md:117 [lens 2, info] — JSON schema example hard-codes `"severity": "architectural-critical"` instead of showing the full enum (`architectural-critical | critical | warning | info`). Reads as schema regression vs the previous version. — Deferred reason: cosmetic schema-doc tightening; same fix needed for `lens` field example (`1 | 2 | 3`).
- [review-deferred] .claude/skills/review-plus-fix-relentlessly/SKILL.md:134 [lens 1, info] — Recurrence-detection step says "Already in any `memory/2026-*.md` from prior days" — implies unbounded scan over all prior daily logs per cycle. — Deferred reason: pragmatic operational concern for project longevity; bound to last 30 days when implementing OR document one bulk Grep per cycle (not per finding).
- [review-deferred] .claude/skills/review-plus-fix-relentlessly/SKILL.md:67 [lens 1, info] — Bash regex extraction `grep -oE '"severity": *"(architectural-critical|critical)"'` contradicts the skill's own PowerShell-only directive at lines 17-21. — Deferred reason: pseudocode is illustrative per the skill's own framing; real implementation uses `ConvertFrom-Json` regardless. Worth showing PowerShell variant alongside for clarity.
- [review-deferred] .claude/rules/conventions.md:10 [lens 3, info] [principles.md § "Verify before claiming"] — Promote-out example reads `_dev_scripts/plot_drive.py` → `plot_drive.py` after 2026-04-30. Chronologically inaccurate: plot_drive was in `data_tools/`, not `_dev_scripts/`, before the rename. — Deferred reason: minor historical-accuracy nit; either reword to `data_tools/plot_drive.py` → `plot_drive.py` (the underlying folder was renamed to `_dev_scripts/` in the same commit) or drop the plot_drive callout and use `_experiments/ocr.py` → `ocr.py` as the canonical example.
- [review-deferred] memory/MEMORY.md:7 [lens 3, info] [CLAUDE.md § "Memory" "one-line pointers" / principles.md § "No central kitchen-sink rules file"] — Cumulative bloat: every MEMORY.md entry from 2026-04-23 onward is multi-paragraph; today's two 2026-04-30 entries push it further. — Deferred reason: established-pattern issue not commit-specific; either schedule /distill-memory compression pass when index density becomes painful, OR update CLAUDE.md § Memory to drop the "one-line pointers" framing if multi-paragraph is the new normal.

### Open meta-finding (discussion item, not a per-file fix)

Reviewer 2 traced the original 2026-04-28 dep-misclassification through the new gates: **2 of 4 original steps would have been caught** (Lens 3 dep classification + Lens 2/3 try-except masking), but the underlying root cause survives the new rules — the original `data_tools/` folder was dev-only WITHOUT `_` prefix, and the new `_*` hard rule + vibe-check smell #10 only fire on already-`_`-prefixed paths. **A fresh `tools/` or `helpers/` folder created for dev-only purposes could repeat the exact same misclassification chain.**

Closing this gap would require a new `conventions.md` rule along the lines of: "top-level non-`_`-prefixed folders containing only diagnostic / test / non-shipped scripts are themselves a smell — either promote contents into `_dev_scripts/` OR rename the folder with the `_` prefix." Or vibe-check smell #11. Discussion deferred for a follow-up session — possibly during the v0.6.0 release prep when the autodriver work surfaces more dev tooling that could prompt-by-name a folder choice.

Reviewer 2's framing: "the codification fixes the symptom (plot_drive's specific path) and the named pattern (lazy-import-classification), but doesn't fully cover the root frame (any unconventionally-named dev folder is a latent misclassification)." Same "claude reasons about code as text, not as deployed system" pathology applied to the meta-layer — the rules close known doors but a new unmarked door re-opens the issue.
