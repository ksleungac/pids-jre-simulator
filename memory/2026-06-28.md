# 2026-06-28

## Session: setup_tims `--tims` launch bridge + 番台選択 model picker + i18n graduation

Continuation of the TIMS-chrome work — three threads, all on the `setup_tims/` package.

**i18n graduation (the subagent incident).** Dispatched two parallel subagents per last session's
"dispatch parallel subagents" codification: one to codify the Noto font decision (docs — clean), one to
move the per-module `*_BY_LANG` label dicts into `translations_app.json` + rewire to `i18n.t()`. The
second BROKE the package — deleted the dicts but left ~26 dangling refs across 5 modules + broke band's
button calls, then reported "imports + render OK" (false: imports pass because Python doesn't run function
bodies at import; its smoke render must not have hit the broken paths). Caught via editor diagnostics,
reverted, redid inline. Lesson: subagents for read-only fan-out + self-contained doc/research, NOT
cross-file code surgery. User also required i18n strings be confirmed first → re-did the extraction with a
zh_CN confirmation gate (user corrected several: 交路选择 not 运用模式选择, 设定 not 设置, 终到站, …). Added an
optional `lang=` to `i18n.t()` for the band's worst-case-across-locales button sizing (the one site needing
a non-active locale).

**Launch bridge — why `--tims` + side-by-side.** A `--tims` flag launches `setup_tims` ALONGSIDE legacy
`setup.py` (not a hard swap) because the new flow can't wire OCR-launch yet (needs the 自動放送設定 page +
consent) and a hard swap would temporarily drop the shipped OCR-auto-PA feature. Manual-mode-first.
`pa_setting`'s 確認 (a dead-end placeholder) became the manual launch (起動), building a config shaped like
`setup.run()`. Start resolved by NAME against the committed variant (`stops.index(start_name)`) — closed
the deferred v0-index finding cleanly (the variant carries its own stops, so name-resolution is
variant-correct without threading indices).

**Model picker (番台選択).** User's idea: repurpose the IRL 番線 (platform) slot — which we'd removed — into
a 列車型號 model-picker entry opening an X00AA grid like `tims_bandai_choice.png`. Design calls (user-driven,
iterated visually): show ALL models incl. unbuilt ones GRAYED, not hidden (the IRL grays unavailable 番台);
grayed = a silver palette, NOT a dark scrim (scrim read as "shadowed" not "disabled"); 2-line staggered
buttons (series系 left / 番台 right); current model FLASHES; no confirm/back (click commits); override
session-persistent (survives route changes within one run). Grayed roadmap trimmed to what the user named
(E231-500, E233-0/1000/5000) after I over-built the full E233 family — Simplicity-First reminder.

Verify: every screen rendered headless across en/zh_HK/zh_CN; bridge builds a real PASimulator + lands on
the chosen start (verified curr_stop). Nothing committed (user's recap-then-commit cadence).

## Codifications this session

- principles.md § "Dispatch independent work to parallel subagents" — scope caveat (not cross-file code surgery).
- conventions.md § UI code style — disabled TIMS button = silver palette, not a dark scrim.
- WIP_setup_redesign.md — launch bridge DONE (manual); new § "Model picker"; graduation step 5 + C07AA button row updated.
- TODO.md — closed the start-variant re-resolve finding; added the `--tims` launch follow-ups (OCR-launch / model display / tutorial / flip-to-default).
