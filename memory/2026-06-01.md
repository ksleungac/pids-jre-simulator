# 2026-06-01

## Session: OCR disclaimer popup — visual design

Implemented TODO #8: merged tutorial + consent into a single scrollable 720×560 popup with scroll-to-unlock OK button.

Top section two-column: intro text + notes in left column, game screenshot with animated HUD pulse in right column. Notes (amber resolution notice + dim misfires line) sit directly below the intro paragraph at the same font size and left edge — several rounds to land this; interim versions placed them after the flowchart or in 使用注意 as a duplicate.

4-step flowchart: box 0 = HUD crop from full-scene screenshot; box 1 = user-provided pre-cropped HUD panel (hud_sample.png, 147×100) with digit templates cycling right — amber border fires when a digit matches the distance value; box 2 = v×t→d; box 3 = animated 3D PgDn key (cap slides over shadow face on press, 2 s cycle).

Two-section structure: 功能說明 (intro + flowchart) then 使用注意 (consent + privacy as bullet points). Resolution note appears once only — amber, in left column under intro. Consent/privacy changed from manual-\n-split to wrap_text at full popup width after user pointed out unnecessary short wrapping.

New i18n keys: `how_it_works_heading`, `resolution` (zh_HK uses 解像度, zh_CN uses 分辨率). `beta` key trimmed to misfires-only after resolution note took over the preview qualifier.

hud_sample.png: user provided pre-cropped HUD panel; renamed from "image (2).png", loaded lazily as `_disclaimer_hud`. game_screenshot.png + hud_sample.png still at project root (pending move to data/).

Friction: "move the amber one" round — I added the resolution note to both locations instead of moving it, requiring two corrective turns. Pattern: when user complains about placement and asks for it "also" somewhere, read as MOVE not DUPLICATE.

## Codifications this session

- no codifications this session (memory only)
