# 2026-05-12

## Session: audio/ per-line README — gap-fill catalog + write-gate wiring

User raised idea of adding sub-folder README/CLAUDE.md under `audio/` + `data/`, citing in-module README as established practice and asking whether it could save CLAUDE.md mental-model space.

Brainstormed via `/brainstorming` skill. Key calls:

- **README, not CLAUDE.md.** Sub-CLAUDE.md auto-loads in subtree; earns its name only if there are agent-targeted rules that fire when editing files there. No such rules for audio/ or data/ — content is orientation, README is the right name.
- **audio/README earns its keep; data/README probably doesn't.** Audit of DATA_FORMAT.md showed it's tidy — almost zero content to redistribute. Audio/README fills a gap (folder convention + per-line IRL/sim quirks) that doesn't currently exist between CLAUDE.md's 2-line tree and DATA_FORMAT's schema. data/'s only orphan was `line_icons/` (small, foldable into DATA_FORMAT later).
- **Granularity: single audio/README + per-line subsections, not per-line README files.** YAGNI; only Yamanote has rich content today. Standard lines = 1-2 bullets. Per-line file scaffolding for 10 lines would create 8 empty docs that rot.
- **Skipped formal spec doc.** Per user: short task, design lives in chat + memory.

User course-corrected several times — scope-misclassification was the recurring trap:

1. Initial framing (mine) was "redistribute from DATA_FORMAT." After audit showed DATA_FORMAT is tidy with essentially zero movable content, framing became "gap-fill." Audio/README adds content currently undocumented, not relocates content from tidy docs.
2. I drifted on Display model — added "Display model: E235-0" to JY entry autonomously. User asked twice ("not sure if it's reframe"; "we discussed the readme like these are for gap filling...?"). I had reintroduced redistribution-from-DISPLAY_E235 under the cover of "in-spec/best-effort summary." Reverted: removed the bullet entirely. Reverse-direction (line → model) is a one-table lookup in DISPLAY_E235.md, not a gap audio/README needs to fill.
3. I missed JJ Joban WIP state — wrote a normal terse entry, user pointed out `validate_data.py` flags 52 issues on Joban. Should have read `route.json` content before authoring the entry, not just folder structure. Inspection ran via `uv run python validate_data.py`; result confirmed only JJ has WIP gaps; 8 other lines clean.

Net per-line entries:
- JY (rich): IRL circular, Sim quirks (dest-cycle override + wrap-around), Audio quirks (STA filename code_3 suffix), Layout anomaly (flat folder)
- JO (moderate): IRL through-service 久里浜→東京→成田空港, `pre_stops` modeling, Chiba frame-swap deferred, 千葉 no STA melody IRL
- JT (moderate): multi-diagram + 国府津 pa_at_station junction + descriptive PA
- JK (moderate): 727B `stops[41..45]` past route-level dest 磯子 for through-running reference
- JE (moderate): long PASSING leg through 千葉貨物 freight terminal + Tokyo-end elaborate melodies + legacy numeric PA
- JJ (WIP marker): explicit "validator flags are expected, not real bugs; don't auto-fix"
- JC / JA / JN / JU (terse): Name + Diagrams + ±1 audio quirk

Mining sources for per-line gaps: memory daily logs (16 line-mentioning files), DISPLAY_E235.md (30 mentions — mostly display-side, stayed there), DISPLAY.md (Keihin 727B through-running fact promoted), WIP_autodriver.md (Keiyo freight-terminal fact promoted), AUTO_INPUT.md (1 mention only — skill example, stayed). Scope-misclassification guard rejected: display-time render behavior (stays in DISPLAY_E235), generic schema rules with line-specific examples (stay in DATA_FORMAT), past incident traces (stay in principles/critical_lessons as illustrations).

Write-gates wired at four layers per discipline of "defense in depth":
- `/split-audio` + `/sta-make` → Documentation hook section before Out of scope + audio/README Related entry
- `/session-recap` placement table → new row for per-line IRL + sim quirks
- `/distill-docs` → audio/README added to in-scope target list

CLAUDE.md got two single-line pointers (file structure tree + Audio/Diagram row).

Surfaced and fixed mid-session: sta-make skill tagged generic recording-source patterns ("yamanote-style hostile recording", "Some lines (keiyo) have KAK") with discovery-instance line names. User: *"in fact Yamanote has KAK as well."* Both patterns are recording-source-driven, not line-specific. Six skill edits retagged patterns generically with one-line origin notes.

Surfaced as TODO.md housekeeping item: `validate_data.py` compound-destination rule is stale relative to DATA_FORMAT § Compound Destinations (2026-05-07 reframe to case-by-case). Validator hard-checks `"A&\nB"` form; doc allows single-line short compound dests. Tool bug, not data bug.

## Codifications this session

- `audio/README.md` — new file (folder convention + 10 per-line entries + EDIT-CONTRACT + `_archive` / `_mock` notes)
- `CLAUDE.md` § File Structure + § When Working On — two pointer lines to audio/README
- `.claude/skills/split-audio/SKILL.md` — Documentation hook section + Related entry
- `.claude/skills/sta-make/SKILL.md` — Documentation hook section + Related entry + 6 generic-pattern retagging edits (drop `yamanote-style` / `keiyo lines` discovery-instance tags)
- `.claude/skills/session-recap/SKILL.md` § Where things live — placement-table row for per-line IRL + sim quirks
- `.claude/skills/distill-docs/SKILL.md` § Scope — audio/README added to in-scope
- `.claude/rules/principles.md` § Verify-before-claiming — recurrence-list example added (route.json content vs folder structure), how-to-apply bullet added
- `.claude/rules/conventions.md` § Naming — "Name patterns by cause, not discovery instance" bullet added
- `TODO.md` § Housekeeping — validate_data.py compound-destination stale-rule item added

---

## Session: housekeeping pass — validator drift + skill-prose stale refs (post-compact)

Brainstormed extending the audio/README sub-module-doc pattern to other folders (data/, displays/train_models/, lcd_references/). Audit collapsed quickly:

- **lcd_references/** dropped — user reframed as ephemeral happy-dev directory; provenance doc would rot faster than data churn.
- **data/** thin gap — line_icons + lines.json line-slug asymmetry potentially folds into DATA_FORMAT § additions, doesn't warrant its own README. Closed set + low growth, unlike audio/'s open-ended per-line catalog.
- **displays/train_models/** zero gap — DISPLAY_E235.md sub-series catalog + conventions.md fork discipline already cover.
- **_dev_scripts/** + **fonts/** — redundant + skip.

Net call: pattern caps at audio/. No new sub-module READMEs this round.

Pivoted to housekeeping. Three items landed:

1. **`validate_data.py` compound-destination rule dropped.** DATA_FORMAT § Compound Destinations was reframed 2026-05-07 from "always A&\\nB" to "case-by-case match IRL"; validator's `check_compound_dest_format` kept the hard `^[^&\s]+&\n[^&]+$` regex for 5 days. Surfaced today on the JJ Joban WIP sweep (validator flagged "Tōkyō&Ueno"). Walked the option fork — (a) length-based heuristic, (b) drop entirely, (c) drop hard + warn extremes — and confirmed no encodable threshold survives "match IRL". Picked (b) drop entirely: bundled checks (whitespace, multi-&, empty halves) had zero historical fire rate across the ~3-5 compound-dest entries (all Yamanote-adjacent, all claude/user-edited, low drift risk). Validator stops adjudicating layout; IRL fidelity is reviewer's call. Smoke-tested: 53 issues all in joban/tsuchiura (expected per audio/README JJ WIP entry), 0 in `data/translations.json` — false alarm closed.

2. **`_dev_scripts/validate_pa.py` reference reframed cross-PC pending.** sta-make:663 lists the file in Related tools; file exists on user's other PC but never committed. Original 2026-04-30 reviewer flagging assumed phantom file → "delete the line." Reframed today to "commit on next other-PC switch + finalize the skill ref" (ship vs scrap decision deferred to when the file is actually viewable). Premise correction: not phantom, just unshipped.

3. **`conventions.md:9` `_dev_scripts/` description trimmed.** Example list said "audio splitters/validators, OCR-asset extractor, drive-recorder CLI, dxcam diagnostic"; drive-recorder CLI referenced plot_drive.py which was promoted to project root 2026-04-30. Replaced with "primitive-bans linter, dxcam + game-capture diagnostics" to match actual folder contents (lint_primitives.py + test_dxcam.py + capture_game.py). Preserved "middlescript" jargon.

Not landed this session (user called the batch at "for now"): conventions.md:10 chrono-error in the promote-out example (`_dev_scripts/plot_drive.py → plot_drive.py` should read `data_tools/plot_drive.py → plot_drive.py`), `data_tools/` directory re-appeared at root, ~17 other deferred review findings.

New rule codified: principles.md § "Sync downstream enforcers when reframing doc rules" — the compound-dest drift was the trigger. Pattern: when a doc rule reframes mechanical → case-by-case, audit downstream enforcers (validate_data.py, _dev_scripts/) in the same session; if reframed rule isn't programmatically encodable, drop the enforcer entirely rather than retaining as advisory.

## Codifications (post-compact session-block)

- `validate_data.py` — `check_compound_dest_format` + `COMPOUND_DEST_RE` removed (3 sites)
- `.claude/rules/conventions.md` § Naming `_*` examples — drive-recorder CLI → primitive-bans linter + dxcam + game-capture diagnostics
- `.claude/rules/principles.md` § Documentation strategy — new entry "Sync downstream enforcers when reframing doc rules" (peer to "Tighten before appending")
- `TODO.md` — line 80 closed, line 91 reframed (cross-PC pending), line 92 closed

---

## Session: pa-make cleanup + 2026-04-30 deferred-findings sweep (later)

User asked about open TODO items, surfaced pa-make / PA-skills as overdue cleanup. Wide-scope rename closed the 2026-04-30 `data_tools/` housekeeping in one pass — three files moved + skill text aligned + downstream refs updated + build script trimmed.

Three PA scripts (`transcribe_pa.py`, `format_pa_timestamps.py`, `validate_pa.py`) had sat untracked in `data_tools/` on this PC throughout the May 5-11 work despite the 2026-04-30 critical_lessons rename. The 2026-05-12 morning daily log had reframed `validate_pa.py` as "exists on user's other PC but never committed" — user clarified later that the "other PC" was actually this PC. File was here all along, just untracked at the un-renamed path. Move closes both housekeeping items in one action.

Sister cleanup on the pa-make skill: missing `## Documentation hook` section parallel to today's morning sta-make / split-audio wiring, missing `audio/README.md` entry in Related, stale Out-of-scope pointing STA work to `split-audio` instead of `sta-make` (split-audio is the generic splitter, sta-make is STA-specific). Conventions.md:10 chrono-error fix landed too — promote-out example read `_dev_scripts/plot_drive.py → plot_drive.py` but the actual history is `data_tools/plot_drive.py → plot_drive.py` (`data_tools/` → `_dev_scripts/` rename was the same 2026-04-30 incident but a separate event from plot_drive's promotion).

After pa-make work, user asked about remaining deferred review findings. Re-examination of the 2026-04-30 cluster (5 skill-doc + 1 conventions/vibe-check) revealed 3 had been silently invalidated 5 days after flagging by the 2026-05-07 triage-policy rewrite — TODO entries never got updated. The remaining 3 were cosmetic-or-structural and not worth fixing. Closed all 6 with one-line resolution notes.

Pattern observed and explicitly declined: deferred items rot when the underlying system gets rewritten without back-reference to TODO.md. Considered adding a write-time hook (session-recap or /commit step that greps TODO `## Deferred review findings` for entries citing files touched in current diff). Decision: skip. Existing TODO.md convention ("sweep when you touch a related area") covers it in spirit; periodic re-examination like today catches what slipped. Cost of 10-min ad-hoc sweep every ~2 weeks < cost of another step in already-heavy /session-recap. Revisit if rot rate climbs.

User flagged verbosity once during the deferred-findings explainer ("lol, it's getting complicated, so much information"). Already covered by `principles.md § No filler narration`; not codifying as new entry — would extend without changing substance.

## Codifications this session (later)

- `data_tools/` directory removed; `transcribe_pa.py` / `format_pa_timestamps.py` / `validate_pa.py` moved to `_dev_scripts/` (matches 2026-04-30 rename)
- `.claude/skills/pa-make/SKILL.md` — 6 path refs updated; new `## Documentation hook` section before Out of scope; Out-of-scope STA reference fixed (`split-audio` → `sta-make`); Related rewritten with `audio/README.md` entry + sta-make split clarified
- `.claude/skills/build/SKILL.md` — dropped `data_tools` from `$shipExclude`
- `.claude/rules/conventions.md:10` — promote-out example chrono fixed (`data_tools/plot_drive.py → plot_drive.py` with parenthetical on the parallel folder rename)
- `_dev_scripts/{transcribe_pa,format_pa_timestamps,validate_pa}.py` — docstring usage examples updated
- `TODO.md` — 9 items closed: data_tools rename (line 57), validate_pa cross-PC (line 91), conventions chrono (line 98), 6 deferred review findings from 2026-04-30 cluster (3 effectively resolved by 2026-05-07 triage rewrite, 3 cosmetic/structural won't-fix)
