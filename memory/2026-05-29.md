# 2026-05-29

## Session: Root tidy cont'd + session_init git sync

Root cleanup continued from 2026-05-28. Deleted `_enum_3line.py` (one-off transfer-info calibration survey, job done) and `requirements.txt` (legacy pip-freeze, project uses pyproject.toml/uv).

`sweep_todo.py` was stranded in `_dev_scripts/` after last session's `_harness/` reorganisation — the file move was missed. Remote PC had made a workaround (adding `_dev_scripts/` to `sys.path` in `session_init.py`). Diagnosed both: our approach (actually moving the file to `_harness/`) is cleaner. Reverted the sys.path workaround, moved the file, updated the docstring + SKILL.md reference.

`session_init.py` now auto-syncs git at startup: `git fetch` → compare ahead/behind → `git merge --ff-only` if only behind, divergence report if local is ahead. Lesson: `git status` without fetch first is meaningless for cross-machine workflows.

Surveyed root for `docs/` consolidation candidates: `DATA_FORMAT.md`, `DISPLAY.md`, `DISPLAY_E235.md`, WIP docs. `WIP_1080p_ocr.md` already deleted by remote (1080p OCR shipped in `2557d3e`). `ROUTES.md` stays at root (linked from all READMEs, GitHub renders as table). Domain docs + preview scripts deferred — high cross-reference churn, no decision yet.

Session recap: trimmed proposed codifications — if harness/default behaviour already enforces the lesson, no rule entry needed.

## Codifications this session

No codifications — all learnings either already enforced by harness or resolved in code.

---

## Session: README screenshot regen + assets/ swap

Regenerated all 5 README screenshots using `preview_display.py` and `preview_chrome.py`. Key decisions: 01=keihin compact 8-station view at Omiya, 02=sobu skip animation (次は都賀, 東千葉 skipped), 03=sobu full-route at Tokyo stop 0, 04=Tokyo transfer JO kept, 05=Shinjuku transfer JY added (new), 06=setup screen scrolled 3 rows. Two transfer screenshots now in README (Tokyo + Shinjuku) — README second row widened to 3 columns.

Incident: claimed no yamanote route data after glob `audio/yamanote/*/route.json` returned nothing. `audio/README.md § JY` explicitly documents flat layout with no diagram subfolder. Should have read the domain doc before the filesystem search.

## Codifications this session

- `principles.md § "Verify before claiming"` — added filesystem-search trigger (domain doc before glob)

---

## Session: unread_readme_hook sensor

Problem: claude was accessing module dirs (audio/, assets/) without reading their READMEs first, leading to confident-but-wrong claims ("no yamanote data"). Root: empty glob result suppresses the "verify" instinct; confidence in the filesystem result blocks checking the domain doc.

Solution: two layers. (1) `assets/README.md` co-located with the screenshots — data-task framing, exact regen commands, and explicit call-out of domain docs needed before regenerating. (2) `_harness/unread_readme_hook.py` PreToolUse(Glob) sensor: fires when glob targets a dir with a README.md that hasn't been Read this session; self-suppresses by scanning `transcript_path` JSONL. `.claude/settings.json` wired.

Key implementation finding: PostToolUse(Glob) additionalContext doesn't surface — only Edit/Bash/PowerShell work post-tool. PreToolUse with `hookSpecificOutput` JSON format is the confirmed mechanism for Glob context injection. Self-suppression via transcript scan (no temp files).

`data/README.md` deferred — DATA_FORMAT.md already covers it. Bash/Python wrong-field sensor deferred.

## Codifications this session

- `conventions.md § Tooling` — sensor tiers: added PreToolUse(Glob) `unread_readme_hook.py` entry; new hook-mechanism bullet (PostToolUse(Glob) doesn't work; hookSpecificOutput + transcript-scan pattern)
