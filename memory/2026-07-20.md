# 2026-07-20

## Session: TODO.md backlog → GitHub Issues migration + working-loop rewire

**The decision.** User wanted a "ticketing system" to replace the fuzzy keyword sweep (`sweep_todo.py` guessed which TODO items a commit closed by ≥2-word title overlap). Explored three mechanisms: a local MCP ticket-server, GitHub Issues, and an in-repo stable-ID + commit-trailer scheme. A `/third-man` design consult killed the MCP-server as over-engineering (verdict: in-repo minimal). But that verdict optimized for constraints the user then *changed* — multi-Claude concurrency (same-PC AND cross-PC), live observability (what's being worked / deferred / closed), anti-staleness, "less load-bearing," "don't reinvent the wheel." Verifying the two standing objections to Issues showed both were **already moot**: `TODO.md` + 60 `memory/` daily logs are already committed to a **public** repo (Issues exposes no new content), and `session_init` already `git fetch`es at start (a `gh` call is the same network assumption). Net: for a solo dev, GitHub Issues' native state/labels/`Closes #N` auto-close/server-side concurrency deliver every new requirement with *less* machinery than any bespoke tracker — the user's "afraid of reinventing" instinct was correct. Chose Issues.

**Migration.** Parsed `TODO.md` → `gh issue create` (area label per section; `## Closed-off paths` NOT migrated — anti-tickets). 65 open. Build-incidents #7/#8 closed (user: addressed via harness work); first-run-smoothness dropped (user); `temporal-stabilization` added from the upstream delta (see git reconcile).

**Loop wiring.** `session_init` now prints a `gh` backlog summary (open-by-area · in-progress · recently-closed · stale; fail-soft offline) in place of the keyword sweep; `/commit` writes `Closes #N`/`Refs #N`; `/session-recap` reconciles via `gh issue list` (closure authoritative, no fuzzy match); `CLAUDE.md § Working loop` documents pickup(`--add-label in-progress`)/park(`deferred`)/finish; `TODO.md` → pointer + closed-off ledger; `sweep_todo.py` retired.

**Git reconcile (root unexplained).** Mid-session, local `master` was 8 commits behind origin (`0d79744`) even though `session_init` reported an ff to `eb8a967` at session start — the reflog showed **no sync in this tree today**. Never explained; I wrongly floated a "concurrent external agent," but the odd `.gemini/antigravity-cli` worktree was my *own* third-man subagent's. The whole session's reads/edits were on the stale base. Reconciled: discard my edits to the 5 files that also changed upstream → ff to `eb8a967` → re-apply the (mechanical) staleness swaps on the fresh base. Migration itself was sound — the 0d79744↔eb8a967 `TODO.md` delta was only +temporal-stabilization / frame-swap→`[x]`, both reconciled on the GitHub side.

**Two errors (caught + fixed).** (1) A PowerShell `Where-Object { $_.title -match "Frame-swap" }` silently matched all 34 `review-finding` issues; the `foreach { gh issue close }` mass-closed them (only #47 intended). Reopened all 33 + re-closed a pre-existing #1 I'd inadvertently reopened. → codified to `critical_lessons §1`. (2) Attributed the leftover subagent worktree to a phantom concurrent agent without evidence; user corrected ("it's you"). Both are "verify the target / don't invent a cause" failures.

**Verification.** A zero-context `/third-man` cross-checked `git show HEAD:TODO.md` vs `gh issue list`: 65 open ↔ backlog, no orphans/dupes, closed-off-paths absent; caught #3 missing its `review-finding` label (fixed). The 3 other flags were user-directed decisions its committed reference predated.

## Codifications this session
- `critical_lessons.md § 1` — batch-destructive scope extended to bulk `gh`/issue mutations (verify the resolved target list before the loop).
- `CLAUDE.md § "Working loop (GitHub Issues)"` — new; the backlog-on-Issues workflow.
- `CLAUDE.md` session-startup line + `/commit` + `/session-recap` + `/distill-docs` skills + 14 docs — `TODO.md`→Issues pointer updates.

## Session (block 2): first real close-loop on the GitHub-Issues backlog — 5 easy review-findings

First live use of the new ticketing loop: picked "easy" deferred review-findings to close. **Reframed what "deferred" means** — a finding was parked because the review *fixer* lacked design context in that pass, NOT because the user must arbitrate it. For 4 of 5 the blocker was a **factual** question answerable from primary source (verify, don't decide): #64 "can the start grid yield idx 0?" (yes — the origin pick) + "is `jump_to_stop(0)` behavior-safe?" (yes — boot ≡ jump(0) ≡ `STOPPING@0`, app.py:44/355/631); #67 the true e235_0 slot wiring (5-station owns EIGHT universally, fallback = horseshoe not e235_1000-linear, `UPPER_HEIGHT`=130); #58 var-liveness. Only **#49** was a genuine placement CHOICE — repoint the dangling `critical_lessons §` cross-ref to the canonical `_swap_dict_literal` inline comment vs re-add the lesson; the editor is `_dev_scripts/` (non-shipping), so a source-edit gotcha stays at the code site, not in deployment-class `critical_lessons` (user: *"i don't care, go with the fix"*). #53 was already-satisfied (annotation present) → close-only.

**The test detour + the learning.** Reflexively proposed a stub-sim T1 for #64's guard; user: *"what is this test… too redundant?"* — right: it asserts `is not None` against a 3-line function written to be asserted (no independent oracle). Course-corrected: the "testability is a precondition" bar is for the silent-failure class, not read-obvious one-liners; extracting the guard *just* to test it = over-engineering. Deeper insight the user drove: **TIMS has no logic core** like the auto-driver — it's a page flow; testing it meaningfully = the pure config-assembly **seam** (`_build_config`: result → launch config, where the #64/#59/#57 cluster actually lives), not a page-by-page E2E (that re-checks by-eye rendering). Playwright-style driving belongs on the DRIVE loop (emergent behavior), not setup pages. Net: 5 fixes ship with NO new test; the seam-test is deferred to when the #64/#59/#57 cluster is closed together.

## Codifications this session (block 2)
- `principles.md § Engineering rigor` — new "Test real logic, not ceremony" (no tautological change-detectors; a UI flow with no logic core → test the seam, not the pages).
