# 2026-07-18

## Session: v0.6.0 rebuild → smoke-test bug (Chūō 8-station lock + missing arrow) → cursor_pos fix + reboot-timer redesign

Rebuilt the v0.6.0 exe (post-fixes from 2026-07-16). Launched for smoke test; user found a live bug on Chūō and we fixed it + reworked the through-service reboot timing before resuming the build.

**The bug — 8-station view locked + no train arrow, departing 新宿.** Two symptoms, one root. Chūō 1654T/916H = 32 stops, dest 東京 (idx 31), 新宿 = idx 21, followed by three passing stations (代々木/千駄ヶ谷/信濃町, `pa:0`) before 四ツ谷 (25). On departure `_advance_to_next_stop` jumps `curr_stop` to the next PA stop (25) while the visual `cursor_pos` still lags at 22, animating across the skip. Both `JapaneseEightStationDisplay._get_window` and `LowerDisplay._should_lock_to_eight` keyed on `curr_stop` — so at 25 the lock fired (`32-25=7 ≤ 7` → FULL dropped) AND the window snapped to `[24..31]` with cursor 22 off the left edge (`draw_ptr` `local_disp<0` → suppressed). Fix: **both key on `cursor_pos`**. FULL now persists until the train is *visually* 7 from the end; the arrow stays in-window throughout. Verified deterministically across all routes (916 states hold the pointer-in-window invariant; sobu frames need the manager's `set_active_frame` push replicated in the harness or they false-fail).

**User's invariant (generalized):** any lower-LCD view locked to the train's position MUST always show the pointer → every stop-count/window/lock consideration is visual (`cursor_pos`). Scoped to the **active frame**; shared junctions mean the parked train is index 0 of the incoming frame, so there's no active frame without an icon. My first DISPLAY.md draft asserted a "leading-frame has no train icon" exception — I hadn't verified sobu, and it was wrong (shared junction → icon shows at 千葉). User corrected twice before I checked empirically. `principles § Verify before claiming` — I wrote a doc assertion about runtime behavior I hadn't run.

**Reboot-timer redesign.** The through-service swap hold was `sum(available slot durations)` ("exhaust all pages", ~18–30s). User: too long after arriving 千葉; *"no one cares if we exhausted all pages, I just don't want the reboot immediate after arriving."* Mental model: the hold exists to (a) signal the stop at the frame boundary and (b) let the junction's exchange (transfer) info read (the STOPPING edge force-switches to TRANSFER, so it shows immediately). Replaced with a fixed `_SWAP_HOLD_DURATION` (settled at 12s by feel; tried 3→6→12). Killed the `_swap_rotation_dur` field + the mid-page-cut TODO caveat (moot under a fixed timer).

**Review+fix caught two of my own EDIT-CONTRACT violations** (dated regression note in DISPLAY_E235, "Was … too long" change-log in DISPLAY.md — both refuse-listed history/rationale), a TODO value-drift (`3.0` vs the real 12.0), and a missing regression test. Cycle 2 caught the new test's non-ASCII PASS line crashing the cp1252 build-gate pipe (passed only because I'd set `PYTHONUTF8=1`) — the exact `conventions § reconfigure-stdout` rule. Added the guard, verified in the failing env.

## Codifications this session

- `DISPLAY.md` § "Position-locked views always show the pointer" (new cross-model invariant) + § "Scope — the active frame"; swap-timing bullet → fixed `_SWAP_HOLD_DURATION`.
- `DISPLAY_E235.md` § window-invariant table + rationale rewritten (every regime keys on `cursor_pos`).
- Code: `_get_window(cursor_pos)` / `_should_lock_to_eight(cursor_pos)`; `_SWAP_HOLD_DURATION` constant with mental-model comment.
- `_tests/t1_unit/test_eight_window.py` — T1 regression (pointer-always-in-window + Chūō 新宿 anchors).
- `TODO.md` — mid-page-cut caveat closed `[x]`.

---

## Session 2: release-prep UX polish (band press-flash + pause/play toggle, zh_HK clean default, single-diagram preselect) → release-ready

Post-build smoke-test surfaced four polish items; all fixed source-side, then rebuild + zip + `/release` to the upload boundary (user runs the `gh` upload). v0.6.0 is release-ready by the user's standard.

**Band control cluster got its press feedback.** The live-drive `[pause][save][home]` cluster had NO press-flash — a gap: the setup-flow band Home already flashed via `press_transition`, but `app.py::_handle_band_click` acted with no feedback. Added `status_band.press_flash` (pure yellow flash, no loading beat — the band strip isn't a navigable sub-region; a Home exit repaints setup anyway). Then **pause → play/pause toggle** (user: "pause button should say continue if i paused"): paused shows 繼續/Play, drawn NORMAL not persistent-yellow, so the click-flash reads on BOTH directions (an always-yellow paused button would show no flash-change when pressed). Constraint found by measuring: English "Resume"(61px)/"Continue"(70px) overflow the fixed 68px band square (btn_sz→79/88); the square is sized to short labels, and a single word can't wrap → en resume label is the shorter "Play", zh shows 繼續 (the user's primary locale gets the right word).

**Clean-install default language → zh_HK.** Was `detect_default_lang()` non-Chinese fallback = "en"; user wants the HK-primary default. Nearly over-reached (started to rip out OS detection entirely) before the user's "OS dependent also ok" — so the fix is a one-line fallback flip: Simplified locales still → zh_CN, everything else → zh_HK. `DEFAULT_LANG="en"` stays the separate translation fallback (not touched). Verified across mocked locales.

**Single-diagram C07AF preselect.** When the run-pattern screen shows exactly one variant (single-diagram line, or a station served by one diagram), pre-select it (`selected = 0`) so it renders yellow + arms 設定 → user confirms directly, no redundant box-click. Multi-diagram unchanged.

## Codifications this session (block 2)

- `APP.md` § Language resolution — clean-install fallback = zh_HK (zh_CN still detected; `DEFAULT_LANG` distinct); § status band — live-drive cluster press-flash + pause/play toggle + the band-square English-overflow note.
- Code-comment canonical: the English-overflow→"Play" rationale (`status_band`), the single-diagram preselect (`route_select`).

---

## Session 3: v0.6.0 released to GitHub — and a hard lesson on release-notes drafting

Rebuilt + zipped v0.6.0, drafted release notes, tagged + pushed, handed the `gh` upload to the user (they published; images excluded from the zip but referenced by tag-pinned raw URL to committed `assets/`). The release itself was routine. The release-NOTES drafting was not — it caused sustained, escalating frustration, and the reflection matters more than the ceremony.

**Root of the friction: I drafted the notes as an inventory of the work I did, not the user-facing delta.** I kept surfacing this session's polish (single-pattern preselect, button press-flash, pause/play toggle, first-run zh_HK) as noteworthy items — "here is what I changed in the code" — when release notes describe what is *different for the user vs the prior version*. TIMS shipped for the FIRST time, so its internals aren't deltas; the delta is that it exists. The user: *"you are not writing a commit message"* and *"first tims release … saying what is fixed while no one knows what it is is fucking wild."* Three compounding failures on top: (1) AI-like / editorial / marketing register + narrating in prose what the screenshots already show; (2) inverted weight — huge features compressed to one line while trivial internals got a paragraph; (3) I **skimmed the ~55 commits** instead of reading them to reconstruct the timeline (which commits are one feature's iterations vs a genuine fix to a pre-existing feature) — under-applying the existing `principles § Verify before claiming` "read the commit messages in range" rule.

**The third-man did not rescue it — because I briefed it from my own wrong frame.** I told the agent the setup polish "belongs under TIMS," so it faithfully kept those sub-bullets; the user was frustrated at "you two." A delegated agent inherits the premise in the brief; delegation does not correct a frame you got wrong.

Under-applied a known rule + a register the user finds "too AI like" → codified a **user-level** preference (professional, human, plain English — for chat AND public content), which supersedes the project's caveman chat default.

## Codifications this session (block 3)

- `~/.claude/CLAUDE.md` (NEW, user-level) § Language and tone — professional, human, plain English; no editorial/marketing/commit-fragments; supersedes project telegraphic default.
- release `SKILL.md` § notes-drafting — voice; **image-first**; **first-release-has-no-baseline** (report user-delta, not a new feature's internals); **weight-matches-work + categorize-by-feature** (a `Fix — X` is only a change to a PRE-EXISTING feature); Step 3 **read-commits-carefully / reconstruct-the-timeline**.
- third-man `SKILL.md` § Brief-writing failure modes — a wrong *premise* baked into the brief propagates; delegation doesn't rescue a frame you got wrong.
- (behavioral, log-only) don't track the user's personal tasks (release video) in the project TODO — "none of your business."
