# 2026-05-08

## Session: principles.md cleanup workflow + /distill-rules sibling skill

User opened with "/distill-docs, any idea to shorten" on `principles.md`. Diagnosis: `/distill-docs` is scoped to domain docs only — rules corpus has no equivalent. The bloat patterns differed too: recurrence prose ("Recurred 2026-X-Y across N substrates..."), sibling cross-reference tails, connective tissue, manufactured compounds, nested sub-rules with own incidents wearing how-to-apply clothing.

**Structural finding:** the gap was a missing EDIT-CONTRACT on `principles.md` itself. The "Tighten before appending" principle existed, but explicitly scoped to domain docs only — exempting the rules corpus from itself. That exemption is the structural reason `principles.md` grew bloated despite the principle being IN it. Doc-hygiene rules need to apply to their own home file; the absence of self-application IS the bug.

User reframed two things mid-session that shaped the workflow:

1. **Examples shape.** Initial draft used one fully-traced incident per Why; user redirected to multiple terse one-liners (matching the user-level CLAUDE.md "If 'Output' works, don't say 'Distillation deliverables'" pattern). Broader pattern coverage > one deep trace. Caveat user added: "not all principles can match claim-vs-reality" — so the example shape is "claim-vs-reality where applicable, otherwise the user's pushback shape."

2. **"Don't mix discussion and implementation questions."** Original draft framed the rule as "Analytical confirmation ≠ execution authorization" (failure-mode label) with the recovery move ("after analytical Q answered, ask execution Q separately") as the rule. User reframed: it's not about handling the post-confirmation moment, it's about NOT BUNDLING in the first place. Renamed to the positive rule with user's vocabulary ("implementation questions vs discussion questions"). Smaller-but-important: the rule fires upstream of where my draft fired.

**Third-man caught a misclassification.** When I drafted the rewritten "Verify before claiming" entry, the third-man's zero-context read found that one how-to-apply bullet (about not importing adjacent assumptions like opacity / render order) was actually a `Ground reasoning in user's stated terms` rule — different trigger, different rule home. The bullet had been filed under `Verify before claiming` in the original, and my rewrite preserved the misfiling. Migration was confirmed by reading 2026-05-02.md: line 55 of that log explicitly notes the opacity-bullet incident was about "rendering-behavior reasoning vs pure-logic reasoning" — i.e. frame integrity, not factual verification. This pattern (how-to-apply bullets that fit a sibling rule better) is now in the EDIT-CONTRACT refuse-list.

**Reconstructed-examples failure recursion.** When drafting v2 example bullets, I wrote 2026-05-02 and 2026-05-03 example one-liners from memory rather than reading the daily logs. The third-man flagged that bullets 2 and 3 didn't show the claim-vs-reality gap. Reading the actual logs surfaced the real substrates (region-map "Debug" column misread; partial vs full pygame redraw; etc.) — the memory-reconstructed bullets were thinner. Failure shape: a "Verify before claiming" failure happening inside the rewrite of the verify-before-claiming rule itself. Codified as Rule 2 in `/distill-rules`: verify against daily logs, don't reconstruct.

**Workflow sequence:** discussed shape → demo on one entry → third-man validation → daily-log verification → write EDIT-CONTRACT first (gate) → batch through entries item-by-item under the gate. The EDIT-CONTRACT-first sequencing matters because the contract gates the trim pass itself, not just future drift.

User authorized doing half the entries first then putting down the contract. Did all of Collaboration (#1-#12, plus 5 promoted peers, 1 fold), plus 5 from Engineering rigor / Tooling / Data modeling. 10 of 27 entries already at contract shape, untouched.

Final structural decision: the cleanup workflow bundles into a sibling skill `/distill-rules` (not extending `/distill-docs`). The disciplines genuinely differ — domain docs describe stable code (restructuring risks doc/code drift; removal-only is right), rules corpus describes evolving judgment (restructuring IS the work). Two skills with one cross-reference is cleaner than one skill with mode flags.

## Codifications this session

- `principles.md` — EDIT-CONTRACT block added at top; Collaboration section trimmed (12 → 11 entries + 5 promoted peers + 1 fold = 16 final); 5 more entries trimmed in Engineering rigor / Tooling / Data modeling sections; "Tighten before appending" extended to cover rules corpus + mention `/distill-rules`.
- `.claude/skills/distill-rules/SKILL.md` — new sibling skill, scope = `principles.md` only, restructure + rephrase + promotion + folding allowed.
- `.claude/skills/distill-docs/SKILL.md` — scope-exclusion line updated to point at `/distill-rules` for `principles.md`.
