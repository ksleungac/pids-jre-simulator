# 2026-06-21

## Session: graduation merge (E235-0 + editor → master) + per-route train-model selection

**Graduation.** FF-merged `feat/calibration-editor` → master (b6aedb8) and pushed — E235-0 displays + the calibration editor are on master. Clean fast-forward (master had been merged into the branch earlier, so 0 behind). Push first failed on GitHub HTTPS auth; user re-authed and pushed. The graduation *plan* lives in `WIP_calibration_editor.md` (Merge plan section); Phase 1 (this merge) is done, **Phase 2 (editor-standard codification: P1 conventions, P2 skill, P5 finish-wiring e235_0, dissolve WIP) still pending** — that doc's "NOT yet executed" status is now stale for Phase 1.

**Why model selection happened this session.** Tracing "is E235-0 reachable in the app" found ZERO production wiring: `app.py` hardcoded `e235_1000`; preview reached `e235_0` only via a monkey-patch rebind. User: code on master first, then model selection — then we built the selection same session.

- **Graduated the dormant registry scaffolding.** `displays/train_models/__init__.py` + `displays/__init__.py` had `# NOTE: NOT called YET` blocks describing exactly this trigger (2nd model lands). Reworked into a record registry (`TRAIN_MODELS` namedtuples, `get_train_model`/`resolve_model_key`/`model_choices`); `app.py` takes a `model` constructor arg, resolves classes + window dims from it (removed the hardcoded import); preview migrated off the rebind onto the same arg (deleted `MODEL_PACKAGES`).
- **Static imports, not importlib.** Registry statically imports both model packages → PyInstaller sees them through `app→registry→packages`, so NO `--hidden-import` needed. This *refined* third-man's importlib+hidden-import suggestion (importlib would've needed the build edit; static sidesteps it).
- **`model` kept OUT of the route_loader closure** — it selects which display *classes* to construct (construction-time), not per-stop renderer data; it's a `PASimulator` arg sourced from the setup dropdown (seeded by route.json `model`, default `e235_1000`, yamanote `e235_0`). Single authority = the user's pick.
- Spawned `/third-man` before coding (structural, ~5 files) per the convention; it validated the shape and sharpened registry home + constructor-arg-over-rebind.

**Dropdown polish (user as visual judge, many rounds).** Per-route expanding-list dropdown; folded state is chrome-less so it blends into the row; big magenta-colorkey'd train front-view icons (`data/train_icons/{key}.png`); per-row hover mirrors the route-row hover; width derived from content; smaller coherent line 2.

**Font episode → the codified learning.** Went `i18n.font` → pinned HelveticaNeue ("check other 2 languages") → Frutiger ("think font") → back to `i18n.font` when user said "coherence of fonts in each i18n". WHY the reverts: a Latin-only label must cohere with the *same-row* locale-font text (the `1654T` diagram code in ShinGo/Noto), so the locale's chrome face wins over a pinned Latin face. Same insight drove deriving box width from the active font (ShinGo `E235-1000` ~30% wider) and advancing line 2 by line-1 ascent not line-height (Noto's 27px leading). Frame-miss (optimized cross-locale sameness twice) but evolving guidance, no friction loop.

**review+fix:** 1 cycle. Architectural-critical = train icons untracked-but-runtime-required (#2) → staged. Two warnings dismissed: reviewer claimed `transform.scale` is bilinear (→ colorkey fringe); verified it's nearest-neighbour (returns only exact source colors), so the magenta key stays clean and the docstring is correct. One info (per-frame box_w recompute) declined — cached, negligible.

## Codifications this session

- `conventions.md § Tooling` — "Multi-locale chrome adapts to per-locale font metrics" (locale chrome face for shared Latin labels; content-derived width; ascent-based stacked-line spacing).
