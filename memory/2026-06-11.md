# 2026-06-11

## Session: Serena MCP setup + Fable-reviewed cleanup of the through-service commit

Set up Serena (oraios) as a project-scoped MCP server, then ran a Fable-model code review over last session's committed through-service feature (`a5b4f1d`) and worked the findings.

### Serena setup
Registered via a hand-written `.mcp.json` (project scope, `uvx`-from-git launch, `--context claude-code`), gitignored as per-machine tooling. The CLI `claude mcp add`/`add-json` route both failed on this Windows host — PowerShell eats the `--` separator and the `.ps1`→`.exe` wrapper strips quotes from JSON. Writing `.mcp.json` directly is the reliable path (→ conventions.md). The JR logo being a flattened-bezier vector (nothing read at runtime) means Serena indexes the codebase fine without asset concerns.

### Fable review — caught more than the original Opus loop
The 06-10 Opus review+fix loop passed this code; Fable surfaced 6 real findings on it. Honest attribution: NOT pure model horsepower — I handed Fable a sharper prompt (named the suspect areas + fed it the 06-10 memory log), it reviewed the *final* coherent commit not the mid-flight dirty tree, and it actually *executed* (`validate_data`, `_flatten_svg_path`). Still a strong showing; Fable is viable for reviews. Real comparison would need the project's own blind-A/B.

### Pathology worth keeping
- **The continuity band-aid SHIPPED despite the 06-10 codification.** `a5b4f1d` fixed 8-station continuity the right way (global-vs-slice predicate) but left the full-route view with the `continuity[2]=1` draw-side force — the exact pattern principles.md § Surgical Changes records the user rejecting *that same day*. The rule didn't fire during authoring; Fable (fed the memory log) caught it. Fixed upstream: `_get_stops_list_disp` now sets the tail slot from one global-terminus comparison, symmetric with 8-station; orphaned `_frame_continues` removed. Lesson reinforced, not re-codified (already in principles.md).
- **`_select_frame` was a dead duplicate, not just dead.** Its only caller was an unreachable `idx is None` fallback (manager always forces the frame for swap-lag). And it byte-duplicated `_natural_frame`'s containment rule under a hand-written "keep in sync" comment. Dropped it → `_natural_frame` is the single frame-picker; the sync-burden is gone. (Serena `find_referencing_symbols` settled "is it dead" cleanly where grep would've buried it in doc hits.)

### Behavioral
Reflexively used grep/Read/Edit even after setting Serena up, until the user nudged twice. "Use it going forward" → codified the Serena-over-grep-for-semantic-questions preference.

## Codifications this session
- conventions.md § Tooling — Serena-over-grep for semantic code questions; `claude mcp add` PowerShell unreliability → write `.mcp.json` directly.
- TODO.md § Deferred review findings — logged the `holding_ahead` backward-jump-to-junction edge (Fable #1, deferred as preview-only / over-engineering to fix).
