# 2026-07-15

## Session: TIMS setup-redesign — button-interaction coherence pass + i18n font blocker + review

Opened with a 4-agent read-only survey (deferred-findings triage / i18n alignment / flow-trace / button-state audit). Deferred findings = all correctly parked (none shipped-reachable). Flow = launch-safe. The real work was **coherence**, scoped by the user to the setup redesign.

**Why the incoherence existed (the frame that matters):** the `setup_tims` screens were authored one-at-a-time as separate drafts, each wiring its own buttons — so *interaction* behavior never had a central contract. The style-warehouse (`tims_chrome.py`, 2026-07-06) centralized the VISUAL vocabulary (palette/presets/fonts) but explicitly NOT interaction. This session graduates the interaction contract the same way: yellow=press-always, loading-beat=navigate-only, flash=actionable/current, silver=inactive. Silver had drifted into **four idioms** (lighter `_GRAY_T` / darker `_FOOT_T_GREY` / dark scrim / no-treatment) → centralized as the one `chrome.DISABLED` overlay.

**Selection-idiom rule (user's mental model, turned out already coherent):** steady-yellow selection when a separate confirm button (設定) carries the "proceed" flash; flash-selection when the click itself commits (no confirm on the page, e.g. model-select). No code change — just named it.

**OCR-settings 設定/返回:** the steppers page had no confirm affordance at all. User chose working-copy semantics: 設定 = save+return, 返回 = cancel/revert to the entry snapshot, only 設定 persists. Safe because `ocr_launch_extras()` re-`_load_state()`s from disk, so an abandoned edit can't reach a launch regardless.

**i18n blocker (non-obvious, packaged-only):** `home.run()` never synced `ACTIVE_LANG` from `i18n.current_lang()` — left at the module-default `zh_HK`. Chrome TEXT is right (via `i18n.t`→`_current_lang`) but chrome FONT keyed off the stale global → zh_CN rendered Simplified text in the TC face = tofu, en got the wrong face. Invisible in dev/preview (both set the global themselves); only the packaged `main.py` entry regressed. Fossil of the zh_HK-hardcoded-draft era. One-line fix at flow root, verified the SC face resolves.

**Audit-blindspot incident:** the button-state audit caught the dead-press + missing-beat (its own shortlist A.1/A.4) but MISSED "no set button." A state-inventory ("does each button have the right state") structurally cannot see an *absent* affordance — absence isn't a state. User flagged it ("how come your scan didn't find that"); the honest answer is state-audit ≠ completeness-audit. (User is drafting a middle-ground review skill for this class on a separate PC.)

**Review+fix (self-run, announced):** dropped stepper clamp → restored (guard + clamp; hand-edited misaligned `settings.json` was the failure path, and the user literally had `settings.json` open). Classic `tutorial.py` still used the dark scrim — aligned to silver so "silver everywhere" is literally true (same `draw_tims_button` primitive, so it was a real violation not a separate visual language).

## Codifications this session

- conventions.md § UI code style — TIMS button interaction model + selection idiom; disabled-silver bullet now names `chrome.DISABLED`; style-warehouse vocabulary gains `DISABLED`.
- conventions.md i18n cluster — per-module UI-locale global must sync from `i18n.current_lang()` at flow entry.
- principles.md § Collaboration — announce self-launched multi-step processes by name before running.
- TODO.md — `ACTIVE_LANG` initial-load fix logged (full 114-site unify still deferred); new housekeeping item for non-button no-press-feedback affordances.

## Session (2nd block): post-compaction cleanup arc — dead-code sweep + APP.md + README assets

Continued the setup wrap into cleanup. **Deferred-triage:** items 1 (button-primitive blockers) + 4 (no-press-feedback) closed — 1's blockers were all resolved by the `widgets.py` graduation (verified against source, not assumed); 4 exempted as out-of-scope (non-buttons with intrinsic result feedback aren't in the bevel-button interaction model), codified so they stop being re-flagged.

**Workspace dead-code sweep** (user: "many modules not used now?"). Built an import-graph audit (scratchpad) FIRST — because most zero-importer files are legit `uv run` dev tools / entry points, NOT dead; the audit separates real orphans from those. Real dead code was the graduation residue: the retired OCR debug panel (`draw_debug_panel` + `handle_panel_click` + their panel-only helper cluster, −381 lines from `driver.py`; the report path kept), the green-tutorial-shell draft, 11 locked-font eval scripts. Discipline that mattered: a "retained for X" NOTE goes stale when X is ALSO removed — sweep the whole cluster, not the head; and do the deletion surgery in the main thread with per-symbol caller verification, not a blind subagent apply (the strip came back /review+fix-clean). Fonts: nothing obsolete (Ark evals were gitignored, already gone).

**APP.md born** (user: "annoyed by no place for app-level specs"). The setup flow was the one un-homed domain — the state machine already lives in DISPLAY.md, the mental model in CLAUDE.md — so a focused-but-broad APP.md fills that gap without duplicating. Dissolved `WIP_setup_redesign.md` into it (structural content only; font/chrome/interaction rules stayed in conventions as cross-refs; logs + graduation plan dropped), repointed 7 cross-refs, registered in CLAUDE.md + the placement table.

**README assets:** the ready-PA + diagram-choice states the preview CLI can't reach → committed `_dev_scripts/gen_readme_tims_pages.py` so every asset in `assets/README.md` stays reproducible (the manifest's contract). Promoted the E235-0 Yamanote 5-station (東京, transfer-dense) + 2 TIMS pages to `assets/07-09`, wired into all 3 READMEs (classic 06 dropped from the grid).

**Self-obs:** stamped 2 edits "2026-07-16" before running `date` — it was still 2026-07-15 (wrongly assumed the post-compaction restart meant a new day). Corrected. The global run-`date`-first rule exists; apply it at session start, ESPECIALLY after a compaction/restart.

## Codifications this session (2nd block)

- conventions.md § UI code style — non-button exemption clause (interaction model governs bevel buttons only).
- APP.md — new canonical doc; registered in CLAUDE.md "When Working On" + session-recap placement table.
- assets/README.md — `NN-kebab-descriptor.png` naming + reproducible-regen note.
- readme SKILL.md — "5-station view" terminology row.
- TODO.md — items 1/4 closed; README-refresh item added; stale `draw_debug_panel` orphan note removed (strip done).
