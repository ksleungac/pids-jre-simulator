# 2026-06-16

## Session: E235-0 5-station inline transfer panel — algorithm stabilization + docs + review

Built and stabilized the inline transfer panel (left column of the E235-0 5-station view) — a *second* transfer surface distinct from the inherited horizontal TRANSFER slot. The algorithm was designed against a curated IRL-vs-algorithm set (same stabilization method as the horizontal slot's Rules 1-4): threshold-T pair-or-stack (N≥6 pairs, else all solo — overfit to Yamanote, whose effective transfer counts are 1,2,3,6,7,8,9, never 4/5, making the 6-cutoff unambiguous), a shared col-2 anchor so 2nd-element badges align, a curved right edge through 3 draggable control points (band sweeps right as it descends → lower rows wider), and a greedy top-down pair + monotone repair loop. Shinkansen are always solo and wrap at ･ against a *narrower fixed* boundary decoupled from the row-push curve, so the cut stays at 北海道｜上越 even when the curve widens for pairing.

**The session's real lesson (friction → codified):** I misread the Yamanote shinkansen wrap off the reference PNG twice (3+3 vs the IRL 4+2), and whiplashed on bracket widths, before resolving it with `_dev_scripts/_sim_panel_layout.py` — a headless replication that reads the live tuneables and mirrors `_build`+repair, emitting exact per-station groupings as text. Reading a compressed/photographed reference for *precise structural facts* (counts, segment cuts, alignment) is unreliable; the PNG is for the human visual judge, the algorithm is for precise facts. The sim later doubled as the doc's regression-corpus generator (12/12 vs the user's IRL set).

Data fidelity: transfer `name_ja` punctuation is per-line IRL, not uniform — 品川's keihin uses full-width brackets （）/dot ・ (IRL-special, don't normalize in a consistency pass), other suffixes use half-width (), shinkansen names use half-width ･ (U+FF65). 上野 dropped UT + gained Keisei; 東京's drop-set reduced. The DATA_FORMAT.md lines.json example was stale (showed half-width) — corrected to match the live data.

`/review+fix` (this session's docs + panel code) caught one ship-blocker: `fonts/ShinGoPr6N-Light.otf` (the panel subtitle face) was loaded at runtime but untracked — staged. Also removed a dead `right_edge` param left over from decoupling the shinkansen wrap from the curve. Smoke-tested via the 30-stop montage (real renderer, no crash).

## Codifications this session

- conventions.md § Tooling — extended the "don't Read preview PNGs back" rule: never extract precise structural facts (counts/cuts/alignment) from a reference photo; build a faithful headless replication and read its text output (the `_sim_panel_layout.py` pattern).
- DISPLAY_E235.md § "E235-0 — 5-station stopping view" — new section (band, markers, inline-panel algorithm, 12-station regression corpus) + stale-note fixes (5-station view shipped, EIGHT slot owner).
- DATA_FORMAT.md — punctuation-fidelity Notes bullet (full-width keihin IRL-special / half-width others / shinkansen ･); fixed stale half-width example.
- (WIP_calibration_editor.md three-state save + `[`/`]` toggle were already written earlier in the continuous session.)
