# 2026-05-09

## Session: Yamanote STA refinement complete + sta-make skill workflow upgrades

The last big un-refined STA route. 30 stops, all now meet the by-ear gate AND the route-level convention. Two skill upgrades emerged from user-named workflow gaps; both landed during the session, not deferred.

### The work

Pre-existing yamanote state: 29 sta files (JY24_OSK appears twice as loop terminus), pre-formalization splits with integer sta_cut values, never through `verify_sta_listen.py`. Recent skill flow targets ~0.5 s pre-voice silence per `sta_cut = round(max(music_end, voice_start − 0.5), 1)`.

The verifier yielded 24 PASS / 6 FAIL initially. The PASSED files retained their original sta_cut values; FAILed files all had a similar pathology — sta_cut placed mid-music with a discrete loud KAK transient sitting between music end and voice start, then voice content (often attacking at digital ceiling). Yamanote recordings are normalized hot (−6 to −10 dB music, voice peaks at 0 dB), so the saikyo-era detector tools bracketed the music itself instead of the KAK.

### Hostile-recording combined-pass pattern (new in sta-make/SKILL.md)

User flagged the workflow inefficiency mid-session: each per-file fix was being iterated in 2-3 ffmpeg passes (KAK splice → user verifies → sta_cut adjustment → user verifies → silence pad → user verifies). Each round needed manual re-verification. Their phrasing: *"why are you not doing that while fixing the KAK sound and sta_cut position? because skill didn't tell you? it saves my verification time."*

Fix: combine all three operations into a single ffmpeg filter graph. Cut music body before its sharp natural end (smooths music exit), insert artificial silence via `anullsrc` to meet ~0.3 s pre-voice convention (yamanote files have no natural silence gap wide enough), skip KAK + first voice burst with `atrim`, crossfade both junctions with `acrossfade`, set sta_cut at start of inserted silence. Section added under Step 12 with the template ffmpeg command.

The artificial-silence insertion specifically — `anullsrc=channel_layout=stereo:sample_rate=22050,atrim=duration=<silence_dur>` — is the new technique for hostile recordings where the natural file structure can't yield the convention's silence pad.

### Route-level gap alignment (new in sta-make/SKILL.md)

User caught me declaring yamanote done after fixing only the 6 FAILed files. Their question: *"did you also ensure all tracks shared the similar silence after sta_cut?"*

Audit revealed huge variance across the 29 files: 3 SHORT (<250 ms, JY03_AKB at 49 ms!), 12 in target band, 14 LONG (>400 ms, JY02_KND/JY25_SGW/JY27/JY28_HMC at 800-940 ms). The variance was audible — PageUp on station X felt snappier than station Y on the same line.

Fix: route.json edit only for LONG (set new_sta_cut = round(voice_attack − 0.30, 1)); insert artificial silence at sta_cut for SHORT (no splice junction needed if the file is otherwise clean). Section added under Step 12 with the audit script + both fix templates.

End state: all 29 files in 263–370 ms range, mean 307 ms, median 304 ms.

### Verifier UI fix

Sidebar list didn't scroll — at 30 stops the bottom 12 were unreachable. Added `list_scroll` state, MOUSEWHEEL handler, auto-scroll on keyboard nav, hit-test offset, and ▲/▼ scroll indicators. Lands in `_dev_scripts/verify_sta_listen.py:377-382` (state) + `:564-566` (wheel) + `:580-583` (auto-scroll in `jump_to`) + render-loop changes. Fix is general — applies to any future long route too.

### Discipline-lapse moment

The user's two workflow corrections (combine-fixes + route-level-audit) hit the same underlying pattern: I treat user-flagged failures as the scope of work rather than auditing the implicit class. SMB → fix SMB only. KAK → fix KAK transient only, not "convention compliance." This is similar in shape to "Implementation-completion-as-spec" but inverse direction — instead of auto-broadening when scope was narrow, here I was auto-narrowing when scope was implicit-route-wide.

Both corrections went straight into the sta-make skill rather than principles.md — the user attributed the gap to the skill not telling me, and the fixes are STA-workflow-specific. A higher-level principle ("audit-scope completeness") would risk being too generic without project anchoring.

### KAK vs voice-attack misidentification (mid-session)

Once during JY29_SMB analysis I called the loud post-cut transient a "voice attack" when the user knew it as a KAK transient (followed by a brief gap, THEN voice). The user corrected: *"not voice attack, it's the KAK."* I had defended the voice-attack interpretation based on amplitude alone; the user's identification came from listening + recognizing the IRL pattern (KAK = staff-cut machine click, separate from voice content). After re-examining the waveform with that frame, I could see the brief silence dip between KAK and voice. This is the kind of "user knows the domain better than the waveform shows" situation — defer to user identification when they're listening + I'm reading numbers.

## Codifications this session

- `.claude/skills/sta-make/SKILL.md` Step 12 — added "Hostile-recording pattern (yamanote-style) — combine fixes in ONE splice" subsection with combined ffmpeg template
- `.claude/skills/sta-make/SKILL.md` Step 12 — added "Route-level gap alignment — audit ALL stops, not just FAILs" subsection with audit script + LONG/SHORT fix templates
- `_dev_scripts/verify_sta_listen.py` — sidebar scroll support (state + wheel + auto-scroll + hit-test offset + indicators)
- `audio/yamanote/` — all 29 sta files refined; route.json sta_cut values aligned to ~0.3 s pre-voice convention

## Session: speed-limit OCR `100`→`110` misread fix + score-gated two-try splitter (PM)

User flagged in today's chuo recording: 23/129 speed_limit samples reading as `110` while IRL was `100`, all at score 0.65. Pattern bursty on a stable IRL value — frame-level OCR variance, not a steady classifier failure.

**Initial misdiagnosis.** Assumed argmin's `0+0` blob splits asymmetrically → produces grammar-valid wrong `110` → bypasses equal_width because the existing 2-try logic short-circuits on first grammar-valid result. Diagnosis ~half-correct: the failure category was right, but the specific argmin output on saved-PNG replay was `160` (grammar-fail → equal_width fallback → correct `100`), not `110`. User had screenshotted a misread frame; running the production reader on it returned `100` correctly. The discrepancy taught a new debugging fact (codified below).

**Saved-PNG vs live-BGRA divergence.** Same wall-clock-second screenshot replays differently than the live dxcam BGRA frame because of sub-pixel column-density shifts during PNG roundtrip. A live `100`→`110` misread can saved-PNG-replay to `100`→`160` (grammar-fail) — same root cause family, different argmin output. Means: capturing corpus via external screenshot tool is unreliable; need to dump the same numpy cell the OCR loop saw.

**Misread dump hook.** Wired into AutoDriver: when a grammar-valid speed_limit reads below 0.75, save `sl_cell` to `_ocr_calibration/_misread_dumps/sl_<ts_ms>_score<NN>_read<NNN>.png`. `ts_ms` matches `int(ts*1000)` from JSONL for cross-reference. User did one chuo drive, captured 31 dumps (19 misread `110`, 12 low-score `100`).

**Empirical band split was clean.** Across all 31 dumps: argmin ∈ {grammar-invalid `160` @ 0.66, valid `100` @ 0.66}; equal_width = `100` @ 0.91-0.92 universally. The 0.65-0.66 vs 0.92+ gap is large enough for a confident threshold. Picked 0.85 — comfortable margin both sides; calibrated against the band.

**Score-gated splitter.** Replaced first-valid-wins with: argmin trusted only when grammar-valid AND `score >= ARGMIN_TRUST_SCORE` (0.85); below that, equal_width runs as second opinion and its grammar-valid result wins. Preserves correct behavior on cleanly-segmenting cases (where argmin scores high) while repairing the limit_100 `0+0` mode.

**Mislabeled calibration file.** When verifying the new threshold against the corpus, `limit_110.png` "regressed" to read `100`. Diagnostic showed argmin and equal_width producing identical bboxes on this file as on user's "100_readas_110" screenshot — suspicion: the file actually contains a `100` cell. Confirmed visually: yes, `_ocr_calibration/limit_110.png` had been authored as a `100` source. This invalidated the original docstring's "equal_width breaks limit_110" warning (it was based on a mislabeled file). User caught + provided a real `110` capture; replaced the file; verified both strategies converge on `110` cleanly (no merged blob) at 0.857. Threshold edge case — sub-pixel variance could dip live `110` reads below 0.85, but equal_width then agrees, so safe.

**Side: KNOWN_LIMIT_VALUES dict cleanup.** Closed a deferred-findings entry: normalized three intermixed naming conventions (`speed_limit_75`, `limit 65` with space, `limit_30` short prefix) to consistent `limit_<N>`. Added `limit_100` to the dict — extractor warns + skips on it (no equal_width fallback in extractor; argmin alone can't split `0+0`), but registers the canonical fallback case for any future extractor enhancement.

**Review-fix cycle 1.** Reviewer caught: (a) `_crop_cell`'s docstring forbids "pygame state from background thread" but the new dump helper uses `pygame.image.save` from the AutoDriver thread — clarified to scope the rule to display state only (image-encode is display-independent); (b) my TODO closure note inaccurately said `speed_limit_75` was "left as-is" when it was actually renamed — corrected per Verify-before-claiming. Two findings deferred (`_try_read_speed_limit`'s `| None` return type is dead; `MISREAD_DUMP_DIR` placement under sources-named folder).

## Codifications this session (PM)

- `ocr.py` — `read_speed_limit` rewritten with score-gated two-try splitter; new `ARGMIN_TRUST_SCORE = 0.85` constant; docstring rewrites with empirical evidence (real limit_110 + 31-frame corpus).
- `auto_input.py` — `SUSPICIOUS_SPEED_LIMIT_SCORE = 0.75` constant + `MISREAD_DUMP_DIR` + `_dump_misread_speed_limit_cell` helper + call site after `read_speed_limit`. `_crop_cell` docstring scope-clarified per cycle 1 review.
- `_dev_scripts/extract_ocr_assets.py` — `KNOWN_LIMIT_VALUES` normalized to `limit_<N>`; `limit_100` added.
- `AUTO_INPUT.md § Speed limit` — score-gated splitter description (replaces stale "first-valid-wins"); new "Debugging misreads" paragraph with PNG-roundtrip caveat + dump hook reference.
- `_ocr_calibration/limit_110.png` — replaced (was mislabeled-actually-`100`; user provided real `110`).
- `ocr_templates/digits_red/{1,5}.png` — re-extracted (digit `1` from real `limit_110`; digit `5` from `limit_35` due to dict reorder).
- `TODO.md` — `KNOWN_LIMIT_VALUES filename inconsistency` closed; 2 new deferred review findings added.

## Session: chuo arrival-fire fix + Yokohama at-station bug + debug-panel redesign + Layer 3 rename (PM cont)

Long PM continuation. Three real bugs hunted-and-fixed plus a major panel UX overhaul plus a state-machine vocabulary rename.

### Chuo Nakano→Shinjuku arrival PA never auto-fired (bug-1)

User reported: latest chuo drive showed approach to Shinjuku with no まもなく PA, train stopped, only after manual press did the PIDS catch up. Traced via the JSONL: detector emitted FIRE_ARRIVAL correctly at dist=852m, but `_fire_arrival`'s upper-bound gate `curr > segment_start_stop + 1` skipped it because curr_stop legitimately jumped 18→21 (Nakano → Shinjuku skipping 東中野/大久保 passing stations). Same gate at `_fire_at_station`. Bug in the original auto-input code since day 1 (commit 65d0cd3) — never surfaced because Yamanote (heavy-test line) has no passing stations. Chuo's dense passing-station gaps (Nakano→Shinjuku has 2, Shinjuku→Yotsuya has 3, Yotsuya→Ochanomizu has 3) was the first time this code path ran in anger.

Fix: introduced `_expected_next_stop()` helper that walks forward from `segment_start_stop` to find the next stopping-station (predicate = `time is not None`, the canonical DATA_FORMAT discriminator). Both `_fire_arrival` and `_fire_at_station` now compare against `expected` instead of `+1`. Verified on a follow-up chuo recording — arrivals fire correctly across Shinjuku, Yotsuya, Ochanomizu segments.

### Yokohama at-station press silently dropped (bug-2)

User reported: Tokaido drive overran Yokohama, train stopped past the platform marker, but LCD stayed stuck on まもなく 横浜 — auto-fire never converted to STOPPING. Manual press one extra time fixed it. Initial JSONL didn't have `at_station` logged so first-pass debugging hit a wall.

Added 3 new fields to JSONL sample logging: `at_station` (sim app state), `cnt_pa_at_station`, `at_station_observed` (detector flag). User then re-ran: at_station_observed flipped True correctly, but sim's at_station took ~10 seconds to flip True after.

Root cause (user's hypothesis was right): `_next_pa()` short-circuits when `audio.is_pa_playing()` is True. Input handler reset `pending_next_pa = False` BEFORE calling _next_pa. When auto-fire fired during the long まもなく PA playback, the flag was reset, _next_pa no-op'd, signal lost. By the time audio finished, no pending flag remained and detector's `at_station_observed` was already True so it wouldn't re-emit.

Manual press doesn't surface the bug because held-key keeps re-asserting every frame. Auto-fire is single-shot; one drop is forever.

Fix: gate consumption on `not self.audio.is_pa_playing()` — pending_next_pa stays True across audio-playing frames; consumed only when action's preconditions are met. Verified on next Yokohama drive: at_st flipped True ~10s after MOVING→STOPPED transition, exactly matching the audio-finish latency. The 10s lag is structurally correct (you don't WANT to interrupt a still-playing PA mid-sentence).

This generalized into a critical_lessons.md entry: "Single-shot signal flags must be consumed AFTER successful action, not before." Pattern recurs anywhere a producer sets a one-shot pending flag and the consumer's action has internal preconditions that may decline — applies to any future `pending_X` flags added for STA, jump, resync, etc.

### Debug-panel redesign (long iterative)

Multi-round visual iteration on the auto-input debug panel from "developer-y, jargon-laden, geometric mess" toward something approaching ship-quality. Driven by user feedback in iteration cycles, each round captured in a `preview_panel.png` render the user could eyeball.

**New preview script** (`preview_debug_panel.py`): standalone pygame window renders the panel with mock status dicts covering boot / stopped / mid-approach / low-confidence / paused / just-arrived / final-approach. Lets us iterate visuals without the game running. Used to drive each round's PNG snapshot.

**Major rounds:**
- **Pause button**: added (top-right pill initially, then top-left square per user). Toggles `AutoDriver.paused`; loop skips capture+OCR while paused, panel header tints orange + button shows play-icon "Resume". Wired through `sim.auto_driver` reference (new attribute on `PASimulator`).
- **Header rename**: `[AUTO-INPUT]` → `Game Sync`. User flagged "Auto Drive" earlier as ambiguous (implies the program drives the train); "Game Sync" describes the relationship (panel synced to game) without control-direction implication.
- **Button shape evolution**: rounded-square (22px) → bigger square (32 → 64 → 72) with icon+text → finally horizontal pills with icon-left + text-right. Pills auto-fit label width — solved the "Save Speed Curve" overflow that all square sizes failed at without going huge. Lines 2-3 reclaim full panel width below pills (32px tall) instead of squeezing past tall buttons.
- **Save button label**: "Report ↓" → "Save Report" (icon+text) → finally "Save Speed Curve" — user feedback that "Report" was unclear (only the author knew what report).
- **Line 2 (OCR readouts)**: split distance and stopping-position into independent fields (was cell-content-priority showing only one). Stopping position only renders when reading present, with brightness-pulse flash via `pygame.time.get_ticks()` to draw attention to the brief post-arrival window.
- **Line 3 (state)**: collapsed from `app: X · game: Y · PA: N` (two parallel labels) to single phrase `<location/segment> · <state-word> · Played: N/M`. State-word from new Layer 3 vocabulary. Played-count bug fixed: cnt_pa is 0-indexed post-play (=0 means pa[0] just played), so display `cnt_pa+1/total` is the correct 1-indexed user-facing count.
- **Auto-fire chip**: green dot + "Auto-fired: <type>" appears for 3 seconds after each successful auto-fire, then fades. AutoDriver tracks `_last_fire = {ts, type}`; status dict carries it; panel renders when `time.time() - ts < 3`. User wanted visibility into when auto-actions actually happen.
- **Observation flags row dropped**: line 4 of the older 4-line layout had `departure ✓ arrival ✓ at-station ·`. Removed entirely — these are detector-internal trigger flags that the Layer 3 state word already implies (e.g. "Approaching" means departure was observed). They're still in JSONL log for debugging.

Panel height: 80 → 100 → back to 80 once line 4 was dropped. End state: 3 content lines + horizontal pill button strip on top.

### Layer 3 wire-name rename

User flagged the Layer 3 enum names (STOPPING_FRESH / STOPPING_AFTER_ARR / APPROACHING_BEFORE_DEP / APPROACHING_AFTER_DEP / MOVING_AFTER_ARR) as confusing — the BEFORE_DEP / AFTER_DEP / AFTER_ARR suffixes leaked the detector's trigger-fire shape into the state name. They describe "which observation flag has flipped" rather than what the train is doing.

Renamed to plain transit verbs: IDLE / STOPPED / DEPARTING / CRUISING / ARRIVING. Wire and panel labels now line up 1:1 (no more title-case translation needed — `_LAYER3_HUMAN` becomes near-identity, kept as a hook for future i18n).

Side note discovered during /review+fix: the new vocabulary creates a Layer 2 ↔ Layer 3 token collision on `STOPPED` (both a badge value and a Layer 3 state). AUTO_INPUT.md now documents this honestly — the collision is intentional (badge is one input to Layer 3 inference; at platform with arrival_observed=True they collapse semantically), disambiguate by field name (`status['badge']` vs `status['inferred_state']`).

### MOVING_AFTER_ARR mislabeled "Overrun"

During Layer 3 panel-label work I named MOVING_AFTER_ARR "Overrun" thinking it meant the train passed without stopping. User caught this on chuo drive — state was firing during NORMAL final approach (dist <900m, decelerating). Renamed to "Final approach" (pre-rename) → "ARRIVING" (post-rename).

Real overrun is rare and would cycle the segment (STOPPED→MOVING transitions reset detector flags) — MOVING_AFTER_ARR / ARRIVING is overwhelmingly the normal "in the deceleration window" phase.

### Game STOPPED-badge invariant codified

User clarified during the bug-2 hunt: "case 1 [transient STOPPED flicker] is not possible since game will never show STOPPED unless you stopped in range or resets you." Worth codifying — affects detector design (no debouncing or N-frame consensus needed for STOPPED reads). Added to AUTO_INPUT.md § Badge classification.

## Codifications this session (PM cont)

- `app.py` — audio-busy gate fix in `_handle_input_real` (gate consumption on `not self.audio.is_pa_playing()` so pending_next_pa survives across audio-playing frames).
- `auto_input.py` — Layer3State rename + `_LAYER3_HUMAN` map + `inferred_state()` returns. Major panel redesign (pill buttons, fire chip, line layout, distance/stopping-position split, plain-English state phrase). New JSONL fields: at_station, cnt_pa_at_station, at_station_observed. `_expected_next_stop` helper for arrival-fire across passing-station gaps. `_last_fire` tracking on AutoDriver.
- `app.py` — `sim.auto_driver: Optional[Any]` attribute declaration (so click handler can reach driver).
- `main.py` — `sim.auto_driver = driver` wiring after AutoDriver construction.
- `preview_debug_panel.py` — new file; standalone pygame preview of debug panel with mock status scenarios.
- `AUTO_INPUT.md` — Layer 3 truth table updated (panel-label column added, plain English values), Status-dict schema rewritten with all 18 fields, JSONL sample doc extended with new fields, "Cross-layer token sharing" honest description added (Layer 2 ↔ Layer 3 share STOPPED), Badge classification gained STOPPED-canonical-not-transient invariant.
- `WIP_autodriver.md` — Layer 3 references migrated to new vocabulary in silent-advance table + resolved-Qs + smoke-test hint.
- `.claude/rules/conventions.md` — Layer 3 vocabulary binding renamed (with rename-history annotation 2026-05-09).
- `.claude/rules/critical_lessons.md` — new entry "Single-shot signal flags must be consumed AFTER successful action, not before" (pending_next_pa pathology).
- `TODO.md` — 3 new deferred review findings under "From 2026-05-09 PM" subsection (`_LAYER3_HUMAN` near-identity, silent click-absorb, pause-loop resume latency).
