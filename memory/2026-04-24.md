# 2026-04-24

## Two bugs + a preview overhaul

Two LowerDisplay bugs fixed and the preview harness rebuilt to share code with the main app.

### Bug 1 — Global vs local index confusion in the lower LCD

Inner gold dot (current-station marker) drifted to the wrong column after the long-route final-window scroll kicked in (Keihin/Chuo at ~12+ stops in). Root cause: `draw_marks` compared `i == self.state.curr_stop` where `i` was a LOCAL index into the visible slice but `curr_stop` was a GLOBAL index into `self.stops`. While the window started at stop 0 (short routes, or long routes before scroll) the two spaces coincided and nothing broke. After scroll the slice shifted — the dot landed `stop_offset` columns ahead of the train.

**Why the fix is structural, not a patch**: threading `stop_offset` into every draw method would have left the same footgun — the next draw method someone adds can silently conflate frames again. Collapsed the accidental axis entirely:
- `_get_stops_list_disp()` returns `list[(global_idx, stop)]` tuples. Every visible cell carries its global index.
- `curr_stop_disp` redefined to live in global frame (it already was in the short-route path; only the long-route refresh had remapped it to local via the now-deleted `_refresh_curr_stop_disp`).
- All draw methods iterate `(gi, stop)` and compare state in global frame. Local index appears only in pixel-column math (`local_i = gi - window_start`) and can't escape into a comparison.

Docs updated in CLAUDE.md Key Feature #8 and notes.md "Long-Route Window Refresh".

### Bug 2 — Single-PA skip target leaks skip state forever

After fix #1 the user found that, on Keihin, departing 上野 produced correct visuals for one frame, then a SECOND PageDown left the red pointer at 秋葉原 but the gold inner dot at 神田 (one ahead). Cause: the Phase-2 skip-completion branch in `increment_current_stop_display` (`if skip > 0 and cnt_pa >= 1`) assumes the skip target has 2+ PA tracks so `cnt_pa` passes through 1 on its way up. But 秋葉原 has `pa: ["1"]` (single track) — the first PageDown at 秋葉原 already satisfies `cnt_pa >= len - 1` and jumps straight to the "advance to next stop" branch. Phase 2 never fires. `skip`/`skip_progress`/`time_to_next` leak into the next transition, permanently stranding `curr_stop_disp` one station behind `curr_stop`.

**Fix**: the `cnt_pa == 0` branch now unconditionally flushes any pending skip at its top before the regular advance. Clears `skip`/`skip_progress`/`time_to_next`, catches `curr_stop_disp` up by whatever was left of the skip. Multi-PA targets still hit Phase 2 normally (skip was already 0 by the time cnt_pa reaches 1 in the new code path, so Phase 2 is a no-op there).

**User's diagnostic framing was on the money**: "maybe wrong for when there's only 1 pa at the next station" — exactly. Worth preserving as a pattern: when state transitions hinge on a counter reaching a specific value, always check whether that value can be skipped over.

### Preview refactor — `PASimulator(preview=True)` instead of duplicated state machine

Original `preview_upper_lcd.py` had its own `advance_pa` that shadowed `_next_pa`. Initially the user OK'd this as acceptable drift risk (option 1 in the earlier discussion). On reflection they reversed — "preview should be like main program as much as possible". Right call: the preview has already caught two real logic bugs, so drift between preview and main would be expensive.

Design landed on flag-in-main-class:
- `PASimulator.__init__(preview=False)` — silent audio, skip mixer+win32gui, pygame-event input. `run()` stays identical; only `_handle_input` dispatches.
- Preview-only extras live on `PASimulator` too (`jump_to_stop`, `_cycle_display_mode`) — reasoning: `jump_to_stop` is likely to become a real app feature later per the user's own comment.
- `preview_display.py` is now ~110 lines, pure CLI plumbing + screenshot mode. Was ~280 before.

Trade-off accepted: mild SRP smell from `_handle_input_main` / `_handle_input_preview` living on the same class. Not worth a subclass.

### Mock route as real file, not in-code constants

`audio/mock/main/route.json` — curated 10-stop edge-case catalog. Loads via the same path as real routes, user can edit freely. After feedback, ended up covering both skip-target PA-count variants:
- 神田 → 秋葉原 (passing) → 御徒町 `pa:["1"]` — **1-skip to SINGLE-PA target** (bug #2 repro)
- 上野 → 日暮里, 西日暮里 (passing) → 田端 `pa:["1","2"]` — **2-skip to MULTI-PA target** (happy path)
- Plus: code_3 present (7) vs absent (3), PA-track counts 0/1/2/3, long-name wrap, compound destination.

Pattern worth reusing: when a future feature has edge-case sensitivity, add the scenario to `audio/mock/main/route.json` rather than writing ad-hoc test fixtures. The mock IS the test suite.

### Debugging workflow that emerged

Throwaway `_bug_repro_*.py` at project root: stands up `PASimulator(preview=True)`, jumps to the interesting state, programmatically drives `_next_pa()` to reach the failure state, prints/screenshots. Delete after. Fast loop, doesn't need a reviewer subagent. Used for both bugs this session.

### Preferences baked this session

- **PowerShell tool, not Bash, on this machine — always.** User stopped me twice to redirect. Saved to user-level auto-memory (`feedback_use_powershell.md`). Bash tool dispatch has ~40s latency on this Windows setup (root cause already documented in `env_claude_code_bash_slowness.md`).
- **`jump_to_stop` defaults to backward rounding** so preview always lands on pre-skip states; testing skip logic by pressing PageDown is the intended workflow.

### Deferred

User flagged the `curr_stop` vs `curr_stop_disp` semantic cleanup for a later discussion. The names are confusing enough that it came up explicitly during bug #1 analysis. Current session just unified the frame (both global); naming is still an open question.

### Files touched

- `display.py` — bug #1 refactor (global-frame rendering) + bug #2 skip flush
- `app.py` — `PASimulator(preview=True)` flag, `_SilentAudio`, `_handle_input_preview`, `jump_to_stop`, `_cycle_display_mode`
- `preview_display.py` — rewrote as thin entry point
- `preview_upper_lcd.py` — DELETED (superseded)
- `audio/mock/main/route.json` — NEW, 10-stop test catalog
- `CLAUDE.md` — file structure + Key Feature #8 (state indices) + preview pointer in "When Working On..."
- `.claude/rules/notes.md` — Station Skip Logic rewrite, Long-Route Window Refresh section, Preview Mode section
- `.claude/rules/preferences.md` — filename rename only
- `.claude/skills/visual-adjust/SKILL.md` + `UPPER_DISPLAY_UPDATE.md` — filename rename only

---

## Cursor refactor + edge-case fix (later same day)

The "deferred `curr_stop` vs `curr_stop_disp` cleanup" from above is no longer deferred — picked it up the same day after the user said "ok try try."

### What collapsed: `curr_stop_disp` is gone

The two-index split was **temporal** (PA target vs animation cursor), not spatial — they only diverge during the few seconds of skip animation. So `curr_stop_disp` was never independent state, just a denormalized cache of "where the cursor currently is" that had to be kept in sync via four separate mutation paths (Phase 1 `+=1`, time tick `+=1`, Phase 2 catch-up, single-PA flush). Each path was a place to forget something — exactly how bug #2 happened.

Replaced by a derived property on `AppState`:

```python
@property
def cursor_pos(self) -> int:
    return self.curr_stop - max(0, self.skip - self.skip_progress)
```

`skip` and `skip_progress` (already existed) fully describe the animation. cursor_pos = curr_stop when no skip is in flight; lags behind during skip; auto-catches up when skip state is zeroed.

### What fell out

- `LowerDisplay.increment_current_stop_display()` **deleted entirely** — its skip-setup logic moved to `app._next_pa`'s "advance" branch (computes `skip = curr_stop - prev_stop - 1` from the gap *after* the passing-station while-loop, not before). Its skip-catchup logic became one line in the "next PA within current stop" branch: `skip = skip_progress = time_to_next = 0`. Cursor snaps to curr_stop via the property.
- The "single-PA flush" hack from bug #2 disappears. The "advance" branch *overwrites* skip from the gap (not appends), and the "within stop" branch unconditionally zeros it. Leak class is structurally impossible — there's no dedicated "flush" path to remember.
- `_update_skip_progress` stops mutating a stored disp. Only does `skip_progress = i` at the time threshold. Cursor walks forward via the derivation.
- `jump_to_stop` dropped one line (`curr_stop_disp = target`). Already zeros skip state.

### Edge case exposed by the refactor (caught by review cycle 1)

`cursor_pos` can fall **outside the visible window** when a 2+ station skip's trailing edge sits in the long-route cut-off zone. Concretely: Keihin (46 stops, window_start=18 when refresh active), if curr_stop = 19 with skip = 2, then cursor_pos = 17 < window_start = 18. `local_disp = cursor_pos - window_start = -1`, and Python's modulo wraps to `per_line - 1`, painting the pointer at the wrong far-right column.

Pre-refactor this couldn't happen: `curr_stop_disp` was incremented one step at a time and `_refresh_curr_stop_disp` clamped it on the flip frame, so it was always within the window.

**Fix**: early-return in `draw_ptr` when `local_disp < 0 or >= len(f_stops)`. Pointer is suppressed; inner red dot at curr_stop still anchors position. Cursor reappears as `skip_progress` ticks bring `cursor_pos` back into the window.

`draw_marks` and `draw_times` are safe by construction — they iterate `for gi, stop in f_stops` so `gi >= window_start` always (no negative `local_i` possible).

### Mock route data hygiene

User asked to fix passing-station data in mock to match real-route convention:
- Real routes (verified against keihin/1275A): passing stations have NO `time` field. The next PA station's `time` covers the full leg including any passing stations en route.
- Removed `time` from 秋葉原, 日暮里, 西日暮里 in mock route.
- Bumped post-skip PA times to preserve the trip-length feel: 御徒町 4→6 (absorbs 秋葉原's old 2), 田端 6→10 (absorbs 日暮里 + 西日暮里's 2+2).

### Insight worth preserving

When you have two pieces of state that are *almost always equal*, the question to ask is: "is the second one stored, or derived?" If the divergence comes from a small, named transient (here: `skip_progress` ticking through a known range), the second piece is almost always derived state — and storing it creates the bug class. Bug #2 in this file (single-PA flush) was exactly the failure mode of trying to keep a denormalized cache consistent across multiple mutation sites. The structural fix (this section) makes the bug impossible by removing the cache.

### Files touched (this section)

- `app.py` — AppState gains `cursor_pos` property, loses `curr_stop_disp` field; `_next_pa` reordered + skip-setup inlined; jump_to_stop / circular reset cleaned up
- `display.py` — all `curr_stop_disp` reads → `cursor_pos`; `increment_current_stop_display` DELETED; `_update_skip_progress` no longer mutates disp; `draw_ptr` gains off-window guard
- `audio/mock/main/route.json` — passing-station `time` removed; post-skip PA times absorbed
- `.claude/rules/notes.md` — Station Skip Logic section rewritten around the derived-cursor model
- `CLAUDE.md` — Key Feature #8 updated to reflect single source of truth

---

## Data audit + cleanup sweep (later same day)

Started as a cross-check of `audio/**/route.json` against `DATA_FORMAT.md`. Quickly revealed that the spec and the data had drifted apart over many sessions — rules that code treated as optional were spec'd as required, rules that were supposed to hold didn't hold anywhere, and one whole line folder (`takasaki/831M`) was broken. Ended up with a persistent validator, a tighter passing-station rule, and 12 route files cleaned.

### The passing-station rule, now final

Was: "passing stations *should* have `time` set for proper countdown" (old spec). Actual behaviour: every route already OMITTED `time` on passing stations, and the code derives the countdown from the next PA station's `time`.

So mid-session, first tightening: passing stations (`pa: []`, non-first) must have NO `time`, NO `sta_cut`.

Then the stronger form came out of a single concrete question: on nambu/4027F, 西府 (passing) references `Nishifu.mp3` but the file is `Nishifucho.mp3`. Rename the file or strip the reference? User's ruling: **"if it's passing, then no need sta."** Generalised: passing stations don't play any audio, so the `sta` field shouldn't exist either.

Final form now in DATA_FORMAT.md + notes.md + validate_data.py:

> Passing stations (`pa: []`, non-first) must omit `sta`, `sta_cut`, AND `time`. The train doesn't stop → no departure melody → no countdown of its own; all three are dead data at best, misleading at worst.

Terminus stations follow the same "no departure, no sta" logic. Stripped `sta`/`sta_cut` from termini on chuo/916H (東京), chuo/1654T (東京), keihin/1275A (大船), nambu/4027F + 603F (立川).

### The `diagram` field was always dead

`setup.py:73` does `folder_diagram if folder_diagram else route_data.get("diagram", "")` — folder name always wins. JSON `diagram` was only a fallback that never fired. Worse, the yamanote entry said `"1275A"` (a Keihin-Tōhoku diagram!) — a copy-paste leftover. Stripped from all 4 routes that had it (nambu × 2, yamanote, mock). Insight: compound folder names already handle multi-diagram sharing (keiyo's `780Y_1510Y/`) — no need for a second source of truth.

### `skip: 1` on nambu/603F was dead too

12 stops had it. Nothing in `app.py` or `display.py` reads `stop["skip"]` — the internal `self.state.skip` is computed from the PA-gap. Appeared to be an authoring artifact: 603F (local) was copied from 4027F (rapid), and the `skip: 1` flags — which had semantically meant "this is a rapid-skipped station" — were never cleaned up for the local variant.

### Folder rename `nanbu` → `nambu`

Hepburn: ん before b/m/p becomes m. Nambu is JR East signage convention. git mv preserved history. No runtime string refs to "nanbu" — only doc-examples in setup.py, notes.md, preferences.md, updated to match.

### `contrast_color`: fix the spec, not the data

Code uses `[224, 54, 37]` (JR red) as default when field is absent. 10 routes relied on this. Spec claimed field was required. Two ways to align: add the field to all 10 routes, or mark optional in the spec. User's call: spec should match reality — field is now documented as optional with default.

### Terminus `sta` / passing `sta` pattern tension on nambu

Pre-rule, passing stations on nambu had working `sta` refs pointing to real files (Shitte.mp3, Kashimada.mp3, ...). After user's "passing → no need sta" ruling, stripped them all. The mp3 files stay on disk, unreferenced. Didn't delete them — user said "sta is fine" earlier re: not wanting to churn audio files, and keeping orphan mp3s costs nothing vs. losing the possibility of a future "play short announcement while passing" feature.

### validate_data.py — the persistence lesson

User noticed I was recreating throwaway `_scan.py` files every time I needed to verify state. Valid pattern for one-off checks, bad pattern for things we want to enforce. Extracted into `validate_data.py` at project root — self-contained, 10 checks, exits 0/1 for CI. The 130-line embedded script in DATA_FORMAT.md was replaced with a 20-line pointer, because embedded scripts drift from the live rules (it was missing all the rules we'd added in prior sessions).

New checks `validate_data.py` catches that the old embedded script didn't:
- passing-station `sta`/`sta_cut`/`time` fields
- first station `time: 0`
- audio file existence for every `pa`/`sta` reference on disk
- compound-dest and stop-level dest override translations
- `stations.json` `code_3` count consistency (22 per spec)

The audio-file check surfaced all the missing-audio incidents — would have caught the kita-ageo incident from critical_lessons.md at write time.

### Surprises from running the validator

- **武蔵小杉 was NOT in translations.json** despite nambu (both routes) and others referencing it. I had asserted it was already there in the initial audit; was wrong. Validator caught it.
- **keihin/727B uses multi-line JSON sta formatting** (e.g. `"sta": [\n    "JK05"\n],`). Simple line-based strip scripts only remove the opening line and leave the continuation orphaned → JSON parse error. Script approach needs to be block-aware, not line-aware. Caught and fixed mid-flight.

### Translation additions (Hepburn macrons)

- 23 Nambu-exclusive: Shitte, Yakō, Kashimada, Hirama, Mukaigawara, Musashi-Nakahara, Musashi-Shinjō, Musashi-Mizonokuchi, Tsudayama, Kuji, Shukugawara, Noborito, Nakanoshima, Inadazutsumi, Yanokuchi, Inaginaganuma, Minamitama, Fuchū-Honmachi, Bubaigawara, Nishifu, Yaho, Yagawa, Nishi-Kunitachi.
- 3 Yamanote: Meguro, Sugamo, Ōtsuka.
- 1 previously-missing: Musashi-Kosugi.

### sta_code for nambu: JN01 → JN26

Sequential, 川崎=JN01 → 立川=JN26. User asked to confirm before I applied mass changes — good instinct. 立川 is JN26 (route-local) even though it's also JC19 on Chuo; sta_code is route-local per spec.

### Preferences baked/reinforced this session

- **Persist validation scripts, don't recreate them.** Trigger for this principle: I wrote `_scan.py` three times in one session. User's comment: "consider extracting it so you don't have to recreate it everytime". Lesson extends beyond validation — any check that could run more than once wants to be a file.
- **Align spec to data OR data to spec, but don't leave drift.** User made this call twice (`contrast_color` → fix spec; `sta`/`sta_cut` on passing → fix data). The specific direction is a judgement call, but drift is not tolerated.
- **Confirm mass-changes before applying.** User stopped me mid-flight on sta_code values: "confirm with me the sta code before adding". I'd already applied JN01-JN26 to 4027F; user was OK with the numbers but wanted the confirmation ritual. Pattern: **even for "obviously correct" sequential data, state the values explicitly and wait for go.** Costs one conversation turn, catches wrong assumptions cheap.
- **Minimum-invasive fixes.** User chose "strip broken sta" over "rename audio file" for 西府/Nishifu; later generalised to all passing stations. Fixing broken refs is local; renaming files has cross-cutting blast radius.
- **Measure twice before destructive ops.** For the nanbu→nambu rename, checked for hardcoded string refs BEFORE running `git mv`. No code refs, only doc-examples. Critical_lessons.md rule held.

### Files touched (this section)

**Data (shipped)**:
- `data/translations.json` — +27 entries
- `audio/chuo/916H/route.json`, `audio/chuo/1654T/route.json` — passing/terminus cleanup
- `audio/keihin/1275A/route.json`, `audio/keihin/727B/route.json` — passing/terminus cleanup (727B had multi-line sta JSON parse issue mid-script)
- `audio/saikyo/1349F/route.json`, `audio/saikyo/759K/route.json` — passing-station `sta` strip
- `audio/tokaido/3535E/route.json` — passing-station cleanup
- `audio/takasaki/3922E/route.json` — passing-station cleanup
- `audio/takasaki/831M/` — DELETED (incomplete)
- `audio/yamanote/route.json` — `diagram` stripped, 大崎 `time: 0` added
- `audio/nambu/4027F/route.json`, `audio/nambu/603F/route.json` — sta_code JN01-JN26, per-segment `time` on 603F, `type_color` → black on 603F, `skip: 1` removed, `diagram` stripped, terminus cleanup
- `audio/nambu/` — folder rename from `audio/nanbu/` via git mv (~100 mp3 files preserved, renamed with history)

**Data (harness)**:
- `audio/mock/main/route.json` — `diagram` stripped, passing-station `sta` stripped

**Program — tools**:
- `validate_data.py` — NEW, project-root validator, 10 checks, CI-ready

**Program — docs**:
- `DATA_FORMAT.md` — passing-station rule tightened; embedded validation script replaced with pointer to validate_data.py; Supported Lines expanded 6→9; line prefixes expanded with JN/JJ/JU; `contrast_color` documented optional; `sta` field empty-value convention updated; `diagram` field removed; `validate.py` → `validate_data.py`; 7 other stale-note fixes
- `.claude/rules/notes.md` — passing-station rule tightened; folder rename references
- `.claude/rules/preferences.md` — folder rename reference
- `setup.py` — folder rename references in comments

### Deferred

- **joban/tsuchiura** — all 19 stops still missing sta_code, 15 stations missing translations, dest "土浦" missing, first station time missing. User said awaiting audio files availability, don't touch.
- **mock/main** — still has `sta_cut: 0` on passing stations, compound dest "品川・高輪ゲートウェイ" missing translation, 高輪ゲートウェイ missing translation. User said "not a concern" — test harness, not shipped.
- **`sta` naming style** on nambu — Romanized filenames (`Kawasaki.mp3`) rather than code-based (`JN01.mp3`). User said "sta is fine" — would need audio-file rename, not worth the churn.

---

## v0.5.2 release cut + /build & /commit skills + release.ps1 rework

Later in the day, shipped v0.5.2 on GitHub (https://github.com/ksleungac/pids-jre-simulator/releases/tag/v0.5.2) and formalized the build/release tooling around it.

### /build skill (new): local exe build + stage + smoke test

Before this session, `CLAUDE.md` pointed at a `/build` skill that didn't exist — `release.ps1` was the only actual path. Created the skill with these tenets:

- **Ask for version when not provided.** Don't guess from tags or prior sessions.
- **Version in exe metadata, not filename.** Exe keeps stable name; Windows version resource (`--version-file`) carries the string + tuple. Readable via right-click → Properties → Details.
- **Subversion letters are sub-revisions, not beta.** `a=1, b=2, c=3` → 4th slot of the version tuple. `0.5.2b` → `(0,5,2,2)` string `"0.5.2b"`.
- **Audio junction for testing.** `dist-release/.../audio` is a Windows directory junction to project `audio/`, so the staged folder is immediately runnable without copying the exe to the project root or duplicating gigabytes of audio.
- **Hard stop before zip** for user smoke test. Claude auto-launches the exe (`Start-Process`) so the user doesn't hunt for it, but does NOT claim the app works — runtime verification is the user's job.
- **Break junction + empty the dir before `Compress-Archive`.** `Compress-Archive` follows junctions → would include all audio in the distribution zip. Junction-safe delete: `[System.IO.Directory]::Delete(path, $false)` (NOT `Remove-Item -Recurse` — that recurses into the target).

### /commit skill (new): pre-commit hygiene

User's motivation: their own commits often bundle small unrelated data tweaks into code commits ("happens all the time"). That makes release-note classification ambiguous at cut time. New skill:

- Classify every staged+unstaged file into shipped/harness/code/docs/internal buckets.
- Infer the "main ask" from conversation, apply relatedness test.
- Green-light mixed commits when the data is REQUIRED by the code change (new feature + its JSON data).
- Red-flag bundled unrelated data fixes; propose a split.
- User's linter-pass refinements: added "never commit" bucket for `*.exe`/scratch `_*.py`, added parallel-Claude-session flagging (files I didn't touch in this session's tool history), commit message drafted alongside the review so user gets one round-trip not two.

### release.ps1 reworked in three commits

- **38bebc4** — Changelog now splits commits into Program / Data sections via path-based classifier (`^(data/|audio/)` = Data-only, else Program). Range = previous tag..HEAD (walks `git tag --sort=-creatordate` to find the tag immediately before `$VERSION`).
- **3496982** — `release_notes.md` honor: if file already exists when script runs, skip the auto-classifier and preserve on cleanup. Lets Claude pre-write prose notes without being clobbered.
- **2b08a09** — Parses `$VERSION` the same way `/build` skill does, generates `version_info.txt`, passes `--version-file` to PyInstaller. Pre-this, exes released via `release.ps1` shipped without metadata.

### Conventions established (canonical in the skills now)

- **Release-note criteria = shipped-artifact test.** Include iff the change affects what's inside the distribution zip (exe, fonts/, data/*.json, audio/). Excluded: README, CLAUDE.md, `.claude/**`, `memory/**`, docs commits, this session's tooling. Future bundled manuals inside `dist-release/JRE-PA-Simulator/` would qualify.
- **Mixed-commit handling at release time.** Claude rewrites commit subjects into user-facing prose, splits mixed commits into Program + Data entries when both shipped portions are substantive (a826509 split into `feat: Render ... badge` + `Add data/stations.json catalog`). Pure-fixture mixed commits go to one side only (c0ac72e mock-route tweak → Program; d0bf069 takasaki fixture → Program).
- **Prose over raw commits for user-facing notes.** Raw subjects like `feat: Render JR East 3-letter station code on upper-LCD badge` become `Station badges now show the 3-letter Roman code (AKB, TYO, …) for the 22 major JR East interchange stations.`

### .gitignore fixes surfaced en route

- **Root-anchoring required for directory ignores**: `build/` was matching `.claude/skills/build/` (my new skill dir). Fixed all top-level dir ignores to `/build/`, `/dist/`, etc. Glob patterns (`*.spec`) stay as-is.
- **`JRE-PA-Simulator.spec` now gitignored** — PyInstaller regenerates it each build; tracking it made every build dirty the tree. `git rm --cached` + `*.spec` in `.gitignore`.
- **New artifacts**: `version_info.txt` and `release_notes.md` added to gitignore.

### .github/ deleted

`release.yml` was from v0.5.0 era, never updated when `stations.json` landed, never generated release notes, and wasn't generating releases (only uploading artifacts to workflow runs). `release.ps1` is the single release path; `.github/` removed entirely.

### One unpushed tooling commit at session end

`3496982` + `2b08a09` are local only — post-v0.5.2 groundwork for the next release. Don't ship, no rush to push.

### Preferences baked

- **"u like" = defer to Claude's judgment** when user has no strong preference, but surface the decision for visibility before executing.
- **Never auto-commit, never auto-push.** Tagging locally is reversible; push + `gh release create` are shared-state actions that need confirmation (though "continue with release flow" counts as authorization).
- **Parallel Claude sessions are a real hazard.** `.claude/settings.local.json` got modified by another session — don't silently include/revert, surface and let user route.
- **Discussion-first applies even to release notes.** Raw commit list → categorized (program/data/excluded) → ask about mixed commits → propose prose → wait for approval → write file. At least one round-trip before anything ships.

---

## README overhaul + i18n baseline + /readme skill

After v0.5.2 shipped, rewrote all user-facing docs to match the new distribution model (bundle zip on GH Release) and extracted the route list so translations stop duplicating it.

### What changed in the READMEs

- **Install → Download**: old model was "clone the repo + drop exe in root". New model is "grab zip from GH Releases, extract, run." Standalone exe is a second asset for users who already have the supporting folders.
- **Removed "install fonts system-wide" step** — fonts load from `fonts/` via `pygame.font.Font(path, size)`; system install was never needed, docs just said so anyway.
- **Removed "git pull master for latest data"** — data ships in each release now.
- **Added manual-control paragraph**: `Page Down drives everything. The simulation is fully manual — the train doesn't advance on a timer; every announcement and every stop happens only when you press Page Down.` User's words: "to emphasize the app won't automatically go to next stop". This answers the #1 "why isn't anything happening?" confusion on first launch.
- **Yellow-square explanation**: "more than one announcement, keep pressing Page Down to play them all before arriving at the station." User corrected my first draft twice:
  1. Don't tie it to a location ("next to the countdown" — UI may change); just say "on the display".
  2. Don't say unplayed ones are "skipped" — they're NOT. The simulation stays stuck. Framing is about *pacing* ("before arriving"), not *consequences*.
- **Fixed Yamanote row**: was listed with diagram `1275A` (wrong — that's a Keihin-Tōhoku diagram). Yamanote ships with no diagram subfolder; row now shows `—`.
- **Added Takasaki row**: `3922E | 快速アーバン`.

### ROUTES.md — language-agnostic extraction

User's idea: the routes table is all Japanese line names + diagram codes + Japanese train types. The zh-HK and zh-CN translations would be character-for-character identical to the English table. Extract to `ROUTES.md` and link from the top of every README instead of duplicating.

Top link bar pattern: each README lists the OTHER two languages + shared `ROUTES.md`:
```
**[English](README.md)** · **[繁體中文](README.zh-HK.md)** · **[對應路線](ROUTES.md)**
```
The current language is not self-linked. The `ROUTES.md` label is localized per file (`Supported Routes` / `對應路線` / `支持的路线`), but the target file is shared.

### Simplified Chinese (zh-CN) added

New file `README.zh-CN.md`. Language list grew from 2 → 3. Terminology diverges between zh-HK and zh-CN in a few places:

| Concept | zh-HK | zh-CN |
|---|---|---|
| diagram | 班次 | 车次 |
| line | 路線 | 线路 |
| interchange | 轉車站 | 换乘站 |
| yellow square | 黃色方格 | 黄色方块 |
| quit | 離開 | 退出 |
| the app (behavior) | 程式 | 程序 |
| setup screen | 選擇畫面 | 选择界面 |

### Chinese phrasing preferences (baked into `/readme` skill)

User's corrections during this session:

1. **Don't literal-translate EN→ZH.** "每個站都需要你手動按 Page Down 才會發生" reads awkward — grammar-checked but not native. Native rewrite: "播放廣播、切換到下一站都需要人手按 Page Down." Rule: if it reads like a translation, rewrite it.
2. **Use 程式 / 程序 for program BEHAVIOR**, not 模擬器 / 模拟器. The latter is reserved for the project identity (title / elevator pitch). Talking about runtime behavior ("doesn't auto-advance") → 程式 / 程序.
3. **User POV over developer POV.** Drop "PA track" for "announcement" (廣播). Don't mention UI locations ("next to the countdown") that may change with future LCD styles.
4. **"Before arriving at the station"** framing for the yellow-square hint — narrative pacing cue, not consequence warning.

### /readme skill

Codified all of the above into `.claude/skills/readme/SKILL.md`:
- Workflow: English first → extract language-agnostic → zh-HK → zh-CN → sanity check top-bars + section parity.
- Terminology baseline table (grows as new terms are coined).
- Chinese-phrasing preferences with bad/good examples.
- Anti-patterns: don't drift section headings across releases, don't leave one language behind after expanding another, don't touch ROUTES.md as part of routine README updates.

### GitHub release asset swap

After confirming 629 MB fits under GH Releases' 2 GB per-file cap, replaced the v0.5.2 `-distribution.zip` asset (was 65 MB, no audio — residue of a split-bundle plan we later abandoned) with the full 629 MB bundle (exe + fonts + data + audio). Standalone exe remains attached for users who already have the supporting folders.

Release notes on GH edited to match (softened the `nanbu/`→`nambu/` warning since the app is folder-invariant; rewrote Distribution section to describe the two assets).

### Preferences baked

- **README is THE source of truth for user-facing flow.** Not release notes, not CLAUDE.md. If a user can't figure out install from README, the README is broken.
- **Concise > complete.** User trimmed my draft twice ("be short and clean", "no need so much implications"). Every sentence must earn its place.
- **Native Chinese > translated Chinese.** Rewrite, don't transcribe.
- **Three-language parity is non-negotiable.** Don't expand one language's README without matching the others in the same session.
