# 2026-05-27

## Session: 8-station continuity triangle + TODO cleanup

Implemented the continuity triangle for the 8-station lower LCD view. The triangle extends from the last cell's colored bar when the route continues past the visible window — appended after the 分 marker area, no chevron arrows. Three iterations to get placement right: first attempt shortened the cell bar (wrong — user wanted extension), second put triangle inside the 分 area (overdrawn by draw_times), third placed it after draw_times. The 分 marker's 3px white cap (bar terminator) was the final detail — it's a color-to-color separator only needed at mid-window dest cells (keihin 727B pattern: dest before route end), pointless on the last cell. Suppressed it there instead of hacking the triangle position.

Dormant `_draw_continuation_marker` scaffolding removed — it was parked since 2026-04-25 waiting for the full-route continuity bug to be fixed, which happened the very next day (2026-04-26 `continuity[2]` state-leak fix). The blocking reason was stale for a month.

Also purged 34 stale `[x]` items from TODO.md (166→120 lines) and confirmed ENGLISH EIGHT slot was already shipped (EnglishEightStationDisplay at lower_lcd.py:1565).

Ralph review+fix: 2 cycles, clean on cycle 2. Findings: dead variable `is_last_cell`, stale "future" comment on `side_margin`.

## Codifications this session

- DISPLAY_E235.md § "Continuity triangle (8-station view)" — rewrote scaffolding section to document shipped behavior
- conventions.md § "Comment dormant scaffolding" — removed stale `_draw_continuation_marker` example
