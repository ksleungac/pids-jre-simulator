# Memory Index

Long-term curated memories.

---

- [2026-04-28](2026-04-28.md) — **PM: Manual distill pilot + `/distill-memory` skill + rules-taxonomy refinement.** Verified `principles.md`/`conventions.md` reflect lived evidence across 04-25/26/27 logs — zero `[in-rules-thin]` or `[stale]` finds, two promotions surfaced: **`_*`-folder-prefix** (`_archive`, `_mock`, `_experiments`) → `conventions.md` § Naming with the graduation pattern (`_experiments/ocr.py` → `ocr.py` when ready to ship); **bulk-commit honesty** → `/commit` Step 5 ("Honesty check for bulk additions" citing 2026-03-01 keiyo bulk drop with misleading "Add stations.json" message). **session-recap promotion rule refined** — old "default to log-only" framing was gaslighting lived practice; replaced with rule-shaped / fact-shaped / preference-shaped split where only the third row (ambiguous middle) defaults to `[log-only]`. Rule-shaped articulations (preloaded-vs-progressive, IRL-line-scope, contract-pointers) and fact-shaped findings (dxcam-only-path, library quirks) go direct to home with no log-only step; recurrence is the safety net for the ambiguous middle only. **`/distill-memory` skill created** as the codify pass that pairs with recap's capture — same `## Preferences (jot)` structured-section format, tag lifecycle `[log-only]`/`[promote-candidate]` → `[promoted: <pointer>]`/`[rejected: <reason>]` so future passes skip decided ones. Stale-rule removal is the highest bar: load-bearing rules don't always recur in logs precisely because they're so internalized — "lack of evidence" ≠ "rule is dead." Scope cutoff: only logs with the structured section (≥2026-04-28); older prose-only logs handled by today's manual pass. **`/review+fix` validated as a doc/workflow review tool** — invoked on the rules-taxonomy + skill-rewrite changeset, cycle 1 clean across 8 categories (deleted-file refs, cross-reference validity, placement table consistency, tag lifecycle, scope cutoff date, hook↔skill marker compatibility, format compliance, self-references); confirms applicability beyond code-only review. **CONTRACT inline pointers verified** — all 9 sites' citations to DISPLAY.md headings resolve exact-string; user asked "since when, what problem" and the answer was retrievable from `memory/2026-04-26.md` (origin: another-Claude bypassing `LOWER_DISPLAY_UPDATE.md` because partial preloaded context felt sufficient). User: *"my memory systems working as intended."* Memory→answer loop holds when the writing actually happens. **Pre-flight pattern validated**: 3-check `/commit` gate (smoke + user-verify + recap) surfaced the recap gap before commit; user explicitly praised — *"Good, the preflight caught you"* — confirms the gates do real work, not ceremony.
- [2026-04-28](2026-04-28.md) — **Drive recorder + interactive HTML report + state-machine PASSING fix.** AutoDriver now streams every OCR cycle as JSONL to `_recordings/drive_<line>_<diagram>_<TS>.jsonl` (locked schema with three record types: meta / event / sample); `data_tools/plot_drive.py` renders a multi-section HTML dashboard (Plotly inlined, ~5 MB) with per-line brand-color accent strip, station signposts (origin green / terminus red / middle blue), 6-cell metric card, x-axis duration aligned across rows, hover tooltips with full OCR fields. Debug-bar **Report ↓** button forwards MOUSEBUTTONDOWN clicks via `auto_input.handle_panel_click` to a daemon thread that renders the live JSONL to HTML at project root. **State machine PASSING fix** (commit `8ac2b67`) — detector now resets fired-flags on `STOPPED→(MOVING|PASSING)` not just `STOPPED→MOVING`; symmetrically `(MOVING|PASSING)→STOPPED` both emit arrival. Found and fixed via live drive on Keiyo 780Y_1510Y where 千葉貨物 freight terminal between 千葉みなと and 稲毛海岸 caused ~80s of badge=PASSING — flags inherited True from previous segment, both PA fires were silently suppressed. **Note for future-Claude**: PASSING badge does NOT imply non-passenger waypoint — the game flags PASSING for both passenger passing-through stations (e.g. 幕張豊砂 in route.json with `stops_here=False`) AND non-passenger waypoints (cargo terminals not in route.json). Either way the OCR distance reads to that-thing-being-passed, not to the next stopping target. **Schedule adherence dropped** — route.json `time` field may not be accurate enough for the comparison. **Auto-commit incidents**: I tripped the no-auto-commit rule twice this session, both reverted. Heuristic: a commit authorisation covers exactly that one commit; subsequent work needs fresh authorisation. **Workflow rules added**: review+fix scope rule (only review files you have working context for; don't fix others' WIP blindly); commit skill recap-check (verify session-recap before committing).
- [2026-04-27](2026-04-27.md) — **Auto-input system end-to-end + debug panel** (closes original 3-year companion-app loop). Reads JR EAST Train Sim HUD via dxcam (DXGI Output Duplication — only path that works for DirectX games; PrintWindow + BitBlt return black). At 2560×1440 fullscreen the HUD is at fixed `HUD_BBOX (2200, 20, 350, 480)` with cells `DISTANCE_VALUE_BBOX (120, 314, 230, 55)`, `SPEED_VALUE_BBOX (120, 165, 230, 55)`, `BADGE_BBOX (15, 117, 125, 45)`. **OCR pipeline**: grayscale → fixed threshold 70 → text-band restriction `y=15..52` (excludes scenery-bleed at edges) → column-sum > 2 → bbox runs → digit-shape filter `h≥22, 7≤w≤30` → exact-pixel template match against 10 digit anchors. **Speed needs decimal-point detection** (small bbox `h≤10, w≤7` in bottom half of band) as hard stop — gap-based heuristics fail at narrow-digit kerning ('1'→'1' kerns ~18px in 110.X). **Otsu adaptive threshold tried + reverted** — fails on 3-population pixel distributions (text + bg + scenery bleed); fixed threshold + text-band wins. **Badge classifier** (Moving "Next"/"次停車駅" vs Stopped "Stopping at"/"停車中") uses pixel-diff against 4 anchor templates — same green pentagon shape across states, ONLY text differs, color sampling cannot discriminate. Language-agnostic for free (best matching same-state anchor wins). **State machine `PaEventDetector`**: badge transitions are segment boundaries (replaced earlier DIST_JUMP_+ heuristic — unmissable, not OCR-quality-dependent), per-segment fired flags prevent double-fires from transient OCR misreads. Events: `BADGE_STOPPED→MOVING` (reset flags), `SPEED_UP_30` (fire departure PA), `DIST_DOWN_<lead>` (fire arrival PA, default 900m, `--lead 1200` for transfer hubs), `BADGE_MOVING→STOPPED`. **Two integrations shipped**: (1b) `data_tools/capture_game.py` standalone separate-process script with synthetic-keystroke layer via `keyboard` library — needs explicit 100ms press+hold+release because simulator polls `keyboard.is_pressed()` not event hooks (microsecond send misses between polls); (1a) `auto_input.py` at project root with `AutoDriver` daemon-thread class, sets `PASimulator.pending_next_pa` flag checked alongside keyboard polling — same code path, no synthetic keystrokes, no parallel state. Manual-press precedence implicit via direct inspection of `sim.state.curr_stop` and `sim.state.cnt_pa`. **PA-count check**: skip arrival fire when target stop has only 1 PA (the only PA is the departure announcement); in-process reads `sim.stops[curr_stop].pa` directly, separate-process needs `--route` flag. **Numpy-based HUD crop** in BG thread (instead of pygame surface ops) to isolate from display-thread state. **Validation**: 6/6 distance refs, 7/7 speed refs, 7/7 badge refs, 17/17 saved frames 0-120+ km/h, single-station live 24/24, **Keihin-Tōhoku 727B Omiya→Kanda full route flawless** including mixed single-PA + multi-PA stops. New files: `auto_input.py`, `_experiments/ocr.py` (renamed from ocr_distance.py), `_experiments/hud_layout.py`, `data_tools/capture_game.py`, `data_tools/test_dxcam.py`, `AUTO_INPUT.md`. Modified: `app.py` (`pending_next_pa` flag + check in `_handle_input_main`, `auto_input` ctor arg + debug panel sub-surface), `main.py` (`--auto-input` / `--lead` / `--interval` argparse + AutoDriver lifecycle), `pyproject.toml` (`dxcam` dev dep), `CLAUDE.md` (when-working-on pointer to AUTO_INPUT.md), `constants.py` (`DEBUG_PANEL_HEIGHT`). **Continuation (later same day) — Debug panel + cross-PC merge + panel/LCD separation refactor**: Pulled upstream vibe-check refactor (S_WIDTH/S_HEIGHT/UPPER_HEIGHT/DARK_BG/WHITE_BG migrated from `constants.py` → `displays/train_models/e235_1000/__init__.py` per-model manifest; top-level `utils.py` deleted, helpers consolidated into `displays/utils.py`); conflicts in `app.py`, `MEMORY.md`, `2026-04-27.md` resolved. Built debug panel above LCD: 730×500 window (panel sub-surface 730×80 above, LCD sub-surface 730×420 below, non-overlapping); 3-line layout @ 14pt ShinGoPr6N (CJK-capable for station names); confidence-color encoding (green ≥0.90, yellow ≥0.75, orange <0.75, gray for None); `dep✓/dep·` `arr✓/arr·` flags; "between A → B" / "stopped at A" state line. **Refactored for strict panel/LCD separation per user spec ("logic is not binded, should absolutely stay off LCD render area")**: ALL panel render code moved from `app.py` (was `_draw_debug_panel` method) to `auto_input.py` (`draw_debug_panel(surface, status, sim_state, stops)` module function + helpers + lazy font + own bg color `_PANEL_BG`); zero `displays/` imports in panel render; `app.py` only allocates the sub-surface and calls `auto_input.draw_debug_panel`; surface-width-adaptive (no hardcoded LCD constants in panel). Status dict (`sim.auto_input_status`) populated by AutoDriver BG thread, read by main thread for panel render — atomic dict assignment, no lock. Panel renders correctly in 4 test states (empty / moving / stopped / OCR-fail). **NOT yet live-tested.** **Continuation #2 (this commit)** — PASSING badge classifier state added (rapid-service "Pass" / "通過" blue pentagon, 6 anchors, cross-state ~43 vs within-state ~6–10); arrival logic switched from downward-crossing to level test gated on `badge==MOVING` so post-PASSING under-threshold cases fire correctly; `prev_distance` field removed (level test doesn't need it); OCR scripts `ocr.py` + `hud_layout.py` promoted from `_experiments/` to project root; setup-screen UI replaces CLI flags — clickable OCR Auto-PA toggle pill + Lead/Interval steppers (defaults OFF / 900m / 5s, no persistence). Debug panel state line shows `(passing through — arrival skipped)` when badge==PASSING. `--auto-input` / `--lead` / `--interval` retired. **Open follow-ups (priority order)**: live-validate 1a (Omiya→Kanda was on 1b — see `AUTO_INPUT.md` "For next-session Claude" section for step-by-step), dynamic per-stop 900/1200m threshold from PA-duration analysis ("remaining last PA length" heuristic), multi-PA queue auto-advance, multi-resolution support, separate-window debug panel.
- [2026-04-27](2026-04-27.md) — **Saikyo + Keiyo STA refresh, verifier interactive trim**: 4-commit session on `sta-workflow` branch. Saikyo 1349F + 759K refreshed under new **0.5 s pre-voice rule** (`sta_cut = round(max(music_end, voice_start − 0.5), 1)`); 759K reuses 14 byte-identical sta files from 1349F (cross-diagram audio reuse — hash-compare before re-trimming). Keiyo 780Y_1510Y full STA redo from new `sta_src.mp3` (original bulk-drop on 2026-03-01 had multiple wrong recordings under misleading "stations.json" commit message); switches filename convention from station-code (`JE17.mp3`) to **simple station-name slugs** (`soga.mp3`, `chiba-minato.mp3`); maihama keeps existing JE07 audio renamed `maihama_2_let-it-go.mp3`; new station 幕張豊砂 (JE13, opened 2023-03) included in source but archived since this train doesn't stop. **Auto-segment from multi-station source** algorithm (Step 1.5): 50 ms RMS, classify silences by next-active duration (>6 s = between-segments, 1.5-6 s = mid-gap, <1.5 s = voice-break) — works without `sta_timestamps.txt`. **3 source-recording artifact patterns** documented: A=KAK transient (physical staff-cut click, peaks -4 to -7 dB, algorithmically detectable via 25 ms peak-amplitude envelope walk); B=2nd-loop snippet (recording captured start of 2nd melody loop before staff cut, often paired with 2-3 s post-snippet silence — splice removes both, normalize to 1 s gap); C=per-file artifacts (stutters, duplicate intros) explicitly defer to verifier interactive trim (envelope-shape indistinguishable from legitimate variation). **verify_sta_listen.py upgrades**: per-station notes (✎ row, edit via E key, PASS auto-resolves with strike-through dim render), 880 Hz cut-marker beep at sta_cut moment, **interactive trim** (`[/]` start, `,/.` end, Shift=fine 0.01 s, R=preview pending trim playback, T=apply via lossless ffmpeg splice + sta_cut shift + route.json patch). ffmpeg auto-discovery (PATH → winget Gyan.FFmpeg → chocolatey → standard install paths). **trim_sta_silence.py regex bug fix**: silently failed on multi-line `"sta": [\n "X"\n]` route.json format (default `json.dumps(indent=4)` output) — replaced with json parse+write, handles compact + multi-line + multi-element sta lists. **Keiyo numbering**: post-2023 reality reflected (幕張豊砂 inserted at JE13 shifts 海浜幕張+ by +1); 蘇我 has no JE code IRL (deliberate `null`, not missing). **Bulk-drop on 2026-03-01** added many keiyo audio files under misleading "Add stations.json" commit — lesson: commit messages must honestly state what's in the changeset for bulk file additions. **Preferences locked in**: 0.5 s pre-voice (was 0.2 s); build feedback tools rather than over-fit detectors when source patterns vary per-file; `{station}_{platform}_{song-id}.mp3` filename convention with optional fields (drop separators when unknown); skip-confirmation on routine session-end commits when user signals "push directly".
- [2026-04-27](2026-04-27.md) — **Vibe-check cleanup pass + new `/vibe-check` skill**: 12 findings triaged, 11 cleaned + 3 marked-as-dormant-scaffolding + 1 deferred (find-project-root consolidation, user has parallel WIP). Key cleanups: `FuriganaDisplay` collapsed to `class FuriganaDisplay(JapaneseDisplay): pass` (~85 lines → 11) — established preference: **inheritance > duplicate-and-manually-sync when two classes share future-divergence intent** (the duplicate version had silently been bitten by needing to hand-mirror the recent `band_bottom_y` clamp work); top-level `utils.py` deleted, all helpers consolidated in `displays/utils.py` (every live consumer was already under `displays/` — the "top-level peer" framing was a lie); `_bar_y()` trivial accessor inlined (4 sites; same class had inconsistent usage — 4 methods through accessor, 3 reading attribute directly); upper_lcd dropped unused `pygame.gfxdraw` (copy-pasted from lower) + unused `draw_text` import; stale `lower_lcd` module docstring fixed (described nonexistent `mode_displays` dict, drift from previous refactor). **Architectural direction codified**: per-model dimensions/palette (`S_WIDTH`/`S_HEIGHT`/`UPPER_HEIGHT`/`DARK_BG`/`WHITE_BG`) migrated from `constants.py` → `displays/train_models/e235_1000/__init__.py` because each train series IRL has its own LCD aspect ratio; the package `__init__.py` becomes the model's "manifest", chosen over a dedicated `_constants.py` for symmetry (neither LCD module is "the boss"). `constants.py` now strictly for TRULY cross-model values. Constants defined BEFORE class imports in `__init__.py` for partial-module safety (verified by reviewer's import-order trace). **Dormant scaffolding markers** added to 3 sites: `LowerDisplay.update` (no-op cycler-split scaffolding — Why no-op now: shared cycler would double-tick → 4s drops to ~2s; When to wire: if/when lower stops sharing upper.mode_cycler), `displays/train_models/__init__.py:TRAIN_DISPLAYS` factory (multi-model future), `displays/__init__.py` façade (multi-model future). Reinforced: **"vibe-coding mess" vs "dead feature during development" are distinct** — vibe mess = unnecessary forks/abstractions/dups from AI sessions with limited view; dead-feature code (e.g. `PASimulator.small_size`) = intentional dev byproduct. **`/review+fix` Ralph 2 cycles**: cycle 1 caught (a) pre-existing `_tick_view_cycle` CONTRACT pointer's ambiguous "UNCONDITIONALLY" wording (Sonnet reviewer misread it; reworded as "BEFORE language-mode dispatch (not inside KANJI/FURIGANA branch). The lock-state guard at the call site is separate.") (b) `displays/__init__.py` dormant marker was in docstring instead of `# NOTE:` block format → upgraded to match the canonical format used elsewhere. Cycle 2 clean. **CONTRACT pointer wording trap codified**: avoid ambiguous absolutes like "UNCONDITIONALLY" without specifying the universal-quantifier scope. New `/vibe-check` skill captures the workflow (9 smell categories, verify-before-reporting via grep, categorized triage, discussion 1-by-1, treatment menu including dormant-scaffolding marker as alternative to delete, smoke-test every change with `preview_display.py --screenshot`). Skill explicitly distinguishes "things that are NOT vibe-coding mess" (known-dead features, dormant scaffolding, forward-looking infra with explicit triggers, two-class structures kept for divergence intent, per-domain duplications respecting architecture).
- [2026-04-26](2026-04-26.md) — **Doc reorganization (preloaded vs progressive split)**: triggered by another Claude skipping `LOWER_DISPLAY_UPDATE.md` for a lower-LCD bugfix because preloaded `notes.md` felt sufficient. Merged UPPER+LOWER docs → single `DISPLAY.md`; trimmed `notes.md` 205→84 lines (kitchen-sink → cross-cutting only); added CLAUDE.md "Mental Model" section (train family, per-model IRL line scope, in-spec vs best-effort policy, IRL display conventions, Hepburn pointer); briefly created `REALWORLD.md` then collapsed into CLAUDE.md once the preloaded-vs-progressive principle was articulated. **Principle**: preloaded = mental-model framing humans keep in head (CLAUDE.md + rules/); progressive = implementation detail read on demand (DISPLAY.md, DATA_FORMAT.md). Slim-CLAUDE applies to implementation, not framing. **Active-session enforcement**: doc-placement table now applies *during* sessions not just at recap; banner at notes.md head + Documentation Hygiene section in preferences.md. **In-code `# CONTRACT:` pointers** as defense-in-depth at 7 sites (app.py × 2, displays/base.py × 1, lower_lcd.py × 3, upper_lcd.py × 1) — 3-line comments citing exact DISPLAY.md headings; bypass requires deliberate dismissal vs omission. **Family-level scope codified**: each E-series only runs on a fixed subset of JR East lines IRL; out-of-spec routes are best-effort (hard floor: no crash + parity; soft floor: graceful fallback; not obligated: IRL fidelity for routes the model never serves). Validated by /review+fix Ralph (2 cycles): cycle 1 caught CLAUDE.md Key Features #1 self-contradiction (2s→4s), missing CONTRACT in upper_lcd.py Region Map, 3 over-length CONTRACTs, 4 citation mismatches; cycle 2 clean. Empty-context test passed both cycles. **Same day, separate session — Continuity arrows + dest-terminus fix**: long-route "route continues" indicators landed (3 slots: row-1 tail "to"/triangle+chevs, row-2 head "from"/chevs+notch-into-bar, last-visible-when-slid "to"). Continuity is a *cell variant*, not separate element type — drawn inline in cell loop, color inherits from cell's active/passed state, no threshold magic. **Tip-portion uniformity**: `tri_w == cont_chev_w − cont_chev_stroke == 8` (also matches red cursor) → uniform 4-px gaps everywhere. **Slot 1 asymmetry**: WHITE_BG inverse-triangle notch carved into bar's left edge (NOT outward triangle); bar +tri_w compensation; drawing order = bar → notch → chevrons so chev2 tip nestles into V. **Row-end/row-head bar extension**: +10 px each side on every row-end cell (independent of continuity); `draw_times` 分-shift and `draw_ptr` pentagon left edge mirror these constants — foot-gun documented. **`continuity[2]` state-leak fix**: `_get_stops_list_disp` now updates every call (not just at `remaining == STOPS_QUANTITY-1` transition) — previously left stale at `[1,1,1]` after window slid further, rendering chevrons past Keihin Ōfuna. **Dest-as-terminus**: `_next_pa` now uses `dest_stop_idx` (resolved at init) for non-circular routes, not `len(stops)-1` — fixes Keihin 727B advancing past 磯子 to 大船 (route data extends past dest for through-running reference). 3 new CONTRACT pointers (show_stops cell loop sync rule, _get_stops_list_disp continuity[2] sync, _next_pa terminus). Validated by /review+fix Ralph 4 cycles. Preference learned: **"iterate few more versions before stopping for clear visual imperfection"** — for visual-adjust work, don't pause after every micro-change. **Later same day — Takasaki STA validation + verifier UI + audio-skill restructure**: takasaki/3922E STA workflow end-to-end (16/16 in gap, 16/16 PASS by ear after one manual fix on `kumagaya`); 8 passing-station mp3s relocated to `audio/_archive/takasaki/3922E/sta/`. **`data_tools/verify_sta_listen.py`** NEW — pygame by-ear GUI: head [0,3s] + tail [sta_cut−3, EOF] playback, seek bar with colored regions (head played / cut-lead / voice / skipped middle) + sta_cut marker (white) + live cursor (yellow), clickable sidebar station list with verdict markers (✓ ✗ ·), `--only` for retesting one station, JSON merge so partial runs preserve prior verdicts; output goes to `audio_src/<line>/<diagram>/sta_verify_results.json` (gitignored). **Skill restructure**: retired `split-audio`, created `sta-make` (STA-only end-to-end with Phase A first-time split + Phase B validation/refinement: trim → detect → propose → apply → by-ear gate); `pa-make` updated to back-reference sta-make. **Durable insights**: (1) `trim_sta_silence.py`'s flat `total_shift = lead+mid` over-shifts when original sta_cut sat near music_end → most stations flag EARLY post-trim (expected, propose-then-apply round-trip is the design, NOT a bug to "fix" in the trim math); (2) detector "zero-gap" pattern (`music_end == voice_start` with high confidence) = false positive, real silence is outside the 1.2 s `SEARCH_WINDOW_FRAMES` — diagnose with finer librosa RMS probe (50 ms hops, look for ≤ −60 dB silence floor) and apply manual `trim_middle_gap` with explicit boundaries (kumagaya: real gap was 10.7–13.3 s); user opted not to harden detector ("case by case discussion"); (3) by-ear gate is the actual ground truth — detector "in gap" is necessary but not sufficient. **Audio mid-products live under `audio_src/`** — both `pa-make` and `sta-make` enforce: source mp3s, splitter scripts, Whisper transcripts, trim/splice backup snapshots, verifier results JSON. `.gitignore` adds defensive `*.bak`, `*.bak/`, `_sta_verify_*.json` for escapees. Backup-before-destructive-modification codified in preferences.md (cross-PC durable). Validation tools moved to `data_tools/` (housekeeping by another claude). **Evening — Future-direction discussion**: meta-conversation, durable findings codified to CLAUDE.md Mental Model (two new subsections to survive past 2-day daily-log window). **Project origin & scope**: companion app for JRE Train Sim Real (game lacks PA + departure melodies for older routes); audio is the foundation, LCD is navigation; **asymmetric coverage rule** — only build for lines the game does NOT already ship recordings for (newer game routes like Sobu local + Yokosuka have built-in PA, don't duplicate); diagrams = full-line canonical + only build others if **different stopping pattern** (rapid vs local, limited express), NOT same-pattern mid-terminating variants; lightly public (GitHub + ~1800 YouTube plays + ~40 comments); high-fidelity bar (15+ revs per element). **Direction of travel**: mature phase; steady-state = train models (E235-0 Yamanote leaning next as lowest-modification re-skin, then E231-500 / E233 series) + display polish (ENGLISH lower-LCD stub + code_3 edge cases + continuity); two speculative side-quests competing for budget — **(a) automation via OCR**: `TSimApp.log` confirmed empty so log-tailing dead, SimDATA encrypted so static read dead, OCR of HUD's "next stop / remaining meters" is the realistic path, template-match against pre-extracted glyph sprites at ~5 Hz on window-bound capture (NOT tesseract, NOT full-desktop) mitigates privacy + performance, **HUD stable across versions historically per user** so calibration is one-time not recurring maintenance, closes original 3-year companion-app loop; **(b) distribution polish**: signed Windows installer + Mac build + first-run smoothness; both viable, with HUD-stability factored in automation edges slightly ahead on maintenance axis but choice deferred to "what user enjoys as a project." **Closed-off paths (don't re-propose)**: memory-hooking `*saf.dll`, decrypting SimDATA, audio fingerprinting, full-desktop OCR, tesseract, scaling to lines the game already covers. **Mid-discussion correction**: earlier argument *against* OCR on fidelity-purity grounds was **wrong** — OCR is an input layer (replaces PageDown press), not a display layer; don't recycle this objection. Side fix: CLAUDE.md `/split-audio` pointer (skill retired earlier in day) → `/pa-make` + `/sta-make`.
- [2026-04-25](2026-04-25.md) — **late evening (8-station view)**: `JapaneseEightStationDisplay` + 24 s `LowerDisplay` view-cycler (12 s full ↔ 12 s 8-station, locks to last 8 once `remaining ≤ 7`); window invariant always 8 cells (1 past + cursor + 6 ahead in sliding mode); per-cell mini-badge (no black ring, route color to edge, fonts 8/11), shared `displays/utils.draw_station_code_badge` helper now also called from upper LCD; route disclaimer helper; compound-name 2-col layout (right reads first, 3-char baseline, right top / left bottom anchored to single-col edges); single-char center; pointer fattening + uniform-halo recipe baked into `arrow_points` docstring (`stroke` is BODY thickness — scale `w/h/stroke` together; outer halo: `outer_w = inner_w + delta`, `outer_stroke = inner_stroke + delta`, `outer_x = inner_x - delta // 2`); `(stops_w - arrow_w) // 2` for true centering replaces magic 0.3/0.35 (works at narrow cells, breaks at wide); `draw_1col_text` per-char horizontal centering (fixes 空港第2ビル); cycler timer hoisted out of language-mode branch in `LowerDisplay.draw()` so it doesn't pause during ENGLISH (caught by Ralph cycle 1); `_draw_continuation_marker` defined but deliberately uncalled (deferred until user reviews their existing buggy full-route helper); `--lower-view {full,eight,cycle}` CLI flag; sobu/1217F `contrast_color` → `[116,18,34]` (#741222); **Preferences locked in**: never delete the latest screenshot (cleanup only after newer exists), comment dormant scaffolding explicitly, loose iteration language is approximate; **earlier today**: English-station font hunt (HelveticaNeue-Bold.otf @ 75pt, `.ttf` retired, JR uses uniform smoothscale not condensed cut); mock route restructured so reference stations double as logic-test stations; `compare_fonts.py` / `compare_grid.py` height-aligned composites; **PM**: Lower LCD modular refactor — `lower_lcd.py` mirrors upper pattern (JapaneseDisplay + EnglishDisplay placeholder + LowerDisplay manager, shared mode_cycler from upper, `set_state(state)` API); `AppState.update_skip_progress` (state-machine logic moved off renderer); `pygame.display.flip()` lifted into `app.run()`; single-line centering bug fix (`min(per_line, num_stops)`); legacy `display.py` deleted; ENGLISH disabled in lower's mode_displays dict, falls back to Japanese until impl; `constants.py` pruned 17 dangling constants, new "shared-across-modules only" rule in preferences.md; `LOWER_DISPLAY_UPDATE.md` created as sibling doc; **late PM**: 2-line English station support (成田空港 → "Narita Airport / Terminal 1", line 1 left-aligned, line 2 right-aligned, ascent-tight stacking, settled at 42pt with line 1 caps at y=37 — 2px below band_bottom_y=35 boundary, fully compliant with containment principle); fixed a clear-rect clobbering bug in EnglishDisplay.draw_station via `band_bottom_y=35` clamp (top), then added bottom clamp to UPPER_HEIGHT to match; **debug-grid mode** added to upper_lcd.py — `DEBUG_GRID` toggle + `_DEBUG_COLORS` dict (manifest) + `_bg(region, default=...)` helper, all clear rects migrated, `--debug-grid` CLI flag in preview_display.py paints each region in distinctive tint to surface overlap/under-clear/clipping bugs; **Element clear-bg convention established** (in UPPER_DISPLAY_UPDATE.md, NOT notes.md — display-specific stays in display docs): every changeable element clears its **full territory** (not glyph footprint) as draw method's first step; all 3 modes share the same territory per element; dest unified across modes at `(0, 50, 180, UPPER_HEIGHT-50)`; train_type intentionally absent from `_DEBUG_COLORS` (WHITE_BG already distinct); **Containment principle** (refined into a unified framework): every region has a declared confinement; ANY drawing the region does (clear rect bg, glyphs, decorations) must stay inside — clear rect is not special. Two checks: D1 (surface containment, `blit_y ≥ confinement.top`, cheap analytical) is a useful pre-check; D2 (visible-pixel containment, requires probe) is the actual rule. Pygame font surfaces have ~10–15px leading for big fonts → D1 can fail while D2 passes (leading absorbs overshoot); pixel-perfect IRL font sizing requires D2 over D1. Probing gotcha: prefix text glyphs in station's overlapping x-range can masquerade as station glyphs — isolate with short-prefix scenarios or probe at exclusive-x ranges (x=522–570, 650–686 for station). Final audit: all 4 station modes comply with D2; clear rects all clamped to y≥35 (Japanese/Furigana got the same `_clear_rect` clamp pattern English already had, closing a 1px bg leak at y=34 in Kanji `name_y` calc); **English dest 2-line layout** (Yamanote "Shinagawa&\nTōkyō", Sobu "Narita Airport\nTerminal 1"): font_dest 25→24pt, lines left-aligned at x=5 with "for" label, full-height pitch (not ascent pitch — descender/cap collision otherwise), top_pad=5 between "for" visible bottom and line 1; lesson: at tight vertical budgets, full-height pitch + smaller top_pad beats ascent pitch + bigger top_pad because descenders overlap caps with ascent pitch when line 1 contains "p"/"y"/etc; **Region Map** lives as comment block at top of `upper_lcd.py`, co-located with `_DEBUG_COLORS` — per-train-model, in code, not in markdown; visual-adjust skill updated (debug-grid section, replaced "Clear rects per mode" pitfall, fixed stale `MOCK_STOPS in preview script` → `audio/_mock/main/route.json`); **evening**: Sobu Rapid 1217F end-to-end (Tokyo→Narita Airport, 19 stops, 28 PA + 16 STA + 4 archived); `/split-audio` skill replaces deleted `AUDIO_SPLITTING_WORKFLOW.md`; canonical PA naming `{prev}-dep`/`{this}-arr`, STA naming `{station}_{platform}_{song}` (station-first for visual scan, underscore between fields, hyphens within), Japanese-to-slug = Hepburn-no-macrons; **`sta_cut` is from START** not end (old doc was wrong); **`_*` folder convention** for preserved-but-not-shipped (`_archive/`, `_mock/`); per-source splitter scripts with per-segment `dest` tag (`"op"`/`"archive"`) for automatic archive routing; **stations IRL with no melody** (e.g. 千葉) get same treatment as termini (omit `sta`/`sta_cut`); audio file = atomic unit, recordings preserved even if route doesn't ref them, no metadata sidecar (filename IS the store, glob for re-use); variants explicitly out of scope; `release.ps1` commit classifier excludes `audio/_*/` + `data/_*` from "Data" bucket; `preferences.md` adds: pragmatic-over-perfect, discuss-first reinforced for data work, per-source ad-hoc scripts
- [2026-04-24](2026-04-24.md) — LowerDisplay: rendering collapsed to single global-index frame (fixes long-route inner-dot drift); single-PA skip-target flush; `PASimulator(preview=True)` + `audio/mock/main/route.json`; **then same day**: `curr_stop_disp` field eliminated → derived `cursor_pos` property, `increment_current_stop_display` deleted, off-window pointer guard in `draw_ptr`, mock-route passing-station times normalized; **also same day**: data audit + cleanup sweep — passing-station rule tightened (no `sta`/`sta_cut`/`time`); `validate_data.py` NEW; nanbu→nambu rename; takasaki/831M deleted; `diagram`/`skip` dead-field removal; 27 translations added; `contrast_color` spec-optional; **end of day**: v0.5.2 cut on GitHub; `/build` + `/commit` skills created; `release.ps1` reworked (Program/Data path-classifier, prose-notes honor, version metadata embedding); audio-junction pattern for testing without copying exe to working tree; `.gitignore` root-anchoring (`build/` was swallowing `.claude/skills/build/`); `.github/workflows/release.yml` removed as unused; **late evening**: README overhaul (Install→Download, manual-control paragraph, yellow-square framing), `ROUTES.md` extracted as language-agnostic shared doc linked from all three READMEs' top bars, `README.zh-CN.md` added, `/readme` skill codifies Chinese-phrasing preferences (use 程式/程序 for app behavior, not 模擬器/模拟器; don't literal-translate EN→ZH; user-POV over dev-POV) + terminology baseline table; GH Release v0.5.2 asset swapped from 65MB (no-audio) to 629MB (full bundle)
- [2026-04-23](2026-04-23.md) — Station code badge (3-letter code): `data/stations.json` introduced; Yamanote `sta_code` backfill; JR East 3-letter catalog is finite (22 stations); `ModeCycler.enabled` (not `paused`) gotcha; release.ps1 + build/SKILL.md fixed so feature assets ship; PowerShell preference baked into review skills
- [2026-04-21](2026-04-21.md) — Docs deduplication, single-source-of-truth routing in session-recap
- [2026-03-31](2026-03-31.md) — Ralph loop skills implementation, key learning about agent roles
- [2026-03-27](2026-03-27.md) — CLAUDE.md reorganization, memory directory setup
