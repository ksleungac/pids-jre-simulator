# Memory Index

Long-term curated memories. One-line pointers — daily logs hold the detail.

---

- [2026-07-18 (block 2) — release-prep UX polish: band press-flash + pause/play toggle, zh_HK clean default, single-diagram preselect; rebuild+zip+release to upload boundary](2026-07-18.md) — Post-build smoke test → 4 polish fixes, all source-side. **Band control cluster press-flash**: the live-drive `[pause][save][home]` had no press feedback (setup band already flashed via `press_transition`); added `status_band.press_flash` (pure yellow flash, no beat). **Pause → play/pause TOGGLE** (user: "should say continue if i paused"): paused shows 繼續/Play drawn NORMAL not persistent-yellow (so the flash reads BOTH directions — an always-yellow paused button shows no flash-change). English "Resume"(61)/"Continue"(70) OVERFLOW the fixed 68px band square (a single word can't wrap) → en resume label = shorter "Play", zh = 繼續 (primary locale gets the right word). **Clean-install default lang → zh_HK**: flipped `detect_default_lang`'s non-Chinese fallback en→zh_HK (nearly over-reached ripping out OS detection; user "OS dependent also ok" → one-line flip; zh_CN still detected, `DEFAULT_LANG="en"` stays the translation fallback). **Single-diagram C07AF preselect**: one variant → `selected=0` (yellow, 設定 armed) so the user confirms directly. Codified APP.md § Language + § status band. v0.6.0 release-ready (user's standard); rebuild+zip+`/release` to the upload boundary.
- [2026-07-18 — v0.6.0 rebuild smoke-test bug: Chūō 8-station lock + missing arrow → cursor_pos keying; through-service reboot-timer redesign](2026-07-18.md) — Rebuilt v0.6.0; smoke test found a live Chūō bug (departing 新宿: 8-station view locked + no train arrow). One root: on departure `_advance_to_next_stop` jumps `curr_stop` past the passing stations (代々木/千駄ヶ谷/信濃町) to the next PA stop 四ツ谷(25) while visual `cursor_pos` lags at 22 — but both `_get_window` AND `_should_lock_to_eight` keyed on `curr_stop`, so the lock fired early (`32-25=7≤7` → FULL dropped) and the window snapped to `[24..31]` with cursor 22 off the left edge (`draw_ptr local_disp<0` → suppressed). Fix: **both key on `cursor_pos`** → FULL persists until visually 7 from end, arrow always in-window (916 states verified). User's generalized invariant: **any position-locked view MUST always show the pointer → every stop-count/window/lock is visual (`cursor_pos`)**, scoped to the ACTIVE frame (shared junctions → parked train is index 0 of the incoming frame, no icon-less active frame). I mis-drafted a "leading-frame no-icon" exception unverified → user corrected twice (`§ Verify before claiming`). **Reboot-timer redesign**: through-service swap hold was `sum(slot durations)` ("exhaust all pages", ~18–30s = too long after arriving 千葉) → fixed `_SWAP_HOLD_DURATION` (12s by feel); mental model = signal the boundary-stop + let junction exchange info read (STOPPING edge force-switches to TRANSFER). Killed `_swap_rotation_dur` + the mid-page-cut TODO caveat. **Review+fix**: caught 2 of my own EDIT-CONTRACT violations (dated regression note + change-log parenthetical), a TODO `3.0`-vs-12.0 drift, a missing test; cycle 2 caught the new T1 test's non-ASCII PASS line crashing the cp1252 build-gate pipe (green only under my `PYTHONUTF8=1`) → stdout-reconfigure guard. Added `test_eight_window.py` (pointer-always-in-window + Chūō anchors).
- [2026-07-16 (4th block) — release-prep /review+fix over new-since-v0.5.4 (setup_tims focus); 6-agent scan; fixer-confidence-gating + vibe-check-fold-in codified](2026-07-16.md) — First real run of the blocks-2/3 review mechanism as a **release-prep whole-code scan** (~13.7k lines/40 files since v0.5.4). Fanned out **6 parallel opus reviewers** (review-dirty lenses + derivation-bypass + integration-residue #13 + vibe-check smells; Lens 4 test-integrity exempted). Two mid-turn steers → codified into review+fix: **fix-confidence gating** (surface built by other sessions → lack design context → obvious-safe applies, needs-context defers to TODO, NOT file-ownership) + **release-prep folds in `/vibe-check`** (codebase-mess lens vs review-dirty's diff-orientation); E235-0 + auto_input logged-not-fixed per "well-tested" steer. **Zero blocking** → one cycle. Applied **17 obvious-safe fixes/9 files** (palette-drift derivations that CLOSED 2 pre-existing deferred-findings; `ocr_setting.run_consent` `_load_state()` behavioral fix; dead-code; `NEXT_FLASH_MS`; feature-graduation-stale "DRAFT/not-wired" docstrings across every setup_tims screen). **18 deferred → TODO**, incl. 3 real out-of-focus bugs (e235_0 passing-skip `None+int`; `driver.py:941` bg-thread single-writer break; `ocr.py:789` unscaled `DIGIT_MIN_W` at 1080p). Verified py_compile + T1 4/4 + T4 1/1 + linters clean.
- [2026-07-16 (3rd block) — language-picker detective → staleness-at-integration root; T4 clean-frame test; review-lens rewrite (integration-residue + test-integrity + INTEGRATION scope)](2026-07-16.md) — Third v0.6.0-incident detective. Root is the OPPOSITE family from re-entry: the picker was **born right, went stale** — legit pre-TIMS, made redundant when the TIMS home grew language knobs, universally redundant once `147a56c` flipped TIMS to default, but its trigger (`if lang not in SUPPORTED_LANGS`, fires only on absent settings.json) never runs in dev so the staleness was invisible. **Axis-blindness** = the mechanism: the flip commit applied the identical redundancy logic to the OOBE gate one line below (on the flag axis it was flipping) and missed the picker (on the orthogonal persisted-state axis). User caught me re-inventing a tier boundary: **fixture ≠ tier** — T4 IS integration (same scope as T3); the settings fixture (absent/valid/corrupt) is an orthogonal axis, T4's name just flags the state-absent one. Built `i18n.resolve_language` (pure/screen-free extraction = the regression guard) + T1 `test_resolve_language` + T4 `test_clean_frame_startup` (fixture-parametrized + picker-not-importable guard). Review mechanism (user: "next app route switch needs a stale-code review"): integration-residue smell (`review-dirty` Lens 2 #13 + `vibe-check` #13), **Lens 4 test-integrity** (test-not-stale/feature-has-test/code-testable), **INTEGRATION scope** (auto-escalate on flow-routing change) + §6 axis-blindness fold + `principles § Engineering rigor` "testability is a precondition".
- [2026-07-16 (2nd block) — re-entry PASSING detective → incomplete-spec root; auto-driver T1 tests + T3 plan; layer-vocab doc-drift fix](2026-07-16.md) — The `or badge=="PASSING"` clause was born WITH a matching wrong spec (the README re-entry table drifted from the canonical PASSING≡MOVING truth table) → a "code matches doc" coherence test would've PASSED; **root = incomplete spec**. Built 3 green T1 tests locking it (`test_reentry_target` incl. the incident row `PASSING@<30,parked→None`, `test_inferred_state`, `test_detector_update_seq`); recognized PASSING≡MOVING as a **cross-cutting invariant** (site-list checklist in `_tests/README`). User's conviction: the test that matters is **drive-record replay (T3)**, blocked on a recording; T1 truth-tables clarify logic but are expensive to spec by discussion — split T3 into a buildable harness (4a) + blocked corpus (4b). Fixed a preloaded `conventions.md` layer binding that mislabelled badge as "Layer 2" (canonical = L3 input) → rewritten to a pointer, "preloaded bindings point, never copy" (doc-internal canonical-source duplication). Ditched a single-attribute re-entry hardening as a rabbit hole.
- [2026-07-16 — v0.6.0 build attempt aborted; 3 incidents + interval 3s, all fixed, flagged for detective work](2026-07-16.md) — Two smoke-test passes on the v0.6.0 exe surfaced three ship-adjacent bugs; release deferred ≥2 days for comprehensive testing, all tracked in a TODO block for tomorrow. **(1) Clean-state first-run** — fresh exe showed the stale pre-TIMS grey `LanguagePicker`; invisible in dev because `settings.json` is always populated → the branch never runs (deployment-frame sibling: reasoning from dev state, not the user's blank one). Removed the picker entirely; first-run → `detect_default_lang()` + TIMS home knobs persist → `critical_lessons §6`. **(2) Version hardcode drift** — exe showed "054" while PE metadata was 0.6.0: `home.py` rendered a hand-maintained `VERSION="0.5.4"`, bypassing `app_version()`'s "no constant" CONTRACT. Slipped because scanners each target ONE primitive (none flag a version literal) + review lenses see only the dirty diff (a committed constant never appears). Added `app_paths.display_version()` (frozen→PE metadata, dev→pyproject), home holds no constant; sensor queued. **(3) Re-entry ate a departure PA** — start-from-middle: a PASSING badge while parked at 1C woke re-entry → silent-advance + suppressed the real `FIRE_DEPARTURE`. I mis-framed PASSING as a misread twice + over-reached with a `synced` flag; user taught **PASSING ≡ MOVING, 900m arrival the only difference**, speed≥30 silent-advance correct-by-design → fix collapsed to a one-line deletion enforcing the existing invariant (trace-verified only, needs live drive). Over-reach → `principles § Surgical Changes` "enforce existing state-machine model before adding state". Interval default 5s→3s all sites.
- [2026-07-15 (4th block) — OCR-band polish + E235-0 click-to-jump + always-on-top regression + big-4 scoping](2026-07-15.md) — Band (`status_band.py`): msg font 13→17; limit cyan flipped RESTING→CHANGE cue (`driver` stamps `limit_change_ts` like `last_fire`); `制限` identity LABEL added (user vetoed red-ink digits — "2 label, no need red ink"); Save-button confirm (`sim.last_save` phase, no filename). **"Labels not variables"** miss — gated the label behind `not no_rd` so it vanished from setup; static chrome renders in ALL states → APP.md. **E235-0 click-to-jump was unimplemented** (both `hit_test` stubs `None`): circular = nearest-dot within radius (JY-keyed; horseshoe overrides index-keyed), 5-station = per-slot AABB, `vis[k]`=stop idx; headless-verified 30/30 + 5/5 → DISPLAY_E235. **Always-on-top regressed** — monolith re-pinned after every `set_mode`, modular rewrite kept it only in the sim → TIMS flow non-topmost; fixed via ONE `window_utils.install_topmost_hook()` seam (not ~18 inline pins) → APP.md. Big-4 home 設定+行車記錄 → silver/disabled+inert (`INACTIVE_ACTIONS`), out of scope. `_joban` still in picker — the `_` prefix hid the ZIP not the PICKER (only `_mock` special-cased); both scanners now skip `_*` → conventions. Auto-launch scoped (cheap preview yes, full-app-flow no) → principles.
- [2026-07-15 (3rd block) — pre-release scope check + validator staleness sweep](2026-07-15.md) — v0.6.0 pre-build audit (release runs off-machine). Theme: the CHECKERS drifted, not the code. Fixed `check_deps` false-blocking its own `/build` gate (win32api alias + preview-scope); added **certifi** to deps (dev-transitive → vanishes in `--no-dev` → silent frozen-exe SSL degrade; §3 flavor, caught pre-ship). Hardened `validate_data.py` for TIMS+E235 field growth: `translations_app.json` locale+placeholder parity, `model`/`line_code`/color checks, full-scan skips `_*` (shipped-data gate) / `--route` still validates. Dropped incomplete `joban`→`_joban` (was picker-selectable + degraded in exe). Build `$shipExclude` += setup_tims/tims_references. line_icons committed (37/37).
- [2026-07-15 (2nd block) — cleanup arc: dead-code sweep + APP.md + README assets](2026-07-15.md) — Post-compaction wrap. Closed deferred items 1/4 (button blockers resolved by graduation; no-press-feedback affordances exempted as non-buttons). **Import-graph-audited FIRST** (most zero-importer files are legit `uv run` tools, not dead), then swept graduation residue: −381-line OCR-debug-panel strip in `driver.py` (report path kept) + green-tutorial-shell + 11 font-eval scripts + 8 TIMS-ref images → `tims_references/`. Lesson: a "retained for X" NOTE goes stale when X is also removed — sweep the cluster, not the head. **APP.md born** as the app-level-spec home (user: "no place for app-level specs") — dissolved `WIP_setup_redesign.md` in, 7 cross-refs repointed, registered. Promoted E235-0 5-station (東京) + 2 TIMS pages to `assets/07-09` + a committed regen helper, wired 3 READMEs. Strip came back /review+fix-clean.

- [2026-07-15 — TIMS button-interaction coherence pass + i18n font blocker + review](2026-07-15.md) — Graduated the setup flow's **interaction contract** (yellow=press · beat=navigate · flash=actionable/current · silver=inactive) the way the style-warehouse earlier graduated visual tokens; centralized silver as `chrome.DISABLED` (killed a 4-idiom drift). OCR-settings gained 設定/返回 working-copy (save/cancel-revert). Fixed the packaged-only **i18n blocker** (`home.run()` never synced `ACTIVE_LANG`→`current_lang` → zh_CN tofu). Audit-blindspot lesson: state-inventory ≠ completeness (missed an *absent* button). Codified: conventions § UI-code-style (model + `chrome.DISABLED` + `ACTIVE_LANG`-sync); principles § announce-self-launched-processes.

- [2026-07-11 — TIMS tutorial port + band-Home-everywhere + setup parity/backlog](2026-07-11.md) — Ported legacy `tutorial.py` into the TIMS flow as `BasicTutorial(Tutorial)` (inherit step machine, override ONLY layout → vertical fit); design landed as a **select-menu → tutorial**, NOT the WIP tab-shell. **Band Home = straight-home + beat on EVERY page** (`"home"` sentinel bubbles up); `ocr_setting` fixed — the truthy `"home"` string would've mis-read as consent-accepted (split before the `if ensure_consent()` check). **OOBE** in `--tims` = 教學-card flash until first visit, not forced tutorial. Verified **TIMS parity with classic setup**; the "OCR-launch crash" was the retired `draw_debug_panel` (live uses `status_band.render`). Model on C07AA → repurposed blank 月台 row → 列車型號 (no IRL ref, temporary). `ACTIVE_LANG` unify DEFERRED (114 sites, masked). Codified: principles § don't-gate-cheap-verification; conventions § `\n`-tofu-in-lowres; readme § zh-HK full-width punctuation.
- [2026-07-11 (2nd block) — `--tims`→default flip + i18n graduation + progress-strip wrap](2026-07-11.md) — **TIMS is now the DEFAULT flow**; flag inverted to `--classic` (opts into legacy, keeps its forced-OOBE reachable). **OOBE flash carries onto the 2nd-page 基本操作 button** — flip to `oobe_completed` deferred to basic-tutorial *entry* so the flash guides home 教學 → 基本操作 → walkthrough. **Version update-hint made REAL** — dropped the `PREVIEW_FORCE_HINT` stub that would've false-flashed a fake `0.6.0` for every user once TIMS shipped default; now `update_check.get_update()`-driven. **Last hardcoded-zh_HK draft strings graduated to i18n** (C07AA table field labels + OCR launch-cluster + consent accept + settings title) — found by scanning text-render calls for literals bypassing `i18n.t`; table `label_font` switched pinned-JP → active-locale (else zh_CN Simplified tofu). Green `NOTIF_TEXT` is INTENDED fixed-JP (annotated). **Progress-strip labels wrap ≤2 lines** so long EN phase names stop overlapping the fixed 97px column (`SCREEN_H` derived → window grows, panel untouched). Removed 8 dead `status_band` placeholders. Codified: conventions § fixed-column-strip-wrap-EN; readme § zh-HK-may-hug-JP-kanji-for-TIMS-chrome.

- [2026-07-06 block 2 — OCR button wiring + grouped-button rule + band no-readings + C07AF fixes](2026-07-06.md) — Wired the pa OCR cluster → consent/settings; **consent SPLIT from settings** (gates LAUNCH 起動 only; `OCR設定` opens settings direct). Ported the **FULL consent tutorial** the earlier block cut (flow steps 2–4 + journey diagram: Fuji/Suica-train/markers) — "full thing, don't cut", primitives verbatim. **Band no-readings state** (all setup pages, pre-OCR): green hint + dim `--` + no flash; only placeholder VALUES changed. C07AF: **filter diagrams to variants stopping at the chosen start station**; 種別 layout by length (distribute 2–3 / cram 4 / compress 6–7) — type lengths SAMPLED from data not a screenshot. New rule: **grouped buttons (positional OR functional) share size+design** → conventions (route bar 返回/設定, 列車型號→cluster size). **OCR launch CRASHES** in live `sim.run()` (init/start clean headless; loop-time, needs traceback) — deferred. Fixed sweep_todo.py cp1252. Committed + pushed.

- [2026-07-06 OCR consent/settings screen + pa OCR launch cluster + TIMS chrome style warehouse](2026-07-06.md) — pa C07AA: built the reserved **OCR launch cluster** (3 equal bevel buttons, 1-over-2 right-aligned, white **L-notch frame following the button silhouette**; `手動報站起動` replaced orphaned `確認`; `列車型號` → row2 line; 2-line center-natural crammed labels — NOT justify). New **`ocr_setting.py`** (consent + steppers), ported from legacy `_draw_ocr_disclaimer_panel`: full-page (no popup) gothic AA-off; consent = ONE-TIME GATE (`ensure_consent`→`settings.json "ocr_consent"`) in front of both OCR entry points, read-only from Tutorials later; `明白` grey-locked→FLASHES at scroll-bottom; pa-button wiring deferred. **TIMS chrome style warehouse**: user tired of re-styling per screen → `/third-man` **design-consult** (new skill pathway) KILLED an over-engineered screen registry + `preset()` helper and caught the palette had **drifted** (near-black `(8,10,14)`↔`(10,13,18)`, ink `(236,241,246)`↔`(232,238,245)`, `_BAR_T`≡`_FOOT_T`) → PALETTE + `BTN_BAR/LABEL/ACTION/STEP` + `FONT_PX` in `chrome.py` (widgets=primitives), all 6 screens migrated, drift fixed. "No false uniformity" = layout, orthogonal to design tokens → conventions. Nothing committed.

- [2026-06-28 setup_tims --tims launch bridge + 番台選択 model picker + i18n graduation](2026-06-28.md) — `main.py --tims` launches `setup_tims` MANUAL-mode **side-by-side** with legacy `setup.py` (no hard swap → keeps OCR-auto-PA until OCR-launch lands): home→pa_setting→**確認/起動**→`_build_config` (config shaped like `setup.run()`)→`PASimulator`+`jump_to_stop`; variants now carry `path`/`model`; **start resolved by NAME** → closed the v0-index finding. i18n: per-module `*_BY_LANG` dicts → `translations_app.json` + new `i18n.t(lang=)`; a **code-rewiring subagent broke 5 modules + false-reported "imports OK"** → principles "subagents = read-only fan-out, NOT cross-file code surgery"; zh_CN user-confirmed (交路选择/设定/终到站…). **Model picker** (X00AA 番台選択): IRL 番線→列車型號, grid = registry-built (selectable) + hardcoded grayed roadmap (E231-500, E233-0/1000/5000), current FLASHES, no confirm/back, session-persistent override; **grayed = silver palette not dark scrim** → conventions. Nothing committed.

- [2026-06-27 setup_tims/ package promotion + Noto .otf-Thin + C07AA design](2026-06-27.md) — promoted the 3 TIMS-chrome drafts → **`setup_tims/` production package** (band/home split + `chrome.py` reuse layer: `title_row` was 3× copy-paste, `blit_lowres` promoted from band-private), side-by-side with `setup.py`; per `/third-man`. Launch bridge (lossy `run_on` dict → `finalize_route`) **P1-deferred**, not wired to `main.py`. Font: `.otf`-only blocker resolved via **Subset OTF Thin** (originals were Thin not Regular — a subagent picked Regular, I propagated unchecked, user caught the stroke regression → principles "verify subagent output"); ArkPixel removed (dead). C07AA: 自動放送始発起動 = launch action (→ live LCD) not toggle, manual = default un-armed launch, OCR tuning behind 自動放送設定, "train type" = train MODEL/skin. Passing-station start-grid fix (stopping-only, DATA_FORMAT predicate). review+fix 0 blocking (cross-variant index → carry `start_name`; 3 findings deferred). Codified: parallel-subagent dispatch + flexible-primitive design (principles.md). WIP doc reconciled.
- [2026-06-27 E235-0 5-station band fill — curve-following feathered reveal](2026-06-27.md) — green fill over the band reworked: hard `draw.polygon` erase (sharp diagonal cut) → **feathered reveal mask** multiplied into the band via `BLEND_RGBA_MULT` (white-alpha mask scales only alpha, RGB green preserved); fixed sweep angle (`_BAND_FILL_ANGLE`, removed) → **arc-length parameterization along the band centerline** (precompute in `__init__` via `_build_fill_centerline`: retired `_TUNEABLES_ARC` p0/p6 caps + live markers m0–m4, densified by the existing CR helper), corridor quads stamped perpendicular to the local tangent so the front stays diagonal to the curve at every height. Trailing feather (0→nothing,1→full). Tuned: feather 18, ease-in (`_BAND_FILL_EASE_POWER`=2.0, "accelerate to end"), 1.5s. review+fix 0 blocking (perf: hoisted reveal surface + samples 80→40). Deferred: `_TUNEABLES_ARC` split-personality (live caps in "retired" dict) → P5 editor cleanup.
- [2026-06-26 font hunt — PixelMplus rejected, MS Gothic = non-shippable gold standard](2026-06-26.md) — small-size pixel crispness = embedded bitmap **STRIKES**, not render size / supersampling. PixelMplus10/12 downloaded + tested → **rejected** (lateral detail vs Ark's 12px grid; JA-only — zh_HK 97.3% tofus 錄, zh_CN 61.5%). MS Gothic confirmed best look but **non-shippable** (MS license + JIS zh_CN tofu) → reference bar: ships ~13 strikes (px 10–22, detail grows per size) then outline from 23; Ark has 0 strikes (single 12px) so 14–22px is rescaled-12 with no added strokes, supersample can't add them. Free-font goal reframed = **multi-strike bitmap face** → Shinonome/jiskan16 (strikes 12/14/16, PD) promoted try-first, benchmark TODO. Tooling: strike-vs-outline probe = AA-ON on opaque bg + count colours (AA-no-bg hides coverage in alpha). WIP § Font + TODO updated.
- [2026-06-24 TIMS setup-screen redesign — page-1 draft + pixel button/text primitive](2026-06-24.md) — TIMS-cab setup look (glossy bevel buttons + low-res pixel text): lang knobs (yellow=pressed-not-selected) + 4 flush action cards + "Version ０５４"; font = Ark Pixel 12px **monospaced** per-locale, ONE shippable build (IRL TIMS monospaced; MS Gothic out — license + JIS no-Simplified); **pixel = small-size aesthetic** (big-button chunk = oversizing → shrink not smooth); primitive gained `align`/`pad` + `lowres_fit_k`. Design + 6-step graduation plan in `WIP_setup_redesign.md` (supersedes TODO single-face claim). OPEN: full-width numeral gap still too wide at 80% squeeze.
- [2026-06-23 PM calibration-editor graduation Phase 2 (P1/P3/P5-upper/P2)](2026-06-23.md) — codified the module-level `_TUNEABLES_*` editor standard in conventions.md (P1); wired e235_0 badge + PA-hint as region-rect tuneables + registered (P5-upper, byte-identical verified — badge clip rect spans framed square + 12px code_3 band so `badge_h` derives `rect.h − band`); authored `/calibration-editor` skill (P2); P3 lazy-import verified. **Route bar DEFERRED** — needs editor lower-view dispatch (full-route vs 5-station share the lower region) → TODO; sole item gating WIP-doc dissolution. review+fix clean (1 cosmetic comment).
- [2026-06-23 git sync + reconcile stashed E235-0 fix onto master](2026-06-23.md) — feat branch was already FF-merged (~06-20/21); WIP merge-plan + TODO:24 were stale ("not merged / work on branch") → de-staled, branch deleted, policy now "E235-0 fixes on master". Recovered the uncommitted 2026-06-16 Osaki circular-wrap fix (+ arrow staggered-fade + `a0_*` ease tuneables) from stash, popped clean (open-horseshoe is a separate class, no overlap). Verified wrap via direct `_visible_stop_indices` (text ground-truth, not PNG); review+fix + fresh vibe-check both clean.
- [2026-06-21 graduation merge + per-route train-model selection](2026-06-21.md) — FF-merged E235-0 + calibration editor → master (b6aedb8); WIP graduation Phase 1 done, Phase 2 (editor standard) pending. Found E235-0 had zero production wiring (app.py hardcoded e235_1000) → built per-route model selection: graduated the dormant `displays/train_models` registry (record `TRAIN_MODELS`, `get_train_model`, constructor-arg `PASimulator(model=)`, **static imports so PyInstaller needs no hidden-import**), `model` kept OUT of route closure (construction-time), route.json optional `model` (yamanote→e235_0). Setup gained a per-route expanding dropdown w/ magenta-colorkey train front-view icons (`data/train_icons/`, `transform.scale`=nearest keeps key clean), blended folded state, content-derived width. Font reverts (Helvetica/Frutiger → `i18n.font`) → codified **multi-locale chrome coherence** (locale face + content-width + ascent line-spacing) in conventions.md. /third-man before coding; review+fix staged the runtime icons.
- [2026-06-20 E235-0 open-horseshoe full-route (non-Yamanote)](2026-06-20.md) — replaced the E235-1000 linear fallback with `OpenRouteFullRouteDisplay`: the Yamanote racetrack with **one cap dropped** (horseshoe — open-left terminals + right fold), subclasses circular + stop-index keyed. `⌈N/2⌉` split right-aligned at the fold; **no sweep** (fit-to-rows, best-effort); band grays behind `cursor_pos` (clip-redraw of `_draw_track` gray). E235-1000 skip carry-over: passing stations (empty `pa` / `time:None`) → chevrons not numbers, `_ahead_indices` skips them — also dodges a `None+int` crash the Yamanote-only circular never hit; cascade pointer unchanged (user: "moves the same"). DISPLAY_E235.md new section + 4 stale "linear fallback" fixes. Scope call: don't fine-tune best-effort — build E233-0/Chūō for real non-Yamanote fidelity.
- [2026-06-16 E235-0 inline transfer panel — algorithm + docs](2026-06-16.md) — built the 5-station view's inline transfer panel (2nd transfer surface, distinct from the inherited horizontal slot): threshold-T pair-or-stack (N≥6, Yamanote N∈{1,2,3,6,7,8,9} → clean 6-cutoff), shared col-2 anchor, curved right edge (3 control pts), greedy pair + monotone repair; shinkansen solo, wrap at ･ on a narrower *fixed* boundary (北海道｜上越 cut, decoupled from curve). Lesson (friction→codified): misread the shinkansen wrap off the reference PNG twice — precise structural facts (counts/cuts) must come from a faithful headless replication (`_sim_panel_layout.py`, reads live tuneables), not photo-reading → conventions.md § Tooling. Data: keihin full-width （）IRL-special (don't normalize), others half-width, shinkansen ･ → DATA_FORMAT.md. /review+fix caught untracked runtime font `ShinGoPr6N-Light.otf` (staged) + dead `right_edge` param. DISPLAY_E235.md new 5-station section + 12/12 regression corpus.
- [2026-06-15 E235-0 5-station passing state + cross-model leak fixes](2026-06-15.md) — built APPROACHING marker (dark-yellow circle + cumulative digit + ring-stamp-halo sweeping chevron + perpendicular masked band fill); editor draw-state-gated handles + decimal float nudge. Key lesson: e235_0 *reuses* e235_1000's LowerDisplay/transfer, so partial override leaks parent bindings — ENGLISH-mode renderers (linear full-route + 8-station) + the transfer's `UPPER_HEIGHT` constant both leaked; fix = same-instance english renderers + `upper_height` param + `surf.get_height()` → conventions.md "Forking a sibling-model renderer". Cumulative minutes double-counted curr_stop leg when stopped (base 0 fix, caught by /review+fix). Red contrast_color [101,20,5] from maxresdefault but probe unreliable (JPEG clips saturated red) — re-probe TODO. Ran with parallel background Sonnet workers.
- [2026-06-12 PM 5-station pentagon → free polygon + editor handles](2026-06-12.md) — `/third-man` unlocked it: the home-plate-plus-knobs pentagon (`m0_a/sl/ra/pt` + affine fit) kept failing because the marker is single-slot/fixed-orientation (`k==0`, view re-centres) — it was modeling curve-following it doesn't have while failing one shape. Rebuilt `_draw_pentagon` as a **free polygon** (drag vertices `v0..v4`; breath/halo/dot kept); user confirmed firm pattern → conventions.md. Editor: `v` prefix + `select_prefixes`; handles → small two-tone open crosshairs (contrast on any backdrop, less obstructive). Misread "the dot is obstructing" as the rendered dot (built+reverted a `D` hide-toggle); meant the handle → principles.md (confirm ambiguous referent, don't anchor on own recent work). Digit-in-circle centering → ink bounds not surface box (`font.metrics`). 5-station view still WIP.
- [2026-06-12 E235-0 5-station badge unify + fixed font face](2026-06-12.md) — 5-station `_draw_jy_badge` looked wrong not from being hand-drawn but from mis-driving the shared `draw_station_code_badge` (`ring_black=0` + wrong fonts ShinGo/Helvetica vs Frutiger); fixed by scaling upper-LCD params by `b/68`. Wikipedia `Template:JRSN` chased + dismissed: it's pure CSS (no artwork, browser font ≠ Frutiger), *lower* fidelity than our badge. User rule → hardcoded the face inside the helper (`_badge_font(size)`), removed all font params; size stays caller-controlled (8-station 0.36/0.5 ratio vs upper 0.27/0.32 — no single ratio works). Also: pentagon apex angle now tuneable (`m0_a`). conventions.md + principles.md (read referenced setup before theorizing risk) + DISPLAY_E235.md.
- [2026-06-11 deferred-findings cleanup](2026-06-11.md) — easiest-first sweep of deferred review findings: paused-loop 0.5s poll, no-driver click hint, cross-reject → JSONL `cross_reject` event (via detector events list — detector can't reach `log_file`), `_crop_cell` 3-way fork → `ocr.py crop_cell` (shared `_crop_hud_cell` body; driver's numpy lookalike ≠ fork). H1 frame-dump removed by decision (spam = debugging feature). H2 commit-gate left for dedicated session.
- [2026-06-11 PM auto-PA arrival lead bump](2026-06-11.md) — per-stop long-approach lead bump for the OCR auto-driver: arrival PA (`pa[1]`) ≥40s → +400m (900→1300), auto-derived from audio headers (soundfile), no route.json authoring. Data-walked 177 PAs first; flat-not-scaled because long PA ↔ slow approach self-compensates in distance (Atami 128s/crawl ≈ Shinjuku 59s in meters) — a z-score/σ model would double-count. Threshold + bump fall out of the lead math (900m ≈ 40s cruise; 1300m ≈ 56s ≈ manual Shinjuku). Time-based `dist/speed≤pa_dur` model parked as the real upgrade. Couldn't live-test (game off); offline-verified on 1217F. **Then recap-workflow correction → hard rule: never prompt to commit** (commit = seal not goal; pre-flight check #4 artifact-loophole) → principles.md § Collaboration + redlines.md.
- [2026-06-11 Serena setup + Fable-reviewed cleanup](2026-06-11.md) — set up Serena MCP (project `.mcp.json`, uvx-from-git, gitignored; PowerShell `--`/quote mangling → write `.mcp.json` directly). Fable review of `a5b4f1d` caught 6 reals the 06-10 Opus loop missed (confounders: sharper prompt + final artifact + it executed). Fixed: continuity band-aid `continuity[2]=1` (had SHIPPED despite the same-day "fix upstream not draw-side" rule) → unified global-terminus predicate symmetric with 8-station, `_frame_continues` removed; `_select_frame` dropped (dead duplicate of `_natural_frame` → single frame-picker); validate_data UTF-8; logo green dedup; SVG → `lcd_references/`; circular-route comment. conventions.md ×2 (Serena-over-grep, mcp-add PowerShell gotcha)
- [2026-06-10 through-service display frames](2026-06-10.md) — E235-1000 frame swap (Sōbu 1217F): `frames` schema + loader closure, `_FrameWindowMixin` clips renderers to active-frame slice (sim↔display↔frame-local rebasing), arm/hold/fire swap lag, full-screen JR-logo restart (flattened-bezier vector polygon, no asset). Continuity made framing-aware (global-vs-slice predicate, NOT draw-side overrides) + fixed pre-existing 8-station triangle-floats-past-pentagon bug (paint 分-area before pointer). WIP graduated → DATA_FORMAT/DISPLAY/DISPLAY_E235; principles.md ×3 (upstream-fix-not-draw-side, doc-example≠capability, record-scope-into-WIP-doc)
- [2026-06-04 disclaimer refinement + OCR goes public](2026-06-04.md) — refined OCR consent popup (misfire caveat → journey section; `→` flowchart glyph tofu'd in en chrome face → vector arrows; Notice→Privacy&consent; resolution reword 解像度/分辨率); Station A redrawn as Tokyo Stn (plum SPIRED turrets not domes — imported real-world knowledge over the reference image; depth via shading not perspective for the flat scene); removed `--auto-input` dev gate → OCR band always visible, still consent-gated + default OFF; conventions.md two-font note extended to non-CJK glyphs (→ ●)
- [2026-06-04 re-entry two-probe consensus](2026-06-04.md) — re-entry commits only on 2 consecutive agreeing probes (forward-only/irreversible → lone misread sticks +1); split `_maybe_reentry` → pure `_resolve_reentry_target` + consensus, `_Detector.reentry_latch`; amber "Re-aligning…" panel chip (`panel.realigning`), `●` routed through name-font (en HelveticaNeue blanks U+25CF, both chips), `A→A` segment collapse when parked; offline-verified (verify_reentry_consensus.py 9/9, preview_panel.py)
- [2026-06-02 disclaimer trigger-timing diagram](2026-06-02.md) — new 報站觸發時機 journey section (Suica-E235 side-view A→B, forward-only fade loop not ping-pong, z-order scenery→stations→rail→markers→penguin, daytime palette on dark bg); /third-man station fix (national-read from roofline/signage not facade: Tokyo Stn de-spired, B→JR viaduct); Tokyo Tower detailed; 報站 not 廣播, ③到達 silent; assets→data/disclaimer/ (bundled); review+fix staged untracked runtime PNGs (critical_lessons §2)
- [2026-06-01 OCR disclaimer popup](2026-06-01.md) — scrollable 720×560 popup: 功能說明/使用注意 two-section, 4-step flowchart (HUD crop, hud_sample.png + cycling templates, v×t→d, animated PgDn), amber resolution+misfires notes in left column below intro, bullet-form consent; hud_sample.png new asset
- [2026-05-31 debug-panel i18n + de-jargon](2026-05-31.md) — OCR debug panel made public-facing: 3-row→2-row (80→54px), de-jargoned (Game Sync→Auto-PA, badge:→state:, Auto-fired→Auto-played, internal fire-names→public), full i18n `panel.*` en/zh_HK/zh_CN; two-font split (chrome via i18n.font + CJK station-name font, baseline-aligned) → conventions.md; chip supersedes Played on width-bound line 1; resolved `_LAYER3_HUMAN` deferred finding
- [2026-05-31 re-entry + WIP graduation](2026-05-31.md) — auto-driver re-entry (Layer 3 → Layer 2): `_maybe_reentry` silent-advances Layer 1 up to game (3C→1A, 3D→1B) + seeds flags as one snapshot; no loop (coupling read-only on Layer 1); `pending_silent_advance` signal keeps AppState mutation main-thread; `pending_next_pa`-set discriminates live-dep vs silent-reentry; subsumes mid-transit click-jump + toggle-ON; WIP_autodriver.md dissolved into auto_input/README.md; principles.md "design one rule at a time"
- [2026-05-30 click-jump re-anchor](2026-05-30.md) — auto-driver Layer 2 → Layer 1 re-anchor on lower-LCD click-jump (parked case); explicit `click_jump_pending` single-shot flag over curr_stop-polling; `prev_speed=None` clean-slate guards first-cycle phantom departure; mid-transit click-jump + toggle-ON deferred
- [2026-05-29 v0.5.4 + update-hint](2026-05-29.md) — released v0.5.4; update-hint feature (app_paths.app_version() reads exe PE metadata, fail-silent GitHub check, setup-screen pill); uv requires static version + dynamic breaks uv-run; commit co-author trailers unreliable pre-05-28 (sub-gap, non-Claude models); "Verify before claiming" gained git-history-from-proxy example
- [2026-05-29 EN full-route](2026-05-29.md) — English full-route label: 45°→60°, line_sep 15px, max_w 105px, +6px right, +2px lower; 11 Sobu/Yokosuka translations added (Kinugasa→Nishi-Ōi)
- [2026-05-29 EN8 tuning](2026-05-29.md) — English 8-station label: 45°→60°, 2-line gap 13→22px, +6px right, bar_y−3; clipping fixed on Narita Airport Terminal cells; smoke-tested 7 lines clean
- [2026-05-29 hook](2026-05-29.md) — unread_readme_hook.py PreToolUse(Glob) sensor: flags module-dir glob if README unread; self-suppresses via transcript-path scan; assets/README.md co-located; conventions.md hook-mechanism pattern
- [2026-05-29 assets](2026-05-29.md) — README screenshots regenerated (01–06); two transfer panels (Tokyo JO + Shinjuku JY); "Verify before claiming" gained filesystem-search trigger
- [2026-05-29 root-tidy](2026-05-29.md) — sweep_todo moved _dev_scripts/→_harness/ (remote workaround reverted); session_init now auto-syncs git (ff-only + divergence report); _enum_3line.py + requirements.txt deleted
- [2026-05-28 multi-res merge](2026-05-28.md) — 1080p OCR merge from other PC (stash/pull/pop); review+fix: stale resolution-gate comment + TODO [x] convention fixed; sweep_todo import path fixed after _harness/ move
- [2026-05-28 root-tidy](2026-05-28.md) — AUTO_INPUT.md → auto_input/README.md (all refs updated); compare_fonts/grid → _dev_scripts/; session-recap placement table gained auto_input/README.md row
- [2026-05-28 harness](2026-05-28.md) — route_json_hook PostToolUse sensor + validate_data.py --route; _harness/ folder (moved 7 scripts from _dev_scripts/); check_harness.py regex generalized; commit skill co-author tightened; conventions.md: sensor design principle (flag not inject) + hook UTF-8 stdout pattern
- [2026-05-28 PA](2026-05-28.md) — TODO data-task triage (transfer-info + EN + joban deferrals); chuo/1654T 32-PA trim + by-ear (7 FAIL → manual trim); verify_pa_listen.py gained interactive trim controls (mouse-drag seek bar, scrub playback, ffmpeg apply); trim_pa_silence.py onset detector v2: sustained-voice gate + adaptive threshold (5/6 better vs old); spectral flatness dead end for low-bitrate PA MP3s
- [2026-05-28](2026-05-28.md) — Deferred-findings triage: 2 removed not-real, paths re-synced to auto_input/ split; 3 fixes (jump_to_stop cross-stop transfer-reveal now intended, mixer.set_reserved(1), _sta_paused exception reset); principles.md Discussion-first gained per-item-gating-for-trivial-batches bullet
- [2026-05-27](2026-05-27.md) — 8-station continuity triangle: extends from last cell bar after 分 marker when route continues; dormant scaffolding removed; TODO.md purged 34 stale [x] items; ENGLISH EIGHT slot confirmed shipped
- [2026-05-26 nudge](2026-05-26.md) — PreToolUse(Edit) context nudge hook: one-liner reminders for display renderers/route_loader/route.json; dual-ROI framing (bug-prevention + token-savings) codified in conventions.md sensor tiers
- [2026-05-26 keihin](2026-05-26.md) — keihin/1275A PA trim: 59 files lead-trimmed (onset 0.22–2.35s), 58+1 verified PASS; verify_pa_listen head+tail playback via `play(start=...)` seek (STA pattern); pa-make skill +2 steps (trim-amount summary, "don't bash-launch GUI audio tools")
- [2026-05-26 sensors](2026-05-26.md) — Sensor buildout: `_*/` import ban + `.ttf` ban (pre-commit), `check_deps.py` (build gate), `check_harness.py` (session-init), `stale_ref_hook.py` (PostToolUse Edit); `/build` skill 6→3 steps; sensor tiers codified in `conventions.md § Tooling`
- [2026-05-26 data](2026-05-26.md) — PA silence cleaning: voice-onset detection replaces -40dB gate (noise_floor+12dB+attack-slope), `trim_pa_silence.py` + `verify_pa_listen.py` shipped; tokaido/1865E 42/42 trimmed + by-ear verified; pa-make skill Step 7 rewritten
- [2026-05-26 PM](2026-05-26.md) — Harness reduction cont'd: session-recap compressed (278→148), `sweep_todo.py` + `session_init.py` scripts for pre-digested init (3-4 Reads → 1 Bash, MEMORY.md truncated to last 10); hook ideas #3-5 parked pending OpenCode compat
- [2026-05-26](2026-05-26.md) — Harness reduction: preloaded rules compression (critical_lessons 277→91, principles 422→308, CLAUDE.md 216→170), PostToolUse commit-classification hook, distill-memory skill deleted; new principle sub-bullet on token-cost asymmetry (preloaded=compress, on-demand=rationale OK, mechanical=hooks)
- [2026-05-21 late](2026-05-21.md) — EnglishDisplay disclaimer font HelveticaNeue-Roman → Medium; English mode verified clean on sobu/1217F (disclaimer text + continuity chevrons all correct)
- [2026-05-21](2026-05-21.md) — Ralph review+fix: EnglishDisplay docstring, draw_minute_marker marker_text fix, ocr.py argmin guard; two-cycle clean
- [2026-05-20](2026-05-20.md) — code_3 edge cases checked and closed; swapped setup-screen badges for authentic Wikipedia PNG line icons; implemented full mouse interactions (hover, wheel scroll, click-to-confirm) on the route selection setup screen, and ran two rigorous review+fix cycles using subagents to eliminate duplicate code and 100% CPU spinning.
- [2026-05-15 Phase 2 editor](2026-05-15.md) — calibration editor Phase 2: Figma-style drag handles for arc waypoints (focused-only, generous hit radius, sidebar auto-jumps to dragged row); centripetal Catmull-Rom centerline replaces straight-line polyline (`_build_catmull_rom_centerline`, C1 with reflected phantom endpoints); `arc_smoothing` 3-tap averaging post-pass to relax "nodes too strong" kink perception (C2 discontinuity diagnosis); 4→7 waypoint bump (p0/p6 end-caps + p1..p5 station slots with placeholder white-circle proxies, dormant-scaffolding NOTE explains deliberate-not-tuneable until real time-circle copy from e235_1000); `--overlay <path>` CLI flag for IRL-ref image (letterbox-fit, alpha 128, Alt+drag pan / Alt+wheel-or-`=`/`-` zoom keeping aspect / `O` toggle, persisted to gitignored `_overlay_state.json` separate from calibration scratch so it survives Ctrl+S commits); WIP_calibration_editor.md Phase 2 mechanics codified via /review+fix
- [2026-05-14 evening Phase 1b arc](2026-05-14.md) — calibration editor's graduation gate (b) partially proven: E235-0 5-station view `JapaneseFiveStationDisplay` scaffold (arc-only, stations/pentagon/minutes Phase 2) wired into EIGHT slot universally; polyline-with-per-point-stroke geometry (4 waypoints × {x,y,stroke}) — initial single-radius arc model couldn't match IRL Yamanote band's variable thickness; new editor mechanics: `target: upper|lower` field on `_REGISTRY` → sidebar layout swap (right-half for lower), `waypoint` + `stroke` param-kinds with ring + bar indicators, always-on polyline preview when arc focused, click-to-row translation aware of layout; AST writeback corruption fix (forward iteration on multi-key-per-line silently truncated subsequent values on the same line — now reversed; critical_lessons entry); "model artifact's actual DOFs, not cleanest math primitive" principle codified in WIP doc; conventions.md gained interactive-editor-launch-in-bash rule
- [2026-05-14 PM calibration framework](2026-05-14.md) — calibration editor framework expanded from 1 → 4 elements (clock + prefix + station added); region-level tuneability pattern pioneered (`_TUNEABLES_*_RECT` + `RECT.update(...)` each frame); cycler hook generalized via `_REGISTRY[el]["cycler"]` (dest + prefix + station wired); `_PREFIX_FURIGANA` / `_PREFIX_ENGLISH` promoted module-level for canonical-source cycler reads; side-by-side LCDs in `_run_edit_loop` (LCD A active + LCD B locked ENGLISH via screen-ref swap); WIP doc gained Region-level / Per-element / Value-cycler / Rule-strength sections + `build` reads-canonical-source rule; principles.md § Verify-before-claiming + conventions.md § Tooling (cp1252 ASCII-safe stdout) extended
- [2026-05-14](2026-05-14.md) — native 1080p OCR feasibility probe (gitignored `_dev_scripts/_probe_1080p.py`); 2/6 references read correct at synthesized 1080p — pipeline geometry sound, failure modes are `1` thin-stroke segmentation + `9` red-template at small scale; production roadmap captured in `WIP_1080p_ocr.md`; post-refactor /review+fix found 6 dangling stale `auto_input.py` refs that third-man missed → principles.md § /third-man entry extended with pre-commit-grep sub-bullet
- [2026-05-13 PM late](2026-05-13.md) — calibration editor v1 on `feat/calibration-editor`: direct-manipulation pixel tuning (sidebar + hit-test + scratch JSON + Ctrl+S writeback, reset/scroll/swatch/ruler-indicator/mode-sync/dest-cycler/key-repeat); param-suffix convention codified in WIP doc (`_x`/`_y`/`_w`/`_h`/`_color`/`_<edge>_offset` drives visualization); `displays/utils.py:draw_text_given_width` per-char widths fix shipped to master (14b5447)
- [2026-05-13 PM](2026-05-13.md) — OCR cluster → `auto_input/` package (move-only, no internal split per third-man); `__init__.py` re-exports keep production import paths unchanged, only `_dev_scripts/` rewritten; downstream syncs landed same commit (build `$shipExclude`, AUTO_INPUT.md Files table, in-code docstrings); principles.md § "Skip-confirmation" gained "each /commit consumes its own recap" sub-bullet after I skipped recap before the second commit
- [2026-05-13 dot fix](2026-05-13.md) — Yamanote compound-dest middle dot → halfwidth `･` (U+FF65) per IRL: data swap across 3 files (route.json + translations.json keys), audio/README JY bullet codified
- [2026-05-13 AM](2026-05-13.md) — auto-input capture-region cut: production `camera.grab(region=CAPTURE_REGION_2560_1440)` + derived HUD_BBOX_IN_CAPTURE; bootstrap full-grab + shape-assert resolution gate (fail-loud, sibling of 3 critical_lessons fail-loud patterns); 6/6 OCR parity verified offline; Pyright identifier-gotcha (`FOO_<digits>x<digits>` style) codified in conventions.md § Tooling; `/third-man` self-propose rule extended with structural-refactor trigger
- [2026-05-12 later](2026-05-12.md) — pa-make cleanup + 2026-04-30 deferred-findings sweep: `data_tools/` rename completed (3 PA scripts → `_dev_scripts/`, folder removed, conventions:10 chrono fix); pa-make skill aligned with sister skills (Documentation hook + audio/README Related + STA reference points to sta-make not split-audio); 6 deferred review findings from 2026-04-30 cluster closed (3 silently resolved by 2026-05-07 triage rewrite, 3 cosmetic won't-fix); declined write-time hook to prevent TODO-rot — periodic sweep cheaper than added /session-recap step
- [2026-05-12 PM](2026-05-12.md) — housekeeping: validate_data.py compound-dest check dropped (DATA_FORMAT 2026-05-07 case-by-case reframe made hard regex unenforceable; bundled checks zero historical fire rate); principles.md Documentation strategy gained "Sync downstream enforcers when reframing doc rules"; conventions.md _* example trimmed (drive-recorder CLI → primitive-bans linter + dxcam + game-capture diagnostics); sta-make:663 validate_pa.py ref reframed cross-PC pending
- [2026-05-12 AM](2026-05-12.md) — audio/README per-line catalog (folder convention + 10 line entries + EDIT-CONTRACT); JJ WIP marker; promoted JE/JK/JO IRL facts from memory + DISPLAY; 4 skill write-gates wired (split-audio, sta-make, session-recap placement table, distill-docs in-scope); 6 sta-make edits retagged discovery-line patterns generically; principles.md Verify-before-claiming gained data-file-inspection example; conventions.md Naming gained "name by cause, not discovery instance" rule; TODO.md gained validate_data.py compound-dest stale-rule housekeeping item
- [2026-05-09 PM cont](2026-05-09.md) — debug-panel redesign (horizontal pill buttons icon+text, fire chip, line layout, distance/stopping-position split with flash, plain-English state phrase) + Layer 3 vocabulary rename (STOPPING_FRESH/AFTER_ARR/APPROACHING_BEFORE_DEP/AFTER_DEP/MOVING_AFTER_ARR → IDLE/STOPPED/DEPARTING/CRUISING/ARRIVING) + 2 real bugs fixed: chuo arrival-fire across passing-station gaps (`_expected_next_stop` helper) + Yokohama at-station press silently dropped (audio-busy gate fix in `_handle_input_real`); critical_lessons "Single-shot signal flags must be consumed AFTER successful action"; JSONL log gained at_station/cnt_pa_at_station/at_station_observed; AUTO_INPUT.md gained STOPPED-canonical-not-transient invariant + cross-layer token sharing description
- [2026-05-09 PM](2026-05-09.md) — speed-limit OCR `100`→`110` misread fix: score-gated two-try splitter (`ARGMIN_TRUST_SCORE=0.85`) replaces first-valid-wins; AutoDriver dump hook for `score < 0.75` cells → 31-frame chuo corpus shows clean 0.65-0.66 vs 0.92+ band split; mislabeled `_ocr_calibration/limit_110.png` (showed `100`) replaced with real `110` after user capture; saved-PNG vs live-BGRA divergence codified in AUTO_INPUT.md § Speed limit; KNOWN_LIMIT_VALUES dict normalized to `limit_<N>`
- [2026-05-09 AM](2026-05-09.md) — Yamanote STA refinement complete (30 stops, all in 263-370ms pre-voice gap); sta-make skill Step 12 gained hostile-recording combined-pass pattern (KAK + sta_cut + artificial silence in one ffmpeg op via anullsrc+acrossfade) + route-level gap alignment audit (audit ALL stops, not just FAILs); verifier sidebar scroll fix
- [2026-05-09 overnight](2026-05-09-overnight.md) — OCR auto-input hardening: black-screen guard (cross-attribute reject + BADGE_DIFF_REJECT=50 confidence gate) + stopping-offset cm reader (green mask, sign-via-shape, dual readers run unconditionally) + speed-limit reader (red templates 0-9 + dilated-dark fallback + 2-try splitter argmin/equal_width + 2D boundary refinement + grammar validation 25-130 km/h) + badge bbox trim (29,122,111,40 — left+top scenery scrubbed); hardening-philosophy refined to "cross-attribute primary + confidence gates secondary where gap is clean"
- [2026-05-08 PM late](2026-05-08.md) — chevron animation restructure (no-overlap two-chev + cross-row phantom-prev + Optional[List] return) + slot_w shared row spacing + font-path migration to project_root() in 4 train-model files; conventions.md "Forking a sibling-model renderer" extended with deployment-frame-inheritance sub-bullet
- [2026-05-08 PM evening](2026-05-08.md) — E235-0 APPROACHING chevron animated cascade (two-chev phase-offset 1.0s, sweep+fade-in 1.0s ease-in alpha-full at position-midpoint, fade-out 0.4s, rest 0.4s) + time-circle reverted to PASSED_COLOR + curr_stop CURRENT_COLOR overlay (no green ring, AA blending only); concept-vs-token sub-bullet on conventions.md "Forking a sibling-model renderer"; new principles.md entry "Converge on the model, not the next correction"; DISPLAY_E235.md updated for both
- [2026-05-08 PM](2026-05-08.md) — pre-commit infra: `.pre-commit-config.yaml` + `_dev_scripts/lint_primitives.py` (SysFont / `_MEIPASS` / `Path(__file__).parent` / `sys.frozen`) + Black wired; 24 files Black-reformatted, 24 alignment-critical sites wrapped `# fmt: off`; session-recap placement table split into region-scoped vs primitive-scoped; conventions.md § Tooling: stale "Black via pre-commit" rewritten + new path-resolution rule sibling to SysFont
- [2026-05-08 AM](2026-05-08.md) — principles.md cleanup: EDIT-CONTRACT added; 16 Collaboration entries (12 trimmed + 5 promoted to peer + 1 folded) + 5 more trimmed in Engineering rigor / Tooling / Data modeling; /distill-rules sibling skill created (rules-corpus audit, restructure allowed)
- [2026-05-07](2026-05-07.md) — E235-0 Yamanote sub-series: upper-LCD fork (no train-type cell) + circular full-route lower-LCD renderer (rounded-rect track, JY-code row mapping, breath-animated pentagon); boot-frame `current_time` race fix in `app.py:run()`; triage-policy rewrite (all deferred review findings → TODO.md, 22 past entries migrated)
- [2026-05-06 late evening](2026-05-06.md) — validator extension: Tier 1 transfer-data checks (lines.json category + icons exist; stations transfers slug resolution) + Tier 2 transfers_by_view ops (drop/edit slug validity + rows sum) + vibe-check pass + A1 (production `resolve_entry` is canonical, validator imports) + A2 (route-loader smoke-check) + B1 (transfer_view consumed by at-least-one stop on this route — direction matters because some station configs are forward-looking)
- [2026-05-06 evening](2026-05-06.md) — Yamanote dest-switching loader seed: route_loader.py + sticky-override closure at load time; "JSON is input grammar; runtime is closure" principle; validator gap-fills + `audio/_*/` fixture-skip; new principles.md § "Engineering rigor" with "Test the change, not just the bug" (distinct from claim-defense Verify-before-claiming)
- [2026-05-06](2026-05-06.md) — v0.5.3 release shipped + PyInstaller deployment-frame pathology codified across 3 layers (principles + vibe-check #11/#12 + review-dirty Lens 1/2); `app_paths.py` canonical helper consolidates 8 sites across 6 modules; /build Step 4 inverted to default-ship + exclusion list; /release skill replaces release.ps1; session-recap Rule 8 (cross-layer alignment)
- [2026-05-05 PM](2026-05-05.md) — transfer-info: render_transfer promoted to production (E235-1000 concrete); E235-1000 color-square policy (`_universal+color`) shipped; WIP_transfer_display.md dissolved into DISPLAY.md (~215 lines); `project_root()` consolidated to displays/utils.py; review+fix Ralph loop 2 cycles (1 critical + 9 warnings fixed)
- [2026-05-05](2026-05-05.md) — transfer-info: N=2/N=3 structural row-grouping rules + `rows` data override + Step 4 column-aware anchor row placement (with 1-vs-2 shinkansen dispatch); corpus 22/22 in-spec ✓; Preserve-named-user-frameworks extended with vocabulary-framing recurrence
- [2026-05-04 evening](2026-05-04.md) — transfer-info: GOLDEN-rule pipeline implementation + 19/22 corpus ✓; cascade vs greedy threshold split (min_inter_gap 0.5× / inter_element_margin 0.7×); sotetsu_through reclassified non_jr; MKG JO_north populated
- [2026-05-04 PM](2026-05-04.md) — transfer-info: per-N text scaling discovery (IRL ladder ≤5/6-9/≥10) + algorithm work paused pending Step 1 calibration; Scope-fidelity + Analytical-confirmation principles codified
- [2026-05-03 eve](2026-05-03.md) — Upper-LCD clip-rect refactor: region-rect manifest + `clip()` context manager → bleed structurally impossible; DISPLAY.md tightened ~58→12 lines; Verify-before-claiming recurrence note extended (3 more substrates)
- [2026-05-03 PM](2026-05-03.md) — transfer-info: name_ja_compress migration + 4 algorithm enhancements (capped row-grouping, single-row equal-spacing + side_pad formula, Rule 1 canvas-fit, blueprint placement) + 新橋/浜松町/大崎 populated; Preserve-named-user-frameworks principle codified
- [2026-05-03](2026-05-03.md) — OOBE action-history feature + ActionFlow refactor + STA seek bar + bundled-EN-fonts; Step 0 codified scope-expansion + weirdness-as-signal patterns
- [2026-05-02 PM cont 3](2026-05-02.md) — transfer-info layout: lex-maximin row-grouping + Rule 4 equal-spacing fallback + `transfers_by_view.edit` op; Pre-stated scope fences codified
- [2026-05-02 PM cont 2](2026-05-02.md) — transfer-info schema: nested variants, dot-notation refs, drop-only `transfers_by_view`
- [2026-05-02 PM cont](2026-05-02.md) — memory-system overhaul: killed log-only middle, sync codification + dedup gate, Step 0 friction loop + third-man behavioral review
- [2026-05-02 PM](2026-05-02.md) — transfer-info Rule 1/2/3 algorithm correction (per-entry asymmetric / leftmost-fit / per-segment) + implementation-completion-as-spec promoted to principles.md (recurrence #4)
- [2026-05-02](2026-05-02.md) — yellow-square nag indicator (blink + disappear-on-acknowledged + audio-busy gate) + dual-stream audio (PA on music, STA on Channel) + APPROACHING/STOPPING auto-fire asymmetry promoted to CLAUDE.md
- [2026-05-01](2026-05-01.md) — transfer-info Yokohama validated + Rule 1/2/3 refactor + through-service variant pattern (option b sibling slugs) + /third-man skill
- [2026-04-30 late evening](2026-04-30.md) — autodriver `pa_at_station` silent drain in `_fire_departure` + AUTO_INPUT.md vocabulary-discipline rules extended (arrow flows)
- [2026-04-30 evening](2026-04-30.md) — /distill pass + MEMORY.md drift traced to 4-23 + /session-recap MEMORY.md gate tightened
- [2026-04-30 PM](2026-04-30.md) — dep-classification pathology + `_dev_scripts/` rename + deployment-frame section in CLAUDE.md
- [2026-04-30 AM](2026-04-30.md) — OOBE tutorial vibe-pass: 9→8 steps, CJK chrome refactor, bilingual zh-HK/zh-CN translations shipped
- [2026-04-29 overnight](2026-04-29.md) — OOBE tutorial built (8-step walkthrough, state-jump pause convention, mixed-script renderer)
- [2026-04-29 late evening](2026-04-29.md) — chrome i18n foundation: i18n.py + language picker + setup translation + line badge + theme refresh
- [2026-04-29 evening](2026-04-29.md) — click-jump MVP shipped + autodriver Layer 3 refactor + state-machine docs split across 3 homes
- [2026-04-29 morning](2026-04-29.md) — pre_stops through-service feature + per_line refactor + Tighten-before-appending convention + omiya cut
- [2026-04-28 late evening](2026-04-28.md) — pentagon polish + autodriver STOPPING hook + final-approach-PA rule + skill chain validation
- [2026-04-28 evening](2026-04-28.md) — STOPPING state shipped: pa_at_station schema + unified state machine + lower-LCD pentagon
- [2026-04-28 PM cont](2026-04-28.md) — click-to-jump design discussion (paused); OCR state vocabulary settled; jump_to_stop verified single-purpose
- [2026-04-28 PM](2026-04-28.md) — manual distill pilot + /distill-memory skill + rules-taxonomy refinement (rule/fact/preference shape split)
- [2026-04-28 AM](2026-04-28.md) — drive recorder (JSONL) + interactive HTML report + state-machine PASSING fix + auto-commit incidents
- [2026-04-27](2026-04-27.md) — auto-input system end-to-end: dxcam HUD OCR + AutoDriver + setup-toggle + debug panel + PASSING badge classifier
- [2026-04-27 (STA)](2026-04-27.md) — saikyo + keiyo STA refresh; verify_sta_listen.py interactive trim + 0.5s pre-voice rule
- [2026-04-27 (vibe-check)](2026-04-27.md) — vibe-check cleanup + new /vibe-check skill + per-model `__init__.py` manifests
- [2026-04-26](2026-04-26.md) — doc reorganization (preloaded vs progressive split) + continuity arrows + dest-terminus fix + STA verifier UI
- [2026-04-25](2026-04-25.md) — 8-station view + LowerDisplay refactor + 2-line English station + Containment principle + Sobu Rapid 1217F end-to-end
- [2026-04-24](2026-04-24.md) — LowerDisplay global-index render + data audit + validate_data.py + v0.5.2 GitHub release + /build + /commit skills
- [2026-04-23](2026-04-23.md) — Station code badge: data/stations.json + Yamanote sta_code backfill + ModeCycler.enabled gotcha
- [2026-04-21](2026-04-21.md) — Docs deduplication, single-source-of-truth routing in session-recap
- [2026-03-31](2026-03-31.md) — Ralph loop skills implementation, key learning about agent roles
- [2026-03-27](2026-03-27.md) — CLAUDE.md reorganization, memory directory setup
