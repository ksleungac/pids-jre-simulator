# 2026-07-16

## Session: v0.6.0 build attempt → aborted; 3 incidents + interval tweak, all fixed but flagged for detective work

Tried to build + ship v0.6.0. Two smoke-test passes surfaced three bugs; release deferred ≥2 days for comprehensive testing. All three fixed today, but user wants each verified live AND the process gap closed — tracked as a block at the top of TODO.md.

**1. Clean-state first-run (language picker).** Fresh exe first-run showed the stale pre-TIMS grey `LanguagePicker`. Invisible in dev because `settings.json` is always populated locally → the `if lang not in SUPPORTED_LANGS` branch never runs. This is a deployment-frame sibling: reasoning from the developer's state, not the user's blank one. Fix (user chose "remove picker entirely"): dropped the picker, first-run → `detect_default_lang()` + persist; the TIMS home knobs own language + now persist on click. Codified `critical_lessons §6`.

**2. Version hardcode drift.** Exe displayed "054" while PE metadata was 0.6.0 — `home.py` rendered a hand-maintained `VERSION = "0.5.4"`. `app_version()`'s CONTRACT ("no hand-maintained constant") existed to prevent exactly this; the home tag bypassed it. User: *"somehow hardcoding is allowed after all this scan, review."* WHY it slipped: the scanners each target one primitive (SysFont / Path(__file__) / .ttf / deps) — none flag a version literal; and review lenses only see the dirty diff, where a committed pre-existing constant never appears. Fix: added `app_paths.display_version()` (frozen→PE metadata, dev→pyproject), `home.py` holds no constant; bumped pyproject 0.6.0. Sensor queued in TODO.

**3. Re-entry ate a live departure PA.** Started from a middle station; on departure a moving badge (PASSING) while still parked at 1C woke re-entry → silent-advance + set `departure_observed=True`, suppressing the real `FIRE_DEPARTURE`. Diagnosis friction: I mis-framed PASSING as a "misread" twice, then over-reached with a `synced` flag. User taught the model — PASSING ≡ MOVING, the 900m arrival is the ONLY difference; and the speed≥30 fast-sample silent-advance is correct by design. Fix collapsed to a one-line deletion (`or badge == "PASSING"` in `_resolve_reentry_target`) enforcing the existing invariant. Verified by trace only — needs a live drive. The over-reach → new principle bullet (enforce the existing state-machine model before adding state).

**Also:** OCR sample interval default 5s → 3s at all sites (user request); confirmed missing settings.json keys fall back to defaults everywhere (sparse override store).

## Codifications this session

- `critical_lessons.md §6` — clean-state first-run paths invisible in dev
- `principles.md § Surgical Changes` — enforce existing state-machine model before adding state
- `TODO.md` — new "v0.6.0 build-attempt incidents" block (3 items) + "Version-drift sensor" (Housekeeping)
- Domain/doc syncs: `APP.md` (picker removal, runtime map), `CLAUDE.md` (file list), `auto_input/README.md` (interval 3s, re-entry truth-table row), `i18n.py` / `tutorial.py` / `widgets.py` / `pyproject.toml` (stale picker + version comments)

---

## Session 2: Re-entry PASSING detective → root = incomplete spec; auto-driver T1 tests; layer-vocab doc drift

Continuation, focused on incident #3 (the re-entry PASSING PA-drop) — detective + testing.

**The spec was born wrong WITH the code.** The `or badge == "PASSING"` clause wasn't a code-vs-spec divergence: the README re-entry table itself said `3C CRUISING (speed≥30) or PASSING → 1A`. Spec + code authored together in `d7320b9`, fixed together in `2b3e9e1`. So the bug was an INTERNAL spec contradiction — the derived re-entry table drifted from the canonical Layer-3 truth table's `PASSING ≡ MOVING` fold. A "code matches doc" coherence test would have PASSED. Root (user's framing): **incomplete spec.** Cognitive origin: I filled the input-level "what does a raw PASSING badge mean at re-entry" gap autonomously (implementation-completion-as-spec), crossing the orthogonal axes — used a distance fact (PASSING's distance is unusable) to force a *departure* conclusion (→1A) — and equated the badge INPUT (PASSING) with a STATE (3C) in my own commit message. Eaten-PA mechanism: the wrong `1A` seeded `departure_observed=True`, which suppressed the normal `SPEED_UP_30` fire; `None` (no advance, no seed) is why the fix works.

**Layer-vocabulary doc drift (bit me live).** `conventions.md § Domain vocabulary bindings` labelled badge reads "Layer 2"; the canonical README has them as **Layer 3 inputs** (Layer 2 = driver belief derived from Layer 1). I trusted the stale preloaded binding over the doc for several rounds until the user pushed. Fixed: rewrote the binding to a pointer — "preloaded bindings point, never copy." Doc-internal canonical-source duplication, same class as the version literal.

**Testing arc.** Built 3 green T1 unit tests: `inferred_state` truth table (16 cells + MOVING≡PASSING), `update()` direct-read sequences (crossings/debounce/arrival/PASSING-skip/cross-reject/departure-through-PASSING), and the resolver grid (incl. the incident row `PASSING@speed<30,parked → None`). Recognized `PASSING ≡ MOVING` as a **cross-cutting invariant** (sites: inferred_state / update / resolver) → per-function testing gives false comfort; named it + a site-list checklist in `_tests/README`. Value ranking (user's conviction): the test that matters is **driving-record + event-series (T3)**, blocked on a recording; T1 truth-tables clarify logic but are expensive to spec-out by discussion. Split T3 into a buildable replay harness (4a) + blocked corpus (4b). **Ditched:** a single-attribute re-entry hardening (require both speed+distance before an irreversible advance) — surfaced, confirmed as the referent, dropped as a rabbit hole. Confirmed in code: re-entry only activates at Layer 1 = 1C (`at_station=True`).

## Codifications this session (block 2)

- `conventions.md § Domain vocabulary bindings` — AutoDriver binding rewritten to a pointer; "preloaded bindings point, never copy"; layer names no longer duplicated.
- `_tests/README.md § Build plan & known gaps` — auto-driver test plan (ordered, buildable-vs-blocked), value ranking, cross-cutting invariant + site-list, coherence≠correctness note.
- 3 T1 tests under `_tests/t1_unit/`.
- `principles.md § Verify before claiming` — stale-preloaded-summary example.
- `TODO.md` — re-entry incident item updated (T1 tests lock the root; live-verify + T3 replay still pending).

---

## Session 3: Language-picker detective → root = staleness-at-integration; T4 clean-frame test; review-lens rewrite (integration-residue + test-integrity)

Third incident detective (the v0.6.0 clean-first-run picker) + the testing/review-mechanism build the user asked to follow from it.

**Root is the OPPOSITE family from re-entry.** Re-entry was *born wrong* (incomplete spec). The picker was **born right, went stale**: legit in `c42c290` (the only place to pick a language pre-TIMS), made redundant when the TIMS home grew its own language knobs (`1f1018b`), and universally redundant + double-selecting once `147a56c` flipped TIMS to default — but its trigger (`if lang not in SUPPORTED_LANGS`, fires only on absent `settings.json`) never executes in dev, so the staleness was structurally invisible. Nothing was mis-implemented. **Axis-blindness** is the cognitive mechanism: `147a56c` applied the identical redundancy logic to the OOBE gate ONE line below (gated it on `args.classic` because "TIMS does its own OOBE") and missed the picker — because the OOBE gate sits on the flag axis being flipped (forced into view) and the picker gate sits on the orthogonal persisted-state axis (invisible). Its own "hardcoded-language sweep" was content-scoped (strings) and never saw the picker (a whole screen, off that axis).

**Fixture ≠ tier (user caught me re-inventing a tier boundary).** I first split "clean vs normal case" as T4-vs-T3 *tiers*; user: *"your T4 also defines integration testing geh."* Correct — T4 IS integration (same scope as T3); the distinguishing thing is the settings **fixture** (absent / present-valid / present-corrupt), an axis orthogonal to the scope ladder. T4's separate name is just a §6 checklist flag for the state-absent fixture no dev ever runs. One integration module parametrizes over fixtures; only the pure `resolve_language(dict)` logic is a genuinely separate tier (T1). Converged in one exchange (self-corrected, no third-man).

**Built:** extracted `i18n.resolve_language(settings) -> str` (pure, screen-free — the extraction is the regression guard; a picker can't come back without breaking its signature); `main.py` calls it (behavior-preserving). `test_resolve_language.py` (T1) + `test_clean_frame_startup.py` (T4, fixture-parametrized + `language_picker`-not-importable guard). Suite green (T1 4/4, T4 1/1).

**Review mechanism (user: "next app route switch should have a stale-code review mechanism").** Integration-residue smell (`review-dirty` Lens 2 #13 + `vibe-check` #13 — reachable-but-no-longer-needed, distinct from dead-code #2, FULL/INTEGRATION-scope only). **Lens 4 — Test integrity** (user's mid-turn ask): test-not-stale · feature-has-test · code-testable. **INTEGRATION scope** (auto-escalate on a flow-routing change → re-walk every state-gated branch; a duty, not opt-in) in `review-dirty` + `review-plus-fix` passthrough. `§6` gained the axis-blindness bullet + a "guarded now" pointer.

## Codifications this session (block 3)

- `critical_lessons.md §6` — axis-blindness cognitive mechanism + guard pointer (INTEGRATION scope + T1/T4 tests).
- `_tests/README.md` — "Fixture ≠ tier" note; `resolve_language` T1 fill + clean-frame T4 fill logged; normal-launch full-wiring still a T3 gap.
- `review-dirty` — Lens 2 #13 (integration residue), **Lens 4 (test integrity)**, INTEGRATION scope mode.
- `vibe-check` — smell #13 (integration residue) + stale count 9→13.
- `review-plus-fix-relentlessly` — INTEGRATION escalation passthrough (auto, not user-gated).
- `principles.md § Engineering rigor` — "Testability is a precondition" (extract-to-test; cites `resolve_language`; enforced by Lens 4).
- `i18n.py` + `main.py` — `resolve_language` extraction; 2 new tests (`test_resolve_language.py`, `test_clean_frame_startup.py`).

---

## Session 4: release-prep `/review+fix` over new-since-v0.5.4 (setup_tims focus) — 6-agent scan, obvious-safe fixes inline, needs-context to TODO

First real exercise of the review mechanism built in blocks 2–3, now as a **release-prep whole-code scan** (not a dirty-diff loop). Scope = every production file changed since `v0.5.4` (~13.7k lines / ~40 files: the `setup_tims/` package, `status_band`/`widgets`/`tims_chrome`, the E235-0 fork, `auto_input/driver`, `setup.py`, entry rewiring). Fanned out **6 parallel opus reviewers** (read-only), each applying review-dirty lenses 1–3 + derivation-bypass scan + integration-residue #13 + the vibe-check smell list, FULL scope, Lens 4 test-integrity exempted (user's call).

**Two mid-turn steers reshaped the run** (→ both codified into the review+fix skill):
1. *"your fixer does not have context … might introduce new bugs, so delve to TODO for nonobvious tasks."* — most of this surface was built by prior sessions, so I lack design context. Gate each finding on **fix-confidence** (obvious-safe → apply; needs-context → defer), not file-ownership. Had every reviewer tag its findings with the flag.
2. *"reviewer should take advantage of vibe check."* — folded vibe-check's codebase-mess smells into the reviewer brief (the right lens for release-prep, vs review-dirty's diff-orientation).
3. *"focus mainly on tims setup … E235-0 should be fine (many testng already)."* — scoped fix-effort to TIMS/chrome/entry; E235-0 + auto_input findings logged-but-not-fixed.

**Zero architectural-critical / critical** across all six → loop terminated in one cycle (warning/info route to deferred-logging, don't block). **Applied 17 obvious-safe fixes / 9 files**: palette-drift derivations (`TITLE_COLOR`→`CYAN`, `CODE_COLOR`→`CODE_INK`, `LINE_INK_GRAY`→`chrome.DISABLED[...]` — which **closed 2 pre-existing deferred-findings** from the earlier harness sweep), a real behavioral fix (`ocr_setting.run_consent` now `_load_state()`s → consent/how-it-works view shows the user's saved interval/lead, not module defaults), dead-code removal (`STRIP_INK`, `BTN_BG`/`BTN_BORDER`/`_font_shingo` — grep-confirmed zero callers), `NEXT_FLASH_MS` extraction, and a sweep of **feature-graduation-stale docstrings** (every setup_tims screen still said "DRAFT / not yet wired into main.py" months after becoming the DEFAULT flow — an instance of smell #8/#13). Verified: py_compile + T1 4/4 + T4 1/1 + both primitive linters clean.

**18 deferred → TODO new section.** Of those, 3 are real potential bugs out-of-focus (worth a pre-ship look, per steer left unfixed): e235_0 5-station view doesn't skip passing stations → future `None+int`; `driver.py:941` bg-thread RMW on `state.cnt_pa_at_station` (single-writer break); `ocr.py:789` unscaled `DIGIT_MIN_W` drops the 1080p speed-limit cue. The deferred-findings mechanism validated itself — logged debt (the palette dups) got closed when the code was next touched.

## Codifications this session (block 4)

- `review-plus-fix-relentlessly` — **fix-confidence gating on surfaces you didn't build** (obvious-safe → apply / needs-context → TODO; deferral ≠ dismissal); **release-prep scope folds in `/vibe-check`** (codebase-mess lens, not for dirty-diff).
- `TODO.md` — 2 deferred-findings closed (`[x]`); new "release-prep whole-code scan" section (18 items, grouped TIMS / entry / E235-0 / auto_input).
- Code (uncommitted at recap time): 17 obvious-safe fixes across `tims_chrome`/`status_band`/`tutorial`/`setup_tims/{home,model_select,route_select,pa_setting,ocr_setting,tutorial_basic}`.
