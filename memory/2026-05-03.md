# 2026-05-03

## Session: OOBE tutorial refinement + mid-session /third-man rescue + skill tightening

Tutorial-pass session — translations, action history feature, fonts. Two friction points landed mid-session:

1. **Step 2 strikethrough off by one.** `cnt_pa_at_station` is the INDEX of the last-fired entry (-1 sentinel before any press), not a press count. Off-by-one on the threshold gate.
2. **Step 3 timing model misread.** User's mental model: history strikethrough on press, next active prompt only after prior audio finishes (mixed timing). Claude pre-rejected this in private thinking as "broken UX" (the in-between empty active area felt wrong) and reverted to fully audio-completion-based when user added the "after audio stops" constraint for one specific transition. User invoked /third-man with explicit frustration ("hard-coding... everything seems to break"). Third-man surfaced (a) the mixed model IS the design — empty in-between is the deliberate listening pause; (b) per-step branchy code was the brittleness root cause.

Refactored to single `ActionFlow` dataclass driving active prompt + history + predicate from one builder method. Per-step logic lives in `_step_action_flow` only; collapsed three predicates into one shared `_pred_action_flow_complete`. Step 3's "cut" prompt is the documented `mid_play_stages={2}` exception.

Other work: STA seek bar fix (pygame.Channel has no get_pos() equivalent — added wall-clock tracking via `time.monotonic()`); bundled HelveticaNeue for EN chrome across picker + setup (was SysFont JhengHei, varied across systems); zh-CN tutorial chrome bumped to YaHei Bold (Regular too thin vs ShinGoPr6N Medium baseline used for zh-HK).

Recap-time Step 0 ran. Two patterns codified — claude's coherence-instinct ("make both axes consistent") and weirdness-reaction ("this design is broken") share the same root: treats internal coherence as authoritative when it should be a signal to re-check user words. Both extended `Ground reasoning in the user's stated terms` rather than new entries. Pattern 3 (per-step branchy code → ActionFlow refactor) was rejected for codification by third-man — engineering shape, not behavioral; insufficient recurrence; risks contradicting `Pragmatic over perfect`.

Recap-skill itself updated mid-pass: Step 0 → Step 1 routing was implicit (Step-0 codifications belong in New Understandings with `→ propose:`, not Corrections), and brevity wasn't enforced — both fixed. Commit-skill /session-recap-first gate strengthened from conditional ("if last commit, ask") to default ("always run unless explicit waiver") per user's "always /session-recap first" framing.

## Codifications this session

- `principles.md § "Ground reasoning in the user's stated terms"` — 2 new How-to-apply bullets (Scope-expansion guard + Weirdness-as-signal) from /third-man Step 0 review.
- `/commit` skill § 4 — /session-recap-first gate strengthened to default-on.
- `/session-recap` skill — Step 0 → Step 1 routing made explicit; Step 1 brevity instruction added.

---

## Session (eve): Upper-LCD clip-rect refactor

User opened with "would TSX/JS/CSS be better than pygame?" — distribution determinism + native audio/OCR coupling rule it out; transfer-info pain is the IRL-mimicking layout algorithm itself, not pygame's fault. That reframed the question to: *what about the clear-rect bookkeeping, then?*

Walked through patterns and walked back the over-built ones. Sketched an instrumented-region framework with overflow warnings + state-sweep harness; user pushed "think more potential problems"; framework collapsed under honest stress-test (font.size() lies, exhaustive enumeration is its own project, intentional-overlap exceptions, dynamic regions don't fit static manifest, bypass paths). Settled on the minimal version: `set_clip` context manager + per-region rect manifest. Each region's draw method wraps in `with clip(...)` — pygame drops out-of-clip pixels at the surface layer, structural guarantee against bleed.

Refactor landed across `displays/utils.py` (clip helper), `upper_lcd.py` (7 rect constants, 11+ method wraps, simplified draw_station), `DISPLAY.md` (Element confinement section collapsed ~58→12 lines, removed D1/D2 distinction + probing methodology — clip makes both moot). `lower_lcd.py` deliberately untouched: opaque-cell paint pattern + dynamic positions don't have the upper's tight-band bleed concern.

`/review+fix` cycle 1 caught 4 findings (none blocking); applied 3 inline including a factually-wrong badge comment claiming y<35 paint when the badge actually starts at y=37.

### Friction trace

Three verify-before-claiming slips this session, all corrected on first pushback (no defensive re-justification): pygame redraw-pattern misdiagnosis claimed partial redraw existed (it doesn't — full redraw at 15 FPS); `font.size()` direction backwards (over-reports vs ink, never under); badge y-range comment wrong despite the constants being defined a few lines above. Same cached-impression pathology from yesterday's recurrence note. Existing principles.md entry already covers it; just extended the recurrence note.

## Codifications this session (eve)

- `principles.md § Verify before claiming` — recurrence note extended with three 2026-05-03 substrates.
- `DISPLAY.md § Element confinement (clip-enforced)` — replaces old "Element Clear-Background Convention" section.
- `displays/train_models/e235_1000/upper_lcd.py` — region rect manifest at module top, `clip()` wrap convention; `# CONTRACT:` block updated to point at the manifest.
- `displays/utils.py` — `clip()` context manager added.

---

## Session (PM): Transfer-info — name_ja_compress migration, algorithm enhancements, 3 new stations

Three threads in parallel: data migration, placement-algorithm enhancements, population of 新橋 / 浜松町 / 大崎.

### Data work

- **`name_ja_compress` field** added to `lines.json` (per-line, optional, default 1.0). Two-tier char-count heuristic in `render_ja` deleted; entries that previously triggered compression got their measured factor written into the data. Tokyo / Yokohama / Shinagawa renders are byte-identical pre/post. Best-effort attribute — only set when an IRL LCD reference exists.

- **3 stations populated**:
  - 新橋: JY/JK/JT/UT.tokaido + Ginza/Asakusa/Yurikamome. New line slugs: `ginza`, `asakusa`, `yurikamome`. JY_inner drops JK/JT/UT (parallel-runners southbound).
  - 浜松町: JY/JK/Tokyo Monorail/Toei Asakusa. New slugs: `tokyo_monorail`, `oedo`. Wide entries (Monorail) ordered first so narrow Ōedo lands on the right anchor without canvas overflow. JY_inner drops JK.
  - 大崎: JA/JS/sotetsu_through/Rinkai. New slugs: `saikyo`, `sotetsu_through` (universal icon, "相鉄線直通"), `rinkai` (icon at `data/line_icons/rinkai.png` extracted from `lcd_references/Rinkai_Line_symbol.svg`). JY_inner drops JA. IRL uses (2,1) split — algorithm currently picks (1,2) (open follow-up).

### Algorithm enhancements (all preserve Rules 1-4 cascade)

1. **Capped lex-maximin row-grouping** — `gap_cap = 2·margin_x`, padded to `max_rows`, `-num_rows` tiebreak, uncapped fallback. Fixed (1,1,1) regression for sparse 3-entry single-row cases.
2. **Single-row equal-spacing for row 0** when `len(rows) == 1`, with `side_pad = (W − Σ) / 14` to bias sides slightly larger than inters per IRL preference.
3. **Rule 1 canvas-fit defensive check** (k ≥ 1) catches wide row-R entries inheriting narrow row-0 anchors. Caught Hamamatsuchō flat overflow once Tokyo Monorail's 330px width was introduced.
4. **Blueprint placement enhancement** — when max-Σ row is NOT row 0, that row is placed first via equal-spacing as the cascade seed instead of waiting for reactive Rule 4 + track-back. When max-Σ IS row 0, behaviour unchanged. Rules 1-4 cascade fully preserved for non-blueprint rows; Rule 4 track-back skipped only in blueprint mode (since shifting the seed would break it). Hamamatsuchō flat improved (yamanote at x=90, balanced sides). Shimbashi flat regressed cosmetically (rows 0/1 cramp left when blueprint=row 2) — accepted, follow-up logged for Rule 1 enhancement.

### Friction (codified silently)

Two `/third-man` invocations:

1. **side_pad formula direction**: claude proposed `side_pad = 10 × n` (more padding for more elements), Socratically debunked. User reframed to "depends on required_width, not n" and `side_pad = (W − Σ) / 14` landed. Caught in one round; not codified separately.

2. **Blueprint scope misread**: claude presented (2) as "more principled framework — eliminates the Rule 4 special-case" and implemented full structural replacement when user said "Go (2)". User: *"don't void my rule 1-4 cascade concept... I am just discussing the enhancements all the time."* Third-man verdict: user's vocabulary was enhancement-shaped throughout; claude's option-menu framing injected the replacement read. Reworked with cascade fully preserved. Codified in `principles.md § Preserve named user frameworks` — new entry covering "don't smuggle structural replacement into options labeled 'more principled.'"

## Codifications this session (PM)

- `principles.md § Preserve named user frameworks` — new entry from blueprint scope-misread.
- `WIP_transfer_display.md` — algorithm narrative updated (4 layers including blueprint placement); follow-up #0 rewritten as two enhancement candidates for tomorrow (category-respecting tiebreaker for 大崎 (2,1); Rule 1 head/tail-distribute for sparse-against-wider-blueprint).
- `DATA_FORMAT.md § lines.json` — `name_ja_compress` field row.
- `data/lines.json` + `data/stations.json` + `data/line_icons/rinkai.png` — 5 new line slugs, 3 new station entries.

---

## Session (evening): Transfer-info hookup onto LowerDisplay

Wired the transfer-info renderer (built across prior sessions in `preview_transfers.py`) into the live lower LCD as a third view in the cycle. Spec gathered through Q&A, then implemented as parent + concrete subclass + cycle rewiring + 16 route.json metadata edits.

**Architecture decided:** parent class in `displays/transfer_info.py` (filter logic + state binding + LCD interface) + per-train-model concrete in `displays/train_models/e235_1000/transfer_info.py` (rendering, calls `preview_transfers.render_transfer` as scaffolding). User explicitly asked for real inheritance from day one despite only one concrete model — "the actual full implementation is in the e235 folder, while root is parent" — so future train models drop in by mirroring the e235 file. First display module with parent/child split (UpperDisplay/LowerDisplay are concrete-only because their bodies are entirely per-model rendering).

**Cycle replaced 2-state boolean (`_is_eight_view`) with slot-list architecture:** `FULL`/`EIGHT`/`TRANSFER` slots, per-slot durations (12s/12s/6s), `_available_slots(state)` computes membership from (window, lock, transfer-availability), `_tick_cycle` advances + reconciles when current slot drops out, `_handle_at_station_edge` force-switches to `TRANSFER` on STOPPING entry with boot-guard via None-sentinel for `_prev_at_station` (so boot's at_station=True doesn't auto-fire).

**Three correction iterations during the work:**
1. Single-PA stations didn't trigger transfer-info window — initially used `state.is_last_pa or state.at_station`, but `is_last_pa` is only set in `_next_in_approaching` (multi-PA path); `_advance_to_next_stop` auto-fires pa[0] for single-PA stations and hardcodes `is_last_pa=False`. Fix: derive `cnt_pa >= len(pa)-1` directly. Recurrence shape of Verify-before-claiming — trusted flag-name without tracing all setter sites.
2. Empty transfers list crashed `render_transfer`'s `max()` (e.g. 鶯谷 on JY: own-line filter drops `yamanote`, JY_inner view drops `keihin_tohoku`, leaves []). User asked for cycle-skip rather than blank-slot UX. Fix: `_station_has_transfers` predicate gates TRANSFER membership in `_available_slots`. Same shape as 佐倉 crash from earlier session — wrapping component should consider edge cases the wrapped one already had.
3. /review+fix surfaced doc + comment drift (DATA_FORMAT.md schema gap, DISPLAY.md cycler section 2-slot stale, `_SLOT_DURATIONS` comment referencing wrong predicate basis). All fixed in-cycle.

## Codifications this session (evening)

- `conventions.md § Display module structure` — new subsection from user's parent/child framing.
- `DATA_FORMAT.md § route.json Route-Level Fields` — `line_code` + `transfer_view` field rows.
- `DISPLAY.md § View cycler (LowerDisplay)` — section rewritten for 3-slot rotation (slot-membership table + window predicate + force-switch + reconciliation + invariant).
- Inline `_in_transfer_window` docstring at `lower_lcd.py` — captures the cnt_pa-vs-is_last_pa rationale (single-PA stations).
